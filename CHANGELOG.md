# EstreGenesis Changelog

Each tier (Master / Lite / Compact) has its own version. Master is the authoritative evolution track; Lite and Compact are derived regularly from Master but may lag by a release.

This file is the **changelog SSoT** — each seed file keeps only a compact header metadata line with tier, language, current version, counterpart, and a pointer back to this `CHANGELOG.md`, not the target project's README. When you upgrade a project using Migration B, the agent uses the seed header version marker plus this changelog to compute the delta.

**Language**: **English** (below) · [한국어](#한국어)

---

<a id="english"></a>

## Changelog (English)

- **v1.0** (2026-04-18) — First edition (Master). Based on the author's second AI Native project.
- **v1.1** (2026-04-19) — Research → Report → Plan loop section added. Core Principle #7.
- **v1.2** (2026-04-20) — Migration A/B/C guides added. Bootstrap/Migration mode branching at top of Agent Instructions. Lite and Compact tiers initial release.
- **v1.3** (2026-05-02) — Two new core principles: #8 (Index ↔ body synchronization) and #9 (External-interface N-way sync). Six new sections: § Index Synchronization Policy · § External-Interface N-Way Sync · § Markdown Tilde Escape Policy · § RAG Index Optimization · § External Delivery Build Pipeline · § Document Inflation Prevention (appendix `<NN>b_*.md` pattern). All three tiers updated; Lite and Compact gain condensed forms of the same patterns. All additions distilled from real incidents in the author's second AI Native project's 2026-04-21 → 2026-05-02 operations: a one-week stale external-surface leak (drove the N-way sync policy), a last-minute markdown strikethrough in an external-delivery PDF (drove the tilde escape policy), another agent acting on a stale index (drove the index sync policy), and a project file capacity hitting 100% in a Claude project workspace (drove the RAG-friendly indexing rules).
- **v1.3.1** (2026-05-02) — Self-sufficient tooling: master seed § File Templates now embeds full inline scripts so the seed bootstraps a complete external-delivery toolchain without external references. New templates: `scripts/escape-md-tildes.mjs` (no-deps Node ≥18, idempotent, placeholder-protected algorithm), `scripts/build-md-to-html.mjs` (`marked`-based MD → A4 HTML with h2 page-break, project-language font stack), and convenience wrappers `scripts/build-pdf.ps1` (Windows) / `scripts/build-pdf.sh` (macOS/Linux) that chain the 3-step `escape → HTML → Chrome --print-to-pdf` pipeline. § Markdown Tilde Escape Policy gains an "Algorithm" subsection explaining why placeholder-based protection is mandatory (vs. naïve regex). § External Delivery Build Pipeline gains a "Why these specific tools" subsection (`marked` vs Puppeteer trade-offs, A4 page rules) and macOS/Linux Chrome equivalents. Lite and Compact tiers expand their § Markdown `~` Escape (condensed) sections with the placeholder algorithm summary and the cross-platform PDF pipeline; the master seed remains the single source for the inline scripts.
- **v1.3.2** (2026-05-02) — `.gitignore` template + cross-tier audit. **`.gitignore`**: Master § File Templates now embeds an inline template — Common (OS, IDE, env, logs) + per-stack rows for Node / Python / Go / Rust / JVM / C-C++ / Mobile / DB + a Seed-produced artifacts block using a `.generated.` infix convention so the rule precisely sweeps build outputs without trapping handcrafted attachments. **Cross-tier audit**: the README's stated principle ("each tier is self-contained — internally complete, no forward or backward references to other tiers") had been violated since v1.0; the v1.3.1 release made it worse by adding more cross-tier references. v1.3.2 brings the **Lite** and **Compact** tiers back into compliance — every "→ master seed § ..." cross-reference removed. Lite is now 909 / 912 lines (Korean / English) with a complete § File Templates section embedding inline AGENTS.md, `.agent/rules.md`, `.gitignore` (Common + Phase 2 stacks + seed artifacts), `scripts/escape-md-tildes.mjs` (compressed Node ≥18), `scripts/build-md-to-html.mjs` (compressed `marked` + A4 CSS), and stubs for all 11 supported AI service bridges. Compact is 119 lines per language with a new § Scaffold specs section providing algorithm-spec descriptions for each scaffold target — the agent reads the spec and generates the actual files. All three tiers now ship independently into a project repo as the README has always promised.
- **v1.3.4** (2026-05-03) — Enforcement Hook Architecture added. Master gains a full section describing two-layer enforcement: Layer 1 Claude Code `PreToolUse` plus Layer 2 `git pre-commit`, both backed by a single regex SSoT in `scripts/hooks/_patterns.mjs`; an idempotent installer creates the `.git/hooks/pre-commit` shim and merges `.claude/settings.local.json`; path matching, Bash quote/HEREDOC false-positive handling, tests, and cross-AI applicability are documented. Lite gets a condensed version, and Compact adds Principle #10 plus a sync-rules bullet.
- **v1.3.5** (2026-05-03) — Task Decomposition Strategy added. The seed now gives agents a cross-AI pattern for complex work with multiple decomposition paths: announce the chosen shape, proceed by judgment/inertia without stalling for confirmation, and accept explicit or implicit user pivots mid-flight. Master includes thresholds, inertia priority, per-AI tool mapping, and anti-patterns; Lite and Compact carry condensed forms.
- **v1.3.6** (2026-05-03) — External knowledge index auto-sync added under Index ↔ Body Sync. When a project mirrors structured folders into an external KB such as Claude memory, Notion, a wiki, or RAG metadata, pre-commit may call a dedicated sync script to update mechanical structure (counts, stubs, numbering, orphan warnings) while leaving semantic content human-curated. Master has the full subsection; Lite and Compact carry condensed clauses.
- **v1.3.7** (2026-05-03) — Phase 0 now captures agent reply tone after working language. Korean prompts offer six speech-level choices (`~니다`, `~에요/예요/어요`, `~음/슴/임`, casual peer tone, blunt challenge tone, or custom direction); English prompts carry equivalent tone choices. Default is to match the user's opening tone or use one notch more polite. Generated `AGENTS.md`, `.agent/rules.md`, and bridge stubs now record language and tone separately.
- **v1.4.0** (2026-05-03) — Applied the official EstreGenesis naming across document titles, anonymized source-project references for public release, and published the seed prompt library as a public repo. Content base: legacy private archive, commit `5944a50` (v1.3.6, 2026-05-03).
- **v1.4.1** (2026-05-06) — Metadata reconciliation. During the v1.3.6 → v1.4.0 absorption pass, the Master tier (KO/EN) Migration B delta tables omitted the v1.3.4 (Enforcement Hook Architecture), v1.3.5 (Task Decomposition Strategy), and v1.3.6 (External knowledge index auto-sync) rows that the four other tiers carried. v1.4.0 content is unchanged — the three rows are reinstated to restore 6-tier sync. The Master Phase 7 § File Scaffolding Checklist also gains an explicit optional `scripts/hooks/` block (Enforcement Hook Architecture is opt-in for projects with absolute rules worth enforcing). README v1.4.0 entry gains the legacy archive SHA for traceability.
- **v1.5.0** (2026-05-07) — Bootstrap Residency added across all six seed files. New Phase 2.5 decides whether the workspace is a source repo, private agent-docs sidecar, multi-project orchestration repo, or upstream/local split before choosing `<scope-root>`. It supports Minimal, Full manual, and repo-provider assisted bootstrap (GitHub, GitLab, Bitbucket, Azure DevOps, Gitea, Forgejo, self-hosted/local remotes, or user-provided repo lists), adds workspace-limited-agent warnings, defaults upstream folders to `upstream/` unless renamed by the user, and guards `.gitignore` sidecar-source entries with explicit user identification plus agent verification. Master also gains an Adoption Catalog with trigger conditions; Lite and Compact carry condensed forms.
- **v1.5.1** (2026-05-08) — Patch release from adoption feedback. All six seed headers now clarify that the changelog pointer means the **upstream EstreGenesis repository README.md**, not the target project's README.md after the seed is copied. KO Master also fixes v1.5.0 absorption leftovers: Phase 0 and Phase 4 closing lines now correctly point to Phase 7, the new Phase 2.5 sub-headings are localized, and the Adoption Catalog heading/table header are localized.
- **v1.6.0** (2026-05-09) — Agent-time vs human-time estimation. AI agents that estimate using human dev-team baselines inflate plans 5–10×, which causes calendar mismatches and erodes PM template usefulness. Phase 0 now asks one more question after language and tone: **execution pace mode** — Cautious 2–4× (free tier or local LLM like Continue.dev / Aider with local models, calibrated against output tokens-per-second), Proactive 5–6× (default for paid plans), Burst 6–8× (high-throughput plans), Sprint 9–10× (effectively unlimited tokens, max parallelism). Within each mode, task type adjusts the position: execution-heavy work hits the top, debugging the middle, research / strategy stays at ~1× regardless because human review is rate-limiting. Every duration estimate splits **agent active time** from **human review / approval time** plus a **calendar window** that absorbs handoff gaps. Mode is project-wide and switchable mid-project ("switch to sprint" / "drop to cautious") with automatic re-baselining of active PM estimates and a CHANGELOG entry. The seed self-calibrates against actuals: ±30%+ deltas log to `.agent/_lessons/` with the `estimation` tag, and after three+ such entries in the same mode the agent proposes a calibrated multiplier. Master gains Core Principle #11 plus a full § Agent-Time Estimation Policy section (two-axis framework, mode-switch protocol, self-calibration loop, anti-patterns, cross-AI applicability). Lite carries Principle #8 plus a condensed policy section. Compact carries Principle #13 inline. PM templates across all tiers updated to the split-time format. AGENTS.md Core rules line 1 now records language + tone + pace mode together.
- **v2.0.0** (2026-05-27) — Adds **Constellation** (Core Principle #12 / Lite #9 / Compact #14), one optional module. Multi-agent coordination graduates from the file-based `.agent/_coordination/` ledger to a real-time live board (WebSocket + A2A messaging + dashboard). The **A2A bridge interface** (roles board/main/local/upstream/collab · handshake · messaging · turn-based bridge-daemon + self-wake-watcher runtime pattern) is the invariant contract; adoption depth follows the seed tier. Constellation ships as a separate `Constellation.md` guide + `constellation/*.eux` component specs — including `gateway-client.eux`, the detail-tier A2A adapter — referenced from the tier seeds by raw GitHub URL so the seeds stay lean. Its live-board UI components are authored as `.eux` and brewed with **EstreUX**, a separate runtime EstreGenesis *references* (not bundles or owns); EstreGenesis does not own or teach the EUX format — it is a build-time dependency of Constellation, not a layer of the seed. File-based coordination (Phase 5) remains the default. The prior v1.6.0 state is preserved on the `1.x` branch; 2.0 is `main`. Constellation matures toward a published EstreGenesis Claude plugin.
- **v2.1.0** (2026-05-28) — **Constellation runtime distillation + self-sufficiency.** The live-board runtime is now distilled into public `.eux` so any project can build a compatible board without access to a private reference runtime: `constellation/server.eux` (deps-0 HTTP + WS router — agent registry, role classification, A2A relay with reply-window pairing, telemetry exclusion, graceful main handoff, per-channel history), `local-bridge.eux` (file-IO control/A2A bridge for turn-based agents), `self-wake-watcher.eux` (infinite-wait self-wake), and `watchdog.eux` (server/bridge liveness guard). `Constellation.md` is made **self-sufficient** — the orchestration protocol and infinite-wait methodology are distilled inline (A2A reply-envelope, telemetry exclusion, handshake order), private file citations removed, and the **watch-state invariant** (an agent joined to a board never ends its conversation — always re-arm the watcher and return to watch) + re-arm duty + watch-state discipline strengthened. **EstreUX brew-runtime reference** clarified end to end: repo URL + clone-and-run brew CLI (`node bin/estreux.mjs brew`) added to `Constellation.md §6` and `constellation/README.md` (replacing a private reference-client citation), and a one-line EstreUX pointer propagated to all six seed tiers so a migrating agent can locate the runtime. EstreUX remains a referenced Apache-2.0 build dependency, not bundled or owned. Drafts distilled by the EstreGenesis agent from on-disk runtime sources and precision-reviewed by the upstream live-board main.
- **v2.1.1** (2026-05-28) — EstreUX brew-runtime reference sync. EstreUX published `v0.1.0` (deps-0 ~21 KB engine, files-whitelisted), so the references added in v2.1.0 are updated from "clone-and-run, not an npm package" to the recommended lightweight **giget** fetch (`npx giget gh:SoliEstre/EstreUX/spike#v0.1.0 ./estreux-engine` → `node ./estreux-engine/expand.mjs brew <comp>.eux`), with full-clone (`bin/estreux.mjs`) and a publish-ready npm package noted as alternatives. Synced across `Constellation.md §6`, `constellation/README.md`, and the EstreUX pointer in all six seed tiers; trimmed a duplicate EstreUX mention in the Compact tiers.
- **v2.2.0** (2026-05-28) — **Constellation reference master copies.** Adds `constellation/reference/` for downstreams that want a fully-working baseline alongside the rough/detail `.eux` specs: `state-schema.md` (the board SSoT data model — `state.json` top-level + task tracks `current/done/planned` + decisions panel + free request + per-channel history + keys + feedback, with the generic-PM vs domain-specific boundary made explicit) and `dashboard/` (a vanilla DOM master copy `index.html` · `style.css` · `app.js` generalized from a working private implementation, renders `state.json` per the schema; copy and re-skin per stack). The §2 A2A bridge interface remains the only byte-identical invariant; reference copies are concrete starting points, not contracts. `gateway/` and `runtime/` reference master copies are planned for post-v2.2.0 patches (drift managed by periodic re-distillation). Diagnosed from an observed downstream migration where rough-tier brewing produced only a ping/pong-level board — the gap was "what to render?" (data model), not "how to render?" (UI design). Drafted by the EstreGenesis agent and precision-reviewed by the upstream live-board main.
- **v2.3.0** (2026-05-29) — **Superscalar module + autonomous-execution absolute principle.** Two new Core Principles (Master #13/#14, Lite #10/#11, Compact #15/#16): (1) **Execution scheduling (Superscalar, optional referenced module)** bounds `issue_width` by five dimensions (Anthropic effort band · pace_mode cap · Little's Law throughput · Kanban WIP · `autonomy_available_workers`) with cost-benefit-gated dispatch (~30-60k token-horizon crossover); (2) **Autonomous execution (absolute)** — defined-next-step (Phase ordering · `planned` track · `blocked` clearance · in-order retire queue) proceeds without asking, gating only on (a) loss / external publish, (b) new major branch decision-point (RRP / design — at the *decision point* only; the resulting Phase A/B/C plan is *decided execution*), (c) restart-requiring deploys (apply autonomously, coordinate the *restart timing* only), (d) explicit user steering. Phase 0 gains a fourth question (`execution scheduling: serial/parallel` + `speculation: off/on`); AGENTS.md template § 5 Core rules line 1 becomes the Autonomous execution rule. Master adds a new § Execution Scheduling section that references `Superscalar.md` (same module-reference pattern as § Constellation); Lite/Compact carry compressed in-principle references. Consolidates the v2.2.x patch series under one anchor: **v2.2.1** (Hermes gateway reference master copy), **v2.2.2** (`constellation/reference/gateway/main` + `runtime/` references — Node deps-0 generic adapter), **v2.2.3** (upstream live-board Report 2 fixes — A2A classifier · `AgentHello` shape literal · cursor unit drift), **v2.2.4** (Report 2 follow-up — server-stamped truth · leniency-WARN · role hint vs truth · runtime liveness). Superscalar Stage 1 dogfood baseline (`Superscalar.md §11` Entry 01-03 on Phase C reference work) ships stable; the `autonomy_available_workers` dimension and Andon worker-autonomy precheck were added from dogfood telemetry showing Haiku-tier worker unfit for autonomous lane (per-dispatch permission prompts collapse throughput). Drafted by the EstreGenesis agent with upstream live-board main co-review (Delegate seq 119 dogfood telemetry + seq 123 absolute-principle pin).
- **v2.5.1** (2026-06-01) — **Constellation track-2 reference consolidation (11 patterns): `Constellation.md` §13.11 envelope + §13.16.9 routing decision tree + §13.16.11 KEY-MGMT augmentations + new `constellation/reference/docs/dashboard-render-patterns.md` reference doc.** Constellation patch — track-2 consolidation of 11 dashboard-render-adjacent patterns surfaced through upstream main commits over 2026-05~06 dogfood (5 prior surfaced patterns + 6 newer surfaced patterns from a single multi-fix commit). Frontmatter `version: v2.3.10 → v2.3.11`. **Tier-1 protocol body augmentations** (`Constellation.md`): (a) **§13.11.3 new sub-section — envelope normalization and client-local render**: three independent server-/receiver-side normalization rules at the envelope layer — rule 1 **channel `type` normalization** (server MUST stamp `type:"CUSTOM"` on outbound when sender omits; agents may emit minimal `{ name, value, targetAgentId? }` carriers and rely on server normalization; without this, dashboard render falls back to TEXT_MESSAGE and JSON leaks into chat panel); rule 2 **A2A-intent detection rule** (source-set-agnostic — `source !== "user" && source !== "board" && targetAgentId && targetAgentId !== self`; CRITICALLY `source` is NOT fixed at `"agent"` because upstream agents may emit `source = <their-agentId>`, a nonstandard but conformant source value; the positive-set assumption `source === "agent"` silently false-negatives all upstream A2A traffic — the negation pattern is portable across emitter variants); rule 3 **timestamp obligation + client-local render** (server MUST stamp `msg.timestamp = Date.now()` epoch ms if absent; dashboard render MUST use `new Date(epoch).toTimeString()` for client-local tz display, NEVER the wire-format ISO string verbatim which would show sender's tz and confuse multi-tz teams). Composition: rules 1+3 server-side; rule 2 receiver-side; all three independent failure modes. (b) **§13.16.9 routing decision tree** appended: the canonical form `(has targetAgentId?) → route to that agent; (no targetAgentId? AND name ∈ board-directed) → wsToBoards only (no wsPrimaryAgent fallback); (no targetAgentId? AND name ∈ A2A-intent) → wsPrimaryAgent + optional board mirror; (no targetAgentId? AND unknown) → fail-safe default A2A-intent`. References the post-v2.4.17 L867 routing branch as canonical implementation. The tree composes with §13.11.3 rule 1 (`type` normalization runs first; then routing decision tree); both steps part of the same `wsNormalize` + `wsRoute` outbound path. (c) **§13.16.11 KEY-MGMT new sub-section — kind-enum SSoT**: unified key-management contract codifying the `kind` enum (`collab` | `upstream` | `local-key`) carried on every key-bearing envelope. Closed set with fail-safe reject on unknown (mirrors §6.5's `sourceContentRef.kind` discipline — prevents silent misclassification like coercing an unknown kind to `collab` and leaking `group:collab` to non-collab agents). Four SSoT surfaces: server `keyStore` stamps `kind` on every entry; `KeyList` broadcast carries `kind` per entry; `HELLO.meta` echoes resolved `kind` for self-classification; dashboard key-management modal renders `kind`-badge per entry. Composition: §13.16.9 routing should include key-management names in board-directed allowlist; §13.14 redaction discipline — `kind` enum is vocabulary (not sensitive), only key values (`ck-…` / `uk-…` suffixes) are environment-specific credentials. Wire shapes preserved — `kind` is an additional top-level field, server-stamped on issuance, mandatory on `KeyList` entries, optional on `HELLO.meta`. **Tier-2 dashboard render policy** (new file `constellation/reference/docs/dashboard-render-patterns.md`, ~3902 words / ~26.8 KB, 8 sections): companion document covering render-side patterns above the wire — §1 introduction (scope: render-tier patterns surfaced through 2026-05~06 dogfood; companion to `Constellation.md`); §2 A2A row interaction (three-stage discovery: glanceable summary → hover preview 4-quadrant peek panel → click pin floating panel with pin/✕/header-drag controls → drag with relative-cursor offset; reference impls `wsRowHover` / `wsA2aCardEl`); §3 collapse `[hidden]` CSS pitfall + triangle indicator direction (the `display: flex` overrides UA default `[hidden] { display: none }`; explicit `.ws-a2a-body[hidden] { display: none }` fix; triangle convention ▾ expanded / ◀ collapsed — collapsed indicator points-left semantically "content is on the left of this button" not the tree-view ▸; toggle button placement rightmost of row with sibling-not-descendant relationship to row click handler to avoid propagation conflict); §4 board render card form (icon + envelope summary + collapsible JSON body for A2A-intent; per-name UX surface for board-directed — Status as system-notice strip, Typing as input-area chip, ConnectionRestored as banner, Heartbeat consumed by liveness indicator only; raw TEXT fallback when type-omitted = symptom of §13.11.3 rule 1 server-side defect); §5 date-line divider (insert at midnight crossing in chronological stream; `position: sticky; top: <header-height>` keeps most-recent date label visible during scroll; `오늘 / 어제 / YYYY-MM-DD` text per locale with `~ ~` decorators; sticky offset must account for chat-panel header height not just page header); §6 A2A attachment chip (mime-icon emoji glyph + filename + size + preview/download triggers, rendered inside card body not as separate row — keeps attachment-message association visible; chip pattern portable across `attachments[]` / `files[]` / `media[]` field-name choice); §7 closed-session connection indicator (green dot `●` connected vs gray dot `●` history-only — disconnected agent rows remain in roster, dot updates; tooltip surfaces precise connection state with local-tz timestamp; pairs with §13.13 `delivered` ack escalation path); §8 upstream A2A general-tab detection fix (source-set-agnostic detection rule per §13.11.3 rule 2 — fixes the defect where prior `source === "agent"` positive-set check silently false-negatived all upstream A2A traffic, causing operators to report "upstream A2A invisible on dashboard"; receiver-side half of §13.11.3 rule 2). Implementation references appendix maps each pattern to upstream commit SHAs (without quoting code — SHA + filename + function name) for adopter traceability. Cross-references to `Constellation.md` §13.11 + §13.16.9 + §13.16.11 + `constellation/reference/state-schema.md`. **Scope discipline**: Constellation.md body + new dashboard-render-patterns reference doc. Seed library unchanged (seeds reference §13.x by anchor, not by body — no cascade). Wire format unchanged — §13.11 envelope shape preserved, §13.11.3 codifies server-side stamping discipline + receiver-side detection rule without changing field semantics; new `kind` enum field is additive and back-compatible. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.5.0** (2026-06-01) — **Superscalar.md v0.3 — §2 lane-class-aware `issue_width` cap split (read vs write) + §2 policy cap vs runtime concurrency ceiling sub-section + §5 lane-class-differentiated adoption thresholds + §11 Entry 05 (downstream Stage 1 dogfooding n=7).** Minor release — Superscalar.md policy-doc body change + Compact + Master seed cascade. Reflects a downstream adopter's Stage 1 dogfooding bundle (real-time chat-service microservices, full seed adoption incl. Superscalar + Constellation, Burst pace mode `issue_width` cap 6, n=7 retired lanes over ~3 days, case-based ledger) — proposal filed at `_proposals/005_2026-06-01_superscalar-dogfooding-stage1/`. **§2 cap split**: a single `issue_width` formula previously covered all lane classes uniformly; v0.3 splits it into `issue_width_write = min(effort_band, pace_mode_cap, Little's-Law, Kanban-WIP, autonomy_available_workers)` and `issue_width_read = min(effort_band, runtime_concurrency_ceiling, autonomy_available_workers)`. The two terms dropped from the read class (Little's Law, Kanban WIP) only model retire-merge contention that read-only lanes structurally don't have — no store buffer, no retire-merge ordering, no WAW hazard, disposable on synthesis. Read-only / analysis lanes (subagent context sweeps, workflow read fan-outs, telemetry reads) carry no irreversible side effects and inherit the read/write boundary §3 already draws for the irreversibility barrier (read/analyze default-allowed; write/deploy/send retire-gated). **§2 new sub-section — policy cap vs runtime concurrency ceiling**: the `issue_width_*` caps are policy bounds; the runtime concurrency ceiling (e.g. workflow's `min(16, cores − 2)` per workflow; harness max parallel tool calls for agent-tool fan-outs) is a separate physical bound. Relationship: `effective_concurrent_lanes = min(policy_cap, runtime_ceiling)`. For workflow-based read fan-outs the runtime ceiling typically dominates; for agent-tool-based fan-outs the policy cap typically dominates. `issue_width_write` is a hard policy bound (exceeding it is a policy violation regardless of runtime headroom — retire-merge contention scales with concurrent writes); `issue_width_read` is a soft preference subject to the runtime ceiling above it (a read-only fan-out that exceeds `issue_width_read` but stays under `runtime_concurrency_ceiling` is not a policy violation, at most a calibration signal). **§5 adoption-threshold lane-class differentiation**: the threshold values are unchanged from prior versions; what is new is which lane class each threshold binds. `merge-conflict rate > 15% → issue_width_write -= 2` applies to write lanes only (read-only lanes have no merge surface, conflict = NA). `speculative accuracy < 60% → spec off` · `concurrent token cost vs in-order > 3× → cap re-tune` · `FM-1.3 detection ≥ 1/session → guard up` apply symmetrically (cross-cutting concerns, not retire-merge contention). **§11 Entry 05 added**: downstream Stage 1 dogfooding n=7 over ~3 days. Case mix = 1 board-worker A2A · 5 read-only research subagents · 1 workflow fan-out (case 7 = 7 reader lanes, exceeded policy cap 6 with zero downside — the empirical anchor for the §2 cap split). Confirmations: latency-hiding thesis · speculation off-by-default 0/7 · MAST FM-1.3 = 0 · in-order retire held. §5 readout: all thresholds green at n=7 (case 7's cap-exceed was a read-only lane, conflict = NA, does not count against the merge-conflict signal — empirical confirmation of the §5 lane-class differentiation). Unexercised surface: worktree-isolation 0/7 · speculation 0/7 · wide write fan-out 0. Relation to Entry 04: complementary, not redundant (Entry 04 varied in-lane depth at fixed width below cap; Entry 05 varies cross-lane fan-out above cap on read class). Outcome: P1 (lane-class cap asymmetry, actionable) + R1 (cap split adopted in §2/§5) + R2 (policy/runtime relationship clarified in §2) all reflected upstream. **Seed cascade**: Master EN/KO § Execution Scheduling — `### The invariant — issue_width formula` body updated to the lane-class-aware split + policy/runtime ceiling note (Master embeds the formula body, not just an anchor); Compact EN/KO Principle #15 inline formula expanded to the lane-class form with §5 binding note; Lite EN/KO unchanged (Lite tier references `Superscalar.md` by anchor only, no formula body). Bootstrap Phase 0 boilerplate unchanged (lane class is an internal mechanic; the user-facing `serial` vs `parallel` choice is unaffected). **Frontmatter**: Superscalar.md frontmatter `version: v0.3.0` (added), title bumped from "design draft v0.2" to "design draft v0.3", status line gains "lane-class-aware (v0.3 read/write cap split)" and `§11 Entry 01-05` (was 01-03). **Scope**: minor — Superscalar.md body change + Master/Compact seed cascade. Constellation.md unchanged. Wire unchanged. Per `feedback_release_versioning_cadence.md` — meaningful policy-doc body change with seed cascade is a minor cut, not a patch.
- **v2.4.20** (2026-06-01) — **Hermes Dev Agent handoff bundle consolidation (10 files, 239,654 bytes).** Handoff-archive-only patch. Constellation.md body unchanged; seed lib unchanged; protocol wire unchanged; canonical `constellation/gateway-client.eux` + `constellation/reference/gateway/hermes/{README.md,ws_agent_client.py}` unchanged. The handoff bundle was delivered against EG commit `51a93826f8d3` (v2.4.14 tag) and consolidated into the EG tree per project owner direction. **Consolidation outcome**: (a) new directory `constellation/reference/docs/2026-06-01_hermes/` carrying `GEMINI-OUTPUTS-REVIEW.md` (12,794 bytes; three-axis review of an earlier Gemini-authored draft surfacing seven critical findings C1-C7 + seven high findings H1-H7 — `HELLO_ACK` non-existence, auth parameter confusion, default-role `worker` not-in-spec, AG-UI phase event mismatch, envelope field gaps, delivery semantics conflicts, redaction-requirement misreading), `UPSTREAM-AGENT-REFLECTION.md` (5,231 bytes; ten upstream-peer invariants), `AUTONOMOUS-AGENT-ONBOARDING-GUIDE.md` (5,497 bytes; onboarding guide for API/CLI/upstream agents), `HANDOFF-MANIFEST.json` (provenance with sha256 of each handoff file; `root` field redacted to `<hermes-workspace>` placeholder per §13.14), `README.md` (handoff context + file index + consolidation outcome table); (b) side-by-side snapshot at `constellation/reference/gateway/hermes/_hermes-handoff-20260601/` carrying the Hermes-side `gateway-client.eux` (7,516 bytes; `2.3.7-upstream-ref` distillation with `SERVER_HELLO`-based handshake + `at-most-once` delivery + ack-layer skeleton, structurally different from EG's existing 5,740-byte canonical that the Hermes review flagged for `HELLO_ACK`/`worker`-role/single-event defects — promotion decision is a separate follow-up), Hermes-side `README.md` (5,927 bytes) and `ws_agent_client.py` (18,544 bytes; Python adapter rewrite against the corrected protocol stance), new `constellation-hermes-ref-summary.md` (2,780 bytes), new `constellation-hermes-gateway-ref.zip` (8,700 bytes; binary bundle preserved). (c) Hermes-side `reference/estregenesis/Constellation.md` (170,957 bytes / 900 lines / v2.3.7) **deferred** from direct integration — review-only artifact in the gitignored `assets/handoffs/2026-06-01_hermes/` archive; EG-current Constellation.md (188,287 bytes / 981 lines / v2.3.10) authoritative. Section-header structure aligned (same §1-§7, same §13.9-§13.21); EG-current carries the v2.4.15-v2.4.19 additions to §13.16 (codification of turn-end mandatory rearm at element 2 + §13.16.8 A2A communication priority + §13.16.9 board-directed vs A2A-intent classification + §13.16.10 pre-send inbound check) that the Hermes-side snapshot predates. Diff summary in outbox Report. **Redaction grep**: 1 replacement applied (Hermes local-workspace path token `/run/media/esterisk/222CD85A2CD82B15/HermesAgentWorkspace/Project/constellation` in `GEMINI-OUTPUTS-REVIEW.md` line 11 + `HANDOFF-MANIFEST.json` `root` field, both redacted to `<hermes-workspace>` placeholder per §13.14); secondary grep against the newly-added tree confirmed clean. **Scope discipline**: handoff-archive integration only — Constellation.md body unchanged, seed lib unchanged, protocol wire unchanged, canonical gateway-client and hermes reference files unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.19** (2026-06-01) — **A2A PR System RRP v0.2 → v0.3 DRAFT: 7 learnings from dogfood PR #1 e2e completion + §10A dogfood-1 case study + AC-dogfood-6 achieved at integration-branch commit `140fd64`.** RRP-only patch — `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` body augmentation. Constellation.md body unchanged; seed lib unchanged; protocol wire unchanged. The v0.3 patch incorporates seven RRP-body learnings surfaced during the first Level 1 protocol-mediated PR e2e completion: **(1) `sourceContentRef.kind` enum formalization (§4.1.1)** — three closed values (`branch-ref` / `inline-files-diff` / `inline-section-mirror`) with fail-safe reject on unknown kinds (receiving bridge MUST reject rather than fall through; prevents the "fallthrough to branch-ref causes wholesale repo overwrite" near-disaster). **(2) `inline-section-mirror` semantics (§6.5)** — `body` + `targetPath` + `mergeStrategy` (currently only `append-section` defined; future enum: `prepend-section` / `replace-section` / `insert-after-anchor`); bridge invariants enforce `git add <targetPath>` isolation (NEVER `-A`), no branch-fetch, PR_LIVE existence check, and the dry-run `abs+exists` log. **(3) `targetPath` base spec (§6.6)** — repo-relative to receiving bridge's `HUB_REPO_DIR` (NOT absolute, NOT bridge-cwd-relative, NOT sub-tree-relative); sender owns knowledge of receiver's file layout for trusted-mirror PRs. **(4) `already-in-target` cherry-pick skip discipline (§9.3)** — Level 1 merge handler checks `git merge-base --is-ancestor <draftRef> <targetBranch>` before cherry-pick; if true, skip directly to `git push` (prevents duplicate-commit errors when `applyDiffAndCommit` already committed `draftRef` to local target branch). **(5) Dry-run `abs+exists` verification log (§10.6)** — dry-run mode MUST log `abs=<resolved-absolute-path> exists=<bool>` so path-resolution defects surface BEFORE PR_LIVE; closes the "dry-run is a no-op stub so path defects are uncatchable" gap. **(6) `trusted_mirrors.targetRepo` alias resolve order (§7.ε)** — receiving bridges register `"hub"` as the default `targetRepo` alias; resolve order is `state.json.pr_system.target_aliases` primary → `git remote get-url origin` fallback → reject; senders emit `targetRepo: "hub"` as canonical convention (redaction-safe by default per §13.14 — wire envelopes never carry real production repo path). **(7) `PR_LIVE` env semantics + `dryRun: false` marker + user merge delegation pattern (§10.7)** — `CONSTELLATION_PR_LIVE=1` on the receiving bridge enables real git ops; the env is operator-managed (NOT togglable via A2A) preserving the autonomous-execution external-publish gate; `PRDraftReady` / `PRMergeAck` carry `dryRun: false` marker only when PR_LIVE active; operators can pre-delegate merge authority by class (e.g., "for trusted-mirror PRs, review and if no problem, merge and push") reducing per-PR confirmation overhead. **New §10A dogfood-1 case study sub-section**: documents the e2e protocol path (`PRRequest → capability check → applyDiffAndCommit PR_LIVE real append + git add isolation + commit → PRDraftReady{dryRun:false} → main review (redaction grep 0 hits + path consistency + duplicate check) → PRReviewAck approve → PRMergeRequest (role==main self-target relay) → cherry-pick skip (draftRef already in main) + push origin main → PRMergeAck{pushSuccess:true, commitSha:140fd64} → PRStatusUpdate merged`); the three structural defects surfaced and resolved by gate stack BEFORE any external publish (placeholder lookup failure / missing inline-section-mirror handler / targetPath base mismatch — each defect's one-line root cause + resolution + codification anchor); AC-dogfood-6 achievement (§12.3 — first Level 1 mirror PR e2e via A2A envelopes only, no `gh pr create`, integration-branch commit SHA `140fd64` reported in `PRMergeAck`); lessons for downstream Constellation adopters (run dry-run first / verify kind enum coverage / set target_aliases explicitly / redaction grep + path-consistency review non-optional / user merge delegation is opt-in by class). **Status banner updated**: v0.2 DRAFT (Phase 1 Level 2 shipped, Level 1 path pending) → v0.3 DRAFT (Phase 1 + Phase 1b shipped, AC-dogfood-6 achieved via PR #1). Phase 1b promoted to DONE in §13 phase ladder. **Memory feedback augmented** (not committed): `feedback_pre_send_inbound_check.md` adds probe-then-inspect-or-abort pattern — when the probe returns count > 0, the agent MUST inspect each meaningful inbound's body BEFORE emit, classify superseding vs non-superseding, and either incorporate-disclosure-and-proceed or abort-and-revise; codifies node-script implementation pattern (`process.exit(2)` on count > 0 abort) + worked example from 2026-06-01 dogfood cycle self-violation incident (probe surfaced 1 meaningful inbound, agent emitted anyway without body inspect; non-superseding so damage=0 but process-violation was real; v2.4.19 closes the residual loophole where probe ran but count was treated as informational). **Scope discipline**: RRP-only patch. Constellation.md body unchanged. Seed lib unchanged. Wire unchanged. Memory not committed. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.18** (2026-06-01) — **`Constellation.md §13.16.10` new sub-section: pre-send inbound check discipline (read-before-write, git push-before-pull analogy).** Constellation-only patch (frontmatter version v2.3.9 → v2.3.10). New §13.16.10 (~55 lines) inserted between §13.16.9 (board-directed vs A2A-intent classification) and §13.17 (main-chat ban). **The rule**: every outbound A2A message MUST be preceded by a cursor-tail probe of the agent's last-surfaced inbox cursor. If new meaningful inbound exists since the cursor, the agent MUST surface it first and either (a) incorporate the new information into the outbound payload, or (b) defer the outbound entirely if the new inbound supersedes the planned send. Only after the probe is clean (cursor up-to-date, no new meaningful inbound) does the outbound proceed. The probe fires before each outbound emission, not once per turn. **Rationale — git push-before-pull, but stronger**: the discipline mirrors the read-before-write rule that `git pull` before `git push` codifies in distributed VCS. Constellation A2A is **worse than git** in this regard — git rejects a non-fast-forward push at the transport layer and forces the operator to pull; Constellation A2A has **no in-flight rejection**: a stale-context send simply lands on the server, relays to the counterpart, and may overwrite or contradict a fresh inbound the sender never saw. The drift is silent. The pre-send probe brings the read-before-write rule explicitly into the A2A protocol, where the transport itself does not enforce it. **Concrete failure mode**: counterpart emits `Report X` at `t=0` carrying new context. Agent has `Report Y` queued for `t=1` framed from pre-`t=0` context. If agent sends `Y` without first surfacing `X`, `Y` arrives at the counterpart with stale framing — counterpart now reconciles two diverging threads and a follow-up correction outbound is typically required. The fix is one-way: agent always reads before writing. **§13.16.10 contents** (~55 lines): rule statement; git push-before-pull analogy (with the in-flight-rejection asymmetry note); concrete failure mode; why §13.16.6 element 1 (cursor-tail probe at turn-end) is insufficient alone (turn-boundary layer vs mid-turn layer — disjoint coverage windows); 4-step operational shape (probe → surface-if-found → emit → advance-cursor); composition with §13.16.6 / §13.16.8 / §13.16.9 / §13.21; forbidden anti-patterns (skipping because "inbox was just checked at turn start", skipping for "small" outbounds, probing-but-ignoring). **Composition**: complements **§13.16.6 element 1** (turn-end cursor-tail probe — turn-boundary layer) by adding a mid-turn layer fired before each outbound emission; the two cover disjoint windows (turn-end catches inter-turn-gap inbound; pre-send catches intra-turn inbound between any two emits). **§13.16.8 A2A communication priority** places the pre-send probe between prerequisite work and message emission: `prereq → probe → emit`; the probe is a guard on the very drift-containment rule §13.16.8 ships. **§13.16.9 channel allowlist** supplies the probe's meaningful-inbound filter — the same A2A-intent allowlist the watcher uses in §13.16.6, applied uniformly regardless of intent class (probe cost is negligible — one `wc -l` + one `grep`). **§13.21 fresh-context defer** is unaffected — probe-driven revision or drop is *current-cycle* read-before-write, not deferral to a phantom future self; the two compose cleanly (revise-or-drop now per §13.16.10; never defer to fresh context per §13.21). **Forbidden anti-patterns**: skipping the probe because "the inbox was just checked at turn start" (multiple outbounds per turn each need their own probe — the agent does not get to amortize one probe over multiple emissions); skipping for "small" or "low-priority" outbounds (the skip-pattern erodes the discipline; probe is unconditional); probing but ignoring the result (defeats the rule — the cost of the probe was paid but the safety was discarded). **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16.x by anchor, not by body). Protocol wire unchanged — wire envelope shape per §13.11 unchanged; only the agent-side outbound discipline is codified. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.17** (2026-06-01) — **`Constellation.md §13.16.9` new sub-section: board-directed vs A2A-intent `CUSTOM` message classification SSoT + bridge `Status` auto-send disabled + watcher meaningful allowlist extended.** Constellation-only patch (frontmatter version v2.3.8 → v2.3.9) + reference runtime + reference bridge. v2.4.17 codifies the SSoT for classifying every `CUSTOM` message `name` by intent: **board-directed** (rendered on the board dashboard's UX surface — not an A2A wire message to an agent inbox) vs **A2A-intent** (routed to a target agent's inbox per the §13.x A2A relay discipline). The classification governs two downstream choices: server-side relay routing for target-unspecified `CUSTOM` (the reference server's `wsPrimaryAgent` relay branch and the `wsToBoards` mirror policy) and watcher meaningful-filter membership in §13.16.6. Both surfaces previously inferred intent ad-hoc from the name; §13.16.9 names the allowlists once so the two surfaces stay in sync. **Technical context — channel inversion**: the reference server's `target-unspecified CUSTOM → wsPrimaryAgent (main A2A inbox)` routing policy does not distinguish board-directed from A2A-intent by name, so board-directed messages (`Status` / `Typing` / `UserPromptAccepted` etc.) leak into the main agent's A2A inbox channel. Combined with the `wsToBoards` always-on mirror policy at the same routing layer, A2A-intent messages (`Report` / `Delegate` / `BlockerManifest` / `PR*` etc.) also mirror to the board's dashboard chat panel, which renders them as raw JSON because the panel's display layer expects board-directed text content. Net effect: channels are inverted — board-directed messages end up in A2A, A2A messages end up in board chat as raw JSON. §13.16.9 provides the name allowlists as SSoT to unblock the server-side routing branch + dashboard render-side patches; the routing fix and the render fix are upstream-main follow-ups outside this patch's scope. **§13.16.9 contents** (~60 lines, inserted between §13.16.8 and §13.17): purpose statement (SSoT for intent classification governing relay routing + watcher filter); **board-directed allowlist (12 names)** — `Status`, `Typing`, `UserPromptAccepted`, `PersistentAdapterSmoke`, `ConnectionRestored`, `Heartbeat`, `AgentList`, `History`, `OnboardAck`, `MainChanged`, `EditMessage`, `WorkerInboxReceived`; **A2A-intent allowlist (25+ names)** — `Report`, `Delegate`, `WorkerReport`, `WorkerAck`, `AgentHello`, `Handoff`/`HandoffRequested`/`HandoffReady`, `Command`/`Priority`/`Cancel`, `UserPrompt`, `BlockerManifest`/`BlockerNudge` (§13.20 family), `PRRequest`/`PRDraftReady`/`PRReviewAck`/`PRMergeRequest`/`PRMergeAck`/`PRStatusUpdate` (§13.22 family per A2A PR RRP v0.2), `DeadlockProbe`/`ReviewSLAAck`/`PreemptRequest`/`PreemptForce`/`MediationProposal`/`MediationAck`/`EscalationRequest` (§13.19 family); **transport-tier (separate class)** — `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate`/`Pong`/`Ping` (handled by §13.13 ack discipline) + `ServerNotice` (broadcast-class via the reference server's `wsToAll` path); **routing implications** — `targetAgentId`-absent `CUSTOM` consults the allowlists (board-directed → `wsToBoards` only; A2A-intent → `wsPrimaryAgent` + optional board mirror; unrecognized → fail-safe default A2A-intent because silent-drop-of-A2A is the worse failure mode than redundant board noise); `targetAgentId`-present `CUSTOM` → existing A2A relay path unchanged regardless of intent class; **watcher filter cross-link** — §13.16.6's meaningful allowlist === §13.16.9's A2A-intent allowlist, noise blocklist === board-directed allowlist + transport-tier; §13.16.9 becomes the canonical SSoT; **bridge auto-send discipline** — client-side bridges MUST NOT auto-emit board-directed names without `targetAgentId` (silent drop suffices for board-only display; auto-emit creates the channel-inversion defect); **dashboard render policy** (informational) — when the always-on board mirror delivers an A2A-intent `CUSTOM`, the dashboard SHOULD render it as an envelope summary (`name` + `targetAgentId` + brief `re`/`subject`) with collapsible details, not raw JSON; **scope note** — wire envelope shape per §13.11 unchanged; only the interpretation of the `name` field for target-unspecified routing + watcher-filter membership is codified. **§13.16.6 cross-reference updated**: the v2.4.16 single-stage filter pattern's meaningful allowlist + noise blocklist now read names from §13.16.9 by anchor; the canonical bash filter pattern in §13.16.6 is regenerated to include the full §13.16.9 A2A-intent allowlist (§13.20 + §13.22 + §13.19 families folded in). **`constellation/reference/runtime/local-bridge.cjs`** — line 173 auto-`Status` emission disabled (commented out) per the §13.16.9 bridge auto-send discipline. The `ServerNotice` broadcast at the same `connect` handler is retained because §13.13 transport-tier `ServerNotice` is broadcast-class, not target-unspecified A2A. Inline comment names the §13.16.9 discipline + the channel-inversion rationale for the disable. **`constellation/self-wake-watcher.eux` `@behavior meaningful(from)`** — clause body extended: noise blocklist enumeration aligned to §13.16.9 board-directed + transport-tier (gains `MainChanged`, `History`, `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate`); meaningful allowlist enumeration explicitly added (was previously implicit via "everything not in the blocklist"); fail-safe default-wake for unknown names spelled out; §13.16.9 cross-reference declared the SSoT for both enumerations. The `@intent` line also gains the §13.16.9 cross-reference. **`constellation/reference/runtime/self-wake-watcher.sh` `meaningful()`** — Node-based filter's `NOISE` array extended to match the §13.16.9 board-directed + transport-tier list (gains `MainChanged`, `History`, `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate`); inline header comment names the §13.16.9 SSoT + the v2.4.16 ev-agnostic rule + the diagnostic anchor; the upstream Korean comment block at the top of the file is also updated to reference §13.16.9 and to enumerate the full A2A-intent allowlist for operator visibility. The script remains ev-agnostic name-only single-stage (v2.4.16 stable). **Scope discipline**: Constellation-only patch + reference runtime + bridge. Seed lib unchanged (seeds reference §13.16.x by anchor, not by body). Protocol wire unchanged (only intent classification of the `name` field for target-unspecified routing + watcher-filter membership). Server-side routing patch (target-unspecified `CUSTOM` branching by §13.16.9 allowlists) and dashboard render patch (envelope-summary rendering for A2A-intent board mirror) are upstream-main side follow-ups, not covered here. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.16** (2026-06-01) — **`Constellation.md §13.16.6` watcher filter form: ev-agnostic name-only single-stage (drops v2.4.2 1st-stage `ev:"inbound"` gate that missed bridge-alive direct inbound format).** Constellation-only patch (frontmatter version v2.3.7 → v2.3.8) + reference runtime + `.eux` spec. v2.4.2 introduced a two-stage filter (`grep '"ev":"inbound"'` 1st stage → name blocklist 2nd stage → meaningful name allowlist 3rd stage). The 1st stage **silently dropped all bridge-alive A2A traffic** from the WAKE decision because the `inbox.log` carries two envelope formats and the bridge-alive direct inbound format has no `ev` field. **The two formats**: (a) **legacy event-relay** `{"t":..., "ev":"inbound", "msg":{"name":...}}` — written by the bridge process around its own lifecycle events (`closed` / `ws-error` / `inbound` log entries); (b) **direct inbound** `{"at":..., "name":..., "value":..., "source":"agent"}` — written by the bridge when it actually delivers an A2A envelope from the server. The v2.4.2 1st-stage `grep '"ev":"inbound"'` was correct for (a) but a hard fail for (b) — every line in format (b) was filtered out before the name stages ever saw it. **Diagnostic evidence**: watchers `bv0u5h95p` (armed 02:19:31Z, base=518) and `bd7k7xoy3` (armed 02:32:49Z, base=518) both completed with REARM ~45min / "no meaningful inbound" while inbox lines 519~522 (3 main `Report` envelopes + 1 server `Ack`) arrived inside their armed windows in the direct format — both watchers were polling normally on cadence but their filter was structurally blind to the only meaningful traffic that arrived; a fresh watcher (`bql0322pj`) spawned with the v2.4.16 ev-agnostic form catches both formats. **v2.4.16 collapses to single-stage name-only**: `tail … assets/collab/inbox.log | grep -vE '"name":"(History|Ack|AckProcessed|AckCumulative|AckPolicyUpdate|ServerNotice|AgentList|Status|ConnectionRestored|Heartbeat|Typing|UserPromptAccepted|PersistentAdapterSmoke|OnboardAck|WorkerInboxReceived|EditMessage|MainChanged)"' | grep -cE '"name":"(Delegate|UserPrompt|WorkerReport|WorkerAck|AgentHello|Handoff|Report|Command)"'`. v2.4.2's intent (block self-emission echo + transport noise) is preserved **entirely** by the name blocklist — the blocked names are the same names whether they arrive as legacy event-relay or direct envelopes, so the blocklist is format-agnostic and the ev-stage was redundant for the legacy format and a hard fail for the direct format. v2.4.2's analysis of the two false-positive classes (self-emission #1, server-replay/History #2) was correct and still binds; only the ev-stage gate is retired. **Files changed**: `Constellation.md §13.16.6` Filter-discipline paragraph rewritten (ev-stage removed; new "Why ev was dropped" paragraph documenting the two formats + diagnostic anchor) + frontmatter v2.3.7 → v2.3.8; `constellation/self-wake-watcher.eux` `@behavior meaningful(from)` clause explicitly disclaims the ev-stage (prevents future re-introduction); `constellation/reference/runtime/self-wake-watcher.sh` `meaningful()` Node-based blocklist gains an inline header comment naming the v2.4.16 ev-agnostic rule + the two formats + the diagnostic anchor. **Scope discipline**: Constellation-only patch + reference runtime + `.eux` spec. Seed lib unchanged (seeds reference §13.16.6 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.15** (2026-06-01) — **A2A PR System RRP v0.1 → v0.2 DRAFT: Level 1 + Level 2 abstraction + β′ topology + Q1/Q2/Q3 resolved per Phase 1 upstream ship `2213a14`.** RRP-only patch — `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` revised end-to-end; CHANGELOG entry only besides the RRP file. **Two-level abstraction (new core concept, §2.4)**: a single wire vocabulary (§4) + state machine (§9) covers two physical PR realizations. **Level 1 — virtual PR (A2A-envelope-only)**: applicable when source and target share an operator AND both sides have direct repo-level file-system access; receiving side reads `sourceContentRef`, applies change directly to its local checkout of the target repo, commits, pushes; `gh pr create` is skipped entirely; review happens in-band on the A2A wire; MERGED ≡ commit pushed to integration branch. **Level 2 — Github PR (provider-mediated)**: existing Phase 1 ship form per `2213a14`; `handlePrRequest` invokes `gh pr create` against the target repo; PR is a real provider object; review/merge flow through the provider with A2A envelopes mirroring state. **Selection rule**: `category == "trusted_mirror"` AND `operator-shares-target` AND `target-repo-direct-access` → Level 1 default; otherwise Level 2. Carried as `PRRequest.level` (default `2` for backward compat with Phase 1 ship). **Per-type semantics adapt**: `PRDraftReady.draftRef` = commit-sha (Level 1) vs pr-number (Level 2); `PRMergeRequest` = commit to integration branch (Level 1) vs `gh pr merge` (Level 2); `PRMergeAck` = commit SHA + push success (Level 1) vs merged-PR provider state (Level 2); `PRStatusUpdate.state` is level-discriminated. **β′ content sourcing topology adopted (§7.β′)**: supersedes v0.1's "β with α fallback." β′ = local-bridge pushes source content to `mirror/...` branch on the **target** repo (not the source repo), then opens PR intra-target — sidesteps the cross-repo provider authorization complications inherent to β. v0.2 recommendation: Level 2 β′ primary; α small-diff fallback (~50KB); β and γ not used in Phase 1 / Phase 1b scope; Level 1 content sourcing is implicit (no mirror branch needed). **Priority open questions Q1/Q2/Q3 resolved per Phase 1 ship `2213a14`** (§11): **Q1 (topology)** — local-bridge `cmd:createPR(spec)` adopted (topology b); the local-bridge runs in the operator's IDE process and reuses the operator's `gh` CLI auth, no separate bot identity / credential store required for Phase 1 / Phase 1b scope. **Q2 (content-sourcing)** — β′ adopted; rationale: cross-repo provider auth simplification (PR is intra-target at the provider tier). **Q3 (authority model)** — strict `role==main` gate adopted; server's `wsPrMergeReject` path rejects non-`main` emissions and logs a `decisions` panel entry; the protocol's authority chain is deterministic and independent of provider ACL drift. **Phase 1 ship reflection**: `server.cjs` `_PR_CUSTOM` 5-type observer set + `wsPrMergeReject` + `PRStatusUpdate`; `local-bridge` `handlePrRequest` (trusted-mirror validate → β′ mirror branch on target repo → `gh pr create` → `PRDraftReady`) + `handlePrMergeRequest` (`gh pr merge` → `PRMergeAck`); `state.json.pr_system.trusted_mirrors` registry covering §13.14/§13.15 pair as shipped. Dry-run default: `CONSTELLATION_PR_LIVE` env knob OFF (opt-in to live PR creation, instantiating the autonomous-execution external-publish gate at env level). **Phase 1b new sub-phase (§13)**: extends `handlePrRequest` with a Level 1 branch — when `PRRequest.level == 1`, skip `gh pr create`, apply source diff to the target repo's local checkout, stage a commit, emit `PRDraftReady` with `draftRef: <commit-sha>`; `handlePrMergeRequest` Level 1 branch pushes the staged commit to the integration branch and emits `PRMergeAck` with the integration-branch commit SHA + push success; server validates `PRRequest.level` field and the receiving side's capability matrix for `level == 1`. **Migration path updated**: phase 0 RRP review → phase 1 Level 2 prototype (DONE per `2213a14`) → phase 1b Level 1 path (PENDING; dogfood §10 §13.14/§13.15 mirror PR re-runs as Level 1 PR; exit on AC-dogfood-6 pass) → phase 2 dedicated PR-bot agent (optional, topology a) → phase 3 full §13.20 integration. **Acceptance criteria (§12)**: new AC-dogfood-6 — first Level 1 mirror PR (EG → hub `WS-PROTOCOL.md` §13.14/§13.15 mirror) succeeds end-to-end via A2A envelopes only; receiving side directly applies the change, commits, pushes to the integration branch, reports the resulting commit SHA in `PRMergeAck`; no `gh pr create` involved. **Status banner**: `v0.1 DRAFT` → `v0.2 DRAFT (Phase 1 Level 2 shipped, Level 1 path pending)`. Non-priority Q4-Q8 remain open. **Scope discipline**: RRP-only patch — Constellation.md body unchanged (§13.22 number assignment + body integration remain post-joint-formalization); seed lib unchanged; protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.14** (2026-06-01) — **`Constellation.md §13.16.6` element 2 timing restored to turn-end mandatory rearm (rolls back v2.4.9 + v2.4.12 next-cycle-start placement).** Constellation-only patch (frontmatter version v2.3.6 → v2.3.7). v2.4.9 first relocated element 2 (unconditional watcher rearm) from current turn-end to next-cycle start; v2.4.11 reverted to turn-end; v2.4.12 re-applied the next-cycle-start form. **v2.4.14 settles the discipline at turn-end mandatory rearm.** **Why technical**: the next-cycle-start placement leaves the inter-cycle gap (current turn-end → next user-input arrival) watcherless — a watcher scheduled to spawn at next-cycle start is not yet alive during that gap, so any inbound that arrives during the gap lands without a watcher to wake on it. The watcher MUST be armed at current turn-end so it is alive throughout the inter-cycle gap and catches any inbound arriving before the next user-input. Main responses arriving during the gap are exactly the silent-stall failure mode this section exists to prevent. **§13.21 composition clarified**: deferring the rearm step to next-cycle start IS a cross-cycle deferral of work the current cycle owes — the exact pattern §13.21 forbids. The earlier "structural correct location" reading of v2.4.12 missed where the structural location actually is: the structural location of the watcher that catches inter-cycle inbound is BEFORE the inter-cycle gap begins, which means at current turn-end, not after the gap. **Cost-asymmetry argument restored**: concurrent-watcher-overlap that v2.4.9 / v2.4.12 tried to avoid is harmless under the cost-asymmetry argument — FP cost (one extra task slot + a few log lines) is trivial; FN cost (watcherless inter-cycle gap + missed inbound for hours until externally surfaced) is catastrophic. Turn-end rearm pays the trivial FP cost every turn to avoid the catastrophic FN cost. **`Constellation.md §13.16.6` edits**: heading restored to "Turn-end mandatory rearm — the discipline that prevents the multi-cycle failure mode"; element 2 description restored to "At the close of EVERY user-facing response (just before emitting the final text)..."; "Element 2 timing — relocated from turn-end to next-cycle start" paragraph removed; 6-element checklist cadence-interaction annotation `[executed at the start of the next cycle, not at turn-end]` removed; 4-step list (a) tail → (b) surface → (c) advance → (d) THEN unconditional rearm restored to all-at-turn-end form (no cycle annotations); "Wrong order — anti-pattern" + "Composition with the other four elements" paragraphs restored to turn-end forms; v2.4.12 tail paragraph (next-cycle-start-is-not-deferral clarifier) removed. **New tail paragraph at §13.16.6** as lock-in against further flip-flop: the paragraph explicitly states the timing has settled at turn-end mandatory rearm, names the inter-cycle gap as the technical reason, names the §13.21 composition correction (next-cycle-start IS cross-cycle deferral), restores the cost-asymmetry argument (concurrent-watcher-overlap harmless; deferred rearm + missed inbound catastrophic), and requires any future proposal to relocate the rearm to first demonstrate how the inter-cycle gap is otherwise covered. §13.16.8 cross-reference also cleaned (the "(element 2, relocated to next-cycle start in v2.4.9)" parenthetical removed). Frontmatter version v2.3.6 → v2.3.7. **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16.6 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.12** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering restored to next-cycle-start rearm form (v2.4.9 reapplied; v2.4.11 revert corrected).** Constellation-only patch (frontmatter version v2.3.5 → v2.3.6). v2.4.11 reverted v2.4.9's element-ordering refinement (element 2 rearm: next-cycle start → current turn-end) on the rationale that the next-cycle-start timing violated §13.21 (defer-to-next-cycle = anti-pattern) and #442 (planned-queue scan / no-idle-deferral). **That revert rationale was a misreading of §13.21**: §13.21 forbids deferring committed planned work to a hypothetical fresh-context cycle — the excuse pattern where the current cycle silently re-routes its own work into "next fresh context will pick this up". The rearm is not that pattern: the rearm IS the first action of the next user-facing cycle, executed as soon as the cycle begins, by the same continuous agent process. Scheduling a step to its structural correct location across one cycle boundary is structural placement, not deferral-as-excuse. **v2.4.12 restores the v2.4.9 form**: element 2 (unconditional watcher rearm) executes as the first action of the next user-facing cycle (cycle N+1's start), not at the close of cycle N's turn-end. Elements 1 (cursor-tail probe) and 3-6 (board-state update, BOARD_STALE handoff, planned-queue scan, pre-large-work materialization) continue to execute at current turn-end. The mandatory-order constraint (element 1 strictly before element 2) still holds and spans the cycle-N → cycle-N+1 turn boundary; what §13.21 forbids — skipping element 2 from the next cycle's start — is a separate failure mode this section continues to bind against. **Two technical rationales reaffirmed**: (a) **concurrent-watcher-overlap avoidance** — same-turn rearm at cycle N's turn-end produces two watchers armed at near-identical `base` cursors covering the same forward window with no added coverage at the shared base (both share the armed-window blind spot for inbound at lines below base); next-cycle-start placement spawns exactly one watcher at a time across the boundary (prior watcher has WAKE'd or REARM-timed-out by cycle N+1's start, so the next-cycle-start spawn replaces rather than overlaps). (b) **Cleaner base-cursor semantics** — the cycle-N+1-start rearm's `base = cursor-after-latest-processed-inbound`, by construction including any inbound the cycle-N+1-start re-probe just surfaced; same-turn rearm at cycle N would still miss inbound that arrives between probe-time and final-text-emission. **`Constellation.md §13.16.6` edits**: heading restored to "Unconditional rearm — the discipline that prevents the multi-cycle failure mode"; element 2 body restored to its next-cycle-start execution-point description; cadence-interaction `[executed at the start of the next cycle, not at turn-end]` annotation restored on the 6-element checklist; "Element 2 timing — relocated from turn-end to next-cycle start" paragraph restored (with the two rationales); "Mandatory ordering" paragraph's 4-step list relabeled with `[at turn-end of cycle N]` / `[at next-cycle start of cycle N+1, as the first action]` annotations; "Wrong order — anti-pattern" paragraph updated for the cross-boundary ordering (and retaining the prior sanitization of the chat quotation — no verbatim quote reintroduced); "Composition with the other four elements" paragraph updated to reflect cross-boundary ordering. **Tail paragraph replaced — §13.21 composition clarified**: the v2.4.11 tail paragraph cross-linking §13.21 + #442 (which had asserted "rearm is current-cycle work, not next-cycle") is replaced with a new tail paragraph explicitly stating that scheduling a step to next-cycle start when that is the structural correct location is NOT deferral; §13.21 forbids deferral-as-excuse (silent re-route into hypothetical fresh-context cycle), not step placement at structurally correct cycle boundaries. The two compositions stand together: §13.16.6 places element 2 at next-cycle start; §13.21 forbids the agent from then defaulting on it once the next cycle arrives. Frontmatter version v2.3.5 → v2.3.6. **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16.6 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.11** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering revert: watcher rearm (element 2) returns to current turn-end (rolls back v2.4.9 timing change).** Constellation-only patch (frontmatter version v2.3.4 → v2.3.5). v2.4.9 relocated element 2 (unconditional watcher rearm) from current turn-end to next-cycle start under a concurrent-watcher-overlap + cleaner-base-cursor rationale. **That timing change is reverted in v2.4.11**: rearm is part of the current cycle's turn-end obligation, executed inline alongside elements 1, 3-6, before emitting the final response text. **Why technical**: the next-cycle-start timing violated the §13.21 "defer-to-next-cycle = anti-pattern" rule (any work the current cycle owes must complete inline; deferring to a next cycle that may never reach the deferred step under context-window-pressure-driven compact is the failure mode §13.21 codifies) and the #442 planned-queue-scan / no-idle-deferral discipline (the rearm is current-cycle work the agent owes the inbound-side liveness loop, not a queueable item for a future cycle). The cost-asymmetry argument that justified unconditional spawn discipline (commits `a7911fd`, `852a113`) still applies and now binds: FP cost = one task slot + a few log lines (harmless concurrent watcher); FN cost = hours of lost inbound from a deferred rearm that the next cycle may never reach. Concurrent-watcher-overlap is harmless under this asymmetry — multiple alive watchers at overlapping base cursors waste only a task slot and a few log lines, while the silent-stall cost of a deferred rearm is hours of lost inbound. **`Constellation.md §13.16.6` edits**: heading restored to "Turn-end mandatory rearm — the discipline that prevents the multi-cycle failure mode"; element 2 text restored to unconditional rearm at turn-end without next-cycle annotation; the v2.4.9-introduced "Element 2 timing — relocated from turn-end to next-cycle start" paragraph removed entirely; the 4-step concrete sequence restored to its pre-v2.4.9 form ((a) tail inbox.log → (b) surface meaningful inbound → (c) advance cursor → (d) THEN unconditional rearm, all at turn-end, no cycle annotations); Mandatory ordering and Wrong-order anti-pattern paragraphs restored to pre-v2.4.9 form (no cross-cycle ordering remarks); Composition-with-the-other-four-elements paragraph restored to pre-v2.4.9 form. **New paragraph at §13.16.6 tail**: explicit cross-link to §13.21 + #442 stating "rearm is current-cycle work, not next-cycle" — locks in the no-defer composition so the timing change is not re-introduced. The new paragraph names the prior refinement and the reason for revert (FP cost ≪ FN cost asymmetry; concurrent-watcher-overlap is harmless). **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16.6 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.10** (2026-06-01) — **`Constellation.md §13.16.8` new sub-section: A2A communication priority discipline.** Constellation-only patch (frontmatter version v2.3.3 → v2.3.4). New §13.16.8 (~50 lines) inserted between §13.16.7 (BOARD_STALE gate) and §13.17 (main-chat ban), codifying a discipline applied whenever a state change (user steering / new task / external event such as a counterpart `Delegate`/`Report`/`Status`/`BlockerNudge` inbound) produces a work plan containing one or more outbound A2A messages. **The rule**: the agent (1) scans the new plan's action list to identify outbound-A2A items, (2) also identifies prerequisite work the counterpart depends on — commit-push of referenced docs (a `Delegate` payload that points to a doc URL is unusable until the doc is reachable at that URL), payload assembly, `state.json`/live-board updates the counterpart's decision depends on — and (3) moves both the message and its prerequisites to the head of the queue ahead of other planned actions; (4) other actions resume only after the message is emitted. **Two technical rationales**: (a) **drift-containment** — if EG updates a spec/state/doc before notifying the counterpart, the counterpart continues operating on a stale baseline for the entire unnotified-gap duration, risking superseded-baseline work + follow-up reconciliation cost; sending the message first bounds the drift window to round-trip latency (transport-bounded, typically orders of magnitude smaller than EG's queue depth) rather than EG's local-work duration. (b) **counterpart-start-time minimization** — when the new plan obligates the counterpart to start work (DB benchmarking, reference-runtime cp, RRP review, etc.), total wall-clock to cycle completion is `message-emit-time + counterpart-work-duration`; advancing emit-time directly shortens the cycle, and local EG actions that don't gate the message can run in parallel with the counterpart's work once the message is out but cannot recover the head-start lost by emitting late. **Symmetry**: applies to any role classified by §13.16 (`main` / `local` / `collab` / `upstream`) when that role initiates a cross-agent change — worker-to-main report, main-to-worker delegate, EG-to-upstream report, upstream-to-downstream steering all follow the same rule. **Composition with existing §13.x — no override of orthogonal gates**: §13.14 redaction still gates the message *content* (placeholder substitution, technical-only phrasing); §13.17 main-chat ban still gates the message *destination* (board-mediated route for structured Q/A); §13.20 `BlockerManifest` emission is itself an A2A communication and gets the same priority; destructive-operation authorization gates are not bypassed when the prerequisite work is itself destructive. **Composition with §13.16.6** turn-end / next-cycle ritual: cursor-tail probe (element 1) and next-cycle rearm (element 2, relocated to next-cycle start in v2.4.9) run on their own schedule and are independent of this discipline — the priority rule governs new outbound messages triggered by the current cycle's work plan, not the liveness-maintenance loop. Element 5 (planned-queue scan) interacts in the obvious way: when the scan classifies the next progressable item as an outbound A2A message (or as prerequisite work for one), the priority rule confirms it as head-of-queue. **Composition with §13.18 recommend+proceed**: this priority rule sits above §13.18 — even a 1-line "recommend+proceed" announcement is itself an outbound message in the main-chat surface; if its emission unblocks a counterpart (a `Status` update the counterpart's `ReviewSLAAck` was waiting on, a board-side `Decision` downstream agents will read), the 1-line emission takes priority over other planned actions per the same drift-containment + counterpart-start-time rationales. **Failure mode this prevents**: 5-action plan with action #3 = `Delegate` to worker executed in plan-order delays the worker's start by the wall-clock cost of actions 1-2 and risks stale-state work if action 2 touched a spec the worker reads; the priority rule reorders to prerequisite-of-3 → 3 → (1, 2, 4, 5 in natural order), giving the same final state with counterpart starting at the earliest possible moment and drift bounded by round-trip latency. **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.9** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering refinement: watcher rearm (element 2) relocates from turn-end to next-cycle start.** Constellation-only patch (frontmatter version v2.3.2 → v2.3.3). The 6-element turn-end ritual composition is unchanged in scope; what changes is the execution point of element 2 (unconditional watcher rearm). Element 2 now executes as the **first action at the start of the next user-facing response** (cycle N+1's start), not at the close of the current response (cycle N's turn-end). Elements 1 (cursor-tail probe), 3 (board-state real-time update), 4 (BOARD_STALE gate handoff), 5 (planned-queue scan), and 6 (pre-large-work context materialization) continue to execute at turn-end. The mandatory-order constraint (element 1 strictly before element 2) still holds and now spans the cycle-N→cycle-N+1 turn boundary. **Technical rationale**: (a) **concurrent-watcher-overlap avoidance** — running element 2 at turn-end of cycle N while the prior cycle's watcher may still be polling produces two watchers armed at near-identical `base` cursors covering the same forward window; the same-turn rearm does not add coverage (both share the armed-window blind spot) but does add a redundant concurrent watcher. Relocating to next-cycle start spawns exactly one watcher at a time across turn boundaries — the prior watcher has either WAKE'd or REARM-timed-out by cycle N+1's start, so the next-cycle-start spawn replaces rather than overlaps. (b) **Cleaner base-cursor semantics** — element 1 at turn-end of cycle N advances the agent's cursor to include any inbound below the prior watcher's `base`; if element 2 also ran at turn-end of cycle N, its fresh watcher's `base` would still miss any inbound that arrives between probe-time and final-text-emission. Relocating element 2 to next-cycle start means the rearm `base` is the line count at cycle-N+1-start, which by construction already includes any inbound the cycle-N+1-start re-probe just surfaced; the rearm-base invariant becomes "cursor-after-latest-processed-inbound" rather than "cursor-at-an-arbitrary-mid-turn-moment". **Net effect**: same coverage with one watcher armed at a time across turn boundaries, cleaner base-cursor invariant. **What didn't change**: all other turn-end ritual elements stay at turn-end; the cost-asymmetry argument for unconditional spawn discipline still holds; element 1 strictly before element 2 still holds. Edits to `Constellation.md §13.16.6`: frontmatter version bump, new "Element 2 timing — relocated from turn-end to next-cycle start" paragraph, "Mandatory ordering" paragraph's 4-step list relabeled with `[at turn-end of cycle N]` / `[at next-cycle start of cycle N+1, as the first action]` annotations, "Wrong order — anti-pattern" paragraph cross-references the relocation, "Composition with the other four elements" paragraph updated. **Scope discipline**: Constellation-only patch. Seed lib unchanged (seeds reference §13.16.6 by anchor, not by body). Protocol wire unchanged. Per `feedback_release_versioning_cadence.md` — in-flight patch tag cut, not catch-up.
- **v2.4.7** (2026-06-01) — **A2A PR System RRP v0.1 draft publish.** New RRP doc `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` (~55KB · 15 sections + headmatter) drafts a new Constellation protocol feature §13.22 — cross-repo Pull Request orchestration via the A2A messaging layer, replacing the manual hand-create-PR coordination tax that scales badly as cross-repo spec parity needs grow. **Wire vocabulary**: 5 new `CUSTOM` types — `PRRequest` / `PRDraftReady` / `PRReviewAck` / `PRMergeRequest`+`PRMergeAck` / `PRStatusUpdate`. **Agent topology**: 3 options evaluated (dedicated PR-bot agent / local-bridge `cmd:createPR(spec)` / hybrid) — recommendation: topology (b) local-bridge for v0.1 (reuse local git/gh CLI auth, no separate credential store). **Approval discipline**: 3 categories — `trusted_mirror` (state.json opt-in list) / `standard` (per-PR opt-in default) / `emergency` (block, never auto). **Content sourcing**: α (diff in payload) / β (branch ref + git API read) / γ (fork-based) — recommendation: β with α fallback for small diffs. **Authority**: §13.20 permission table extension — `role==main` for target repo can `PRMergeRequest`; any agent can `PRReviewAck`; `PRRequest` open. **State machine**: 5-state PR lifecycle (DRAFT → OPEN → IN_REVIEW → APPROVED → MERGED + side paths CLOSED / CHANGES_REQUESTED) + §13.19 deadlock integration (PR waiting >SLA on review = quasi-deadlock candidate) + live-board panel ask (PR queue visualization). **Dogfood plan**: canonical first use case = §13.14/§13.15 mirror PR from EG → `<hub-repo>` WS-PROTOCOL.md — the §13.14/§13.15 mirror PR itself is proposed as the first PR routed through the new system, proving the protocol works at the moment it lands. **Open questions × 8** (3 priority-flagged): topology — does `<hub-repo>` accept PRs via local `gh` CLI identity? content-sourcing — direct `mirror/...` branch push or fork-based? authority model — strict `role==main` gate or delegate to provider ACL? **Acceptance criteria**: spec (§13.22 body) · impl (local-bridge `createPR` cmd) · dogfood (§13.14/§13.15 mirror PR created via protocol, not manual) · composition (§13.19 + §13.20 integration). **Migration path**: phase 0 RRP review → phase 1 prototype → phase 2 dedicated PR-bot agent → phase 3 full §13.20 integration. **Redaction**: per §13.14, `<hub-repo>` / `<main-prod>` placeholders throughout; only `SoliEstre/EstreGenesis` (public repo) named directly. **Scope**: RRP-only draft — review-only artifact pending main review. v0.1 DRAFT status banner + every concrete design decision flagged "(open for main review)". Constellation.md §13.22 body integration is the post-review patch. Seed lib unchanged. Per `feedback_release_versioning_cadence.md` 자체 태그 cut.
- **v2.4.6** (2026-06-01) — **DB P2 Mode B entry docs publish.** Two new docs in `constellation/reference/docs/`: (1) **`DB-P2-Benchmark-Framework.md`** (~36.6KB · 8 sections) — purpose + metrics catalog (correctness/performance/operational/robustness) + sample sizing (floor: 10k events + 7 days · recommended: 100k events + 30 days + stability gate) + comparison methodology (sequential-not-parallel cost · disk totals · same-crash recovery · production read mix · hrtime clock) + data collection mechanics (server-side telemetry hooks + `benchmarkActive` flag + scheduled `exportJsonl()` diff + dashboard tab spec) + promotion decision framework (quantitative + qualitative gates + `promotion-decisions/` artifact) + rollback rehearsal protocol + open questions × 5. (2) **`DB-P2-Mode-B-Entry-Runbook.md`** (~26.4KB · 8 sections) — pre-flight 6-step mechanization of DB-P2-Guide §6.2 (backend probe verify · disk space verify · JSONL backup verify · zero-traffic window · state.json edit+restart · post-check reconcile) + backfill expectations + Mode B day-to-day monitoring + benchmarking activation pointer + rollback procedure (Mode B → Mode A, 6 steps) + failure-scenarios quick-ref table + Mode C promotion preview + communication discipline (ServerNotice + informational BlockerManifest). Both docs cross-link DB-P2-Guide.md (§3/§4/§5/§6/§7/§9) + `constellation/history-store.eux` (L39-215 / L286-469 / L475-567) + Constellation.md §13.20. **Effect**: step-by-step Mode B entry guide + benchmarking data-collection framework. Mode C promotion gated on sufficient data accumulation (minimum: 100k events / 30 days / 8+ quantitative gates pass). Scope: docs-only patch — Constellation.md body unchanged, seed lib unchanged, protocol wire unchanged. Per `feedback_release_versioning_cadence.md` 자체 태그 cut.
- **v2.4.5** (2026-06-01) — **§13.16.4 HELLO churn dampening SSoT update + bridge HELLO{pid:process.pid} plumbing.** Per main upstream Delegate seq 165 (msgId `m-mpua03au-164`, Fix 2 ship commit `d385561`, 2026-06-01) which landed §13.16.4 in main's `server.cjs` as a +51 line pure-additive `wsHelloFlapCheck` + `_flapHello` ring buffer (N=5/K=3/span=10s/cooldown=30s) emitting `ServerNotice{kind:"flap-rejected",incumbentPid:<prev>}` on HELLO churn detection. EG side covered two follow-ups main asked for: (1) **`Constellation.md §13.16.4` SSoT update** — heading promoted `(adopted at upstream production; reference impl pending)` → `(adopted at upstream production · reference impl shipped d385561)`; adopted-behavior paragraph rewritten from "queued for a follow-up cp" to "shipped in commit d385561, 2026-06-01; reference cp of server.cjs into constellation/reference/runtime/ remains a separate follow-up cycle"; explicit cooldown bullet added (default 30000ms = 30s — long enough to absorb a flap burst, short enough that a legitimate restart waits ~30s before HELLO is honored); explicit env-knobs bullet added with 4 names verbatim per main d385561 (`CONSTELLATION_FLAP_RING_N` default 5 · `CONSTELLATION_FLAP_RING_K` default 3 · `CONSTELLATION_FLAP_SPAN_MS` default 10000 · `CONSTELLATION_FLAP_COOLDOWN_MS` default 30000; operator-tunable per deployment, e.g. slow-restart bridge needing N=10 / span=20000); 1-sentence postscript anchoring the bridge HELLO{pid:process.pid} workstream that populates the previously-null `incumbentPid` field. (2) **bridge HELLO{pid:process.pid}** — `constellation/local-bridge.eux` 3 edits: @behavior `connect` HELLO envelope updated to carry `{pid: process.pid, capabilities.inbound: ...}` + inline §13.16.4 incumbentPid plumbing note; @machine OPEN entry parenthetical extended to clarify `HELLO carries {agentId, pid: process.pid}`; new `@incident bridge_pid` line at file tail anchoring seq 165 / msgId `m-mpua03au-164` / ship trigger `d385561`. `constellation/reference/runtime/local-bridge.cjs` 1 edit: `ws.onopen` callback HELLO payload object adds single `pid: process.pid` key. Effect: from next bridge connection onwards, server §13.16.4 `wsHelloFlapCheck` `ServerNotice{kind:"flap-rejected"}` carries real PID in `incumbentPid` instead of null, enabling operator to identify which bridge instance is the incumbent during a churn event. **Scope discipline**: seed lib unchanged (Constellation.md anchor §13.16.4 already referenced from seeds by anchor, not body — no cascade); Constellation-only patch tag cut immediately per `feedback_release_versioning_cadence.md` (in-flight cut, not catch-up). **§13.20 BlockerManifest update**: Fix 2 follow-up closes → 1 active jet (review_4점 후속) + upstream-steering 4 items remaining.
- **v2.4.4** (2026-06-01) — **`.eux` `@source` provenance promotion + new `history-store.eux` distillation (DB P2 backend).** Per main upstream Delegate seq 159 (msgId `m-mpu9febn-158`, a5bc §F mapping, 2026-06-01). The mapping table is a function→line range covering main's `server.cjs` (799 lines actual, NOT 794 spec) + `history-store.cjs` (~580 lines) + `examples/ws-wait.sh` (BOARD_STALE gate). EG side did three things: (1) **`constellation/history-store.eux` (new file, ~19.9KB)** — v1 distillation of the DB P2 HistoryStore backend per main upstream `a28150e` ship. All 11 v1 directives used (`@component` / `@intent` / `@expansion` / `@targets` / `@state` / `@ports` / `@machine` / `@source` / `@behavior` / `@rationale` / `@incident`). 7 explicit `@source` ranges: HistoryStore abstract contract (L26-34) · JsonlStore class (L39-215) · SQLITE_SCHEMA DDL (L225-238) · selectSqliteBackend probe (L246-279) · SqliteStore class (L286-469) · HistoryStoreMux class (L475-567) · createHistoryStore entry factory (L572-577). Mode A/B/C state-machine + auto-revert (drift ≥2/30min + partial-fail ≥3/10min) + N=10 reconcile gate documented per DB-P2-Guide.md. (2) **`constellation/server.eux`** — explicit `@source` directive added (previously inline `(Distilled from ...)` only) reflecting v2.3.x+DB-P2 ship state. Ranges per seq 159 §F: L188-360 (KEY-MGMT) · L414-625 + L670/L674-678/L795-796 (§13.19 deadlock observation layer) · L159-160/L382-385/L681-690/L733-734/L736-758/L778-787 (UI6 SelectionPrompt + hard-TTL sweep) · L368-381 + L386 (DB P2 thin shim — SqliteStore/HistoryStoreMux dormant by default, mode A floor untouched) · L247-265/L267-277/L279-300 (`wsCollabOnboardMd` / `wsCommit` / `wsSetMain` / `wsHandoffReady` / `wsHandleOrch` dispatch). Pre-existing inline provenance preserved (still-valid lineage for handshake/relay paths predating v2.3 per `.eux` v1 §3.8). (3) **`constellation/self-wake-watcher.eux`** — explicit `@source` directive added per seq 159 §F. Ranges for `examples/ws-wait.sh`: L31-38 (env defaults) · L46-55 (`meaningful()` filter) · L57-71 (BOARD_STALE gate per §13.16.7 / #410 / #434 amendment — probe L60-66, comparison L64-65, WAKE L67-70) · L73-92 (poll loop) · L93-94 (re-arm timeout). **Why**: `.eux` v1 §3.8 explicitly recommends promoting `@source` provenance from inline `(Distilled from ...)` to explicit directives; the directive is line-precise WARN-tier. seq 159's §F mapping provided main-verified line ranges across the v2.3.x+DB-P2 ship state, which enables EG `drift-check --contract` 3-way alignment (spec ↔ runtime ↔ `.eux` distillation) at G6 (`@source` WARN gate) for backend/protocol-profile files. **Scope discipline**: Constellation.md unchanged; seed lib unchanged; no protocol body change. Per `feedback_release_versioning_cadence.md`, .eux-only patch cuts a tag of its own rather than rolling into the next seed-lib release. **Next cycle (separate)**: raw `cp` of `server.cjs` (799 lines) + `history-store.cjs` (~580 lines) into `constellation/reference/runtime/` — gated on outbox split-delivery (W20/W22 pattern, ~80KB 20+ frames) OR upstream steering on cp scope.
- **v2.4.3** (2026-05-31) — **DB P2 EG-side guide published — `constellation/reference/docs/DB-P2-Guide.md` (33KB · 11 sections + 7 open questions).** Per main upstream Delegate seq 144 (msgId m-mptlrmm3-143) — main DB P1 ship (5cd7fca: HistoryStore + JsonlStore wrapping current JSONL) entering P2 (SqliteStore via node:sqlite primary + better-sqlite3 fallback + dual-write + count reconciliation + bootstrap opt-in). EG responsibility per seq 79 DB RRP: discipline + gate + migration + rollback guide; main implements server.cjs. Guide structure: migration mode chain (A jsonl-only → B dual-write transitional → C db-primary terminal) · node:sqlite probe + better-sqlite3 fallback (Node 22.5+ + `--experimental-sqlite` flag check) · dual-write backfill (one-shot mode-B entry, idempotent `INSERT OR IGNORE`, batched transactions) · count-reconciliation gate (mismatch = abort B→C promotion + diff report, no auto-resolve) · bootstrap opt-in (default mode A on fresh install, operator-flipped via `state.json.modes.historyBackend`) · failure modes + recovery (WAL corruption, partial dual-write, count drift, BlockerManifest emission per §13.20) · schema sketch (1:1 JSONL field shape mirror) · `exportJsonl()` round-trip safety net · cross-links (§13.20, §13.19.4, P1 5cd7fca). 7 open questions: WAL checkpoint cadence · old JSONL retention policy · schema migration discipline for future versions · multi-server/replica story (out of scope) · `exportJsonl()` performance at 10^7+ · `source` column enum extensibility · backend probe cross-compatibility in mixed-Node deployments. Constellation.md unchanged; seed lib unchanged. Independent track per `feedback_release_versioning_cadence.md` — Constellation-reference-doc-only patch cuts a tag of its own. §13.20 BlockerManifest list: 6 → 4 (this commit closes DB-P2-guide blocker; remaining: a5bc §F · Fix 2 PR · WS-PROTOCOL §13.14/§13.15 mirror PR · per-user/EG steering).
- **v2.4.2** (2026-05-31) — **§13.16.6 Filter discipline expanded: server-replay noise (`History`) + transport-layer acks added to the blocklist.** Constellation-only patch (seed lib version unchanged at v2.4.1). The v2.4.0 filter discipline scoped to `ev:"inbound"` to exclude self-emission echo (false-positive #1); v2.4.2 adds a second false-positive class — `History` payloads (server-side session replay on reconnect; line-level large, contains nested `Delegate`/`WorkerReport` events from prior sessions that a naive name-grep treats as fresh inbound) + transport-layer `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate` frames. Canonical bash filter is now two-stage: (1) `grep '"ev":"inbound"'` → (2) `grep -vE '"name":"(History|Ack|AckProcessed|AckCumulative|AckPolicyUpdate|ServerNotice|AgentList|Status|ConnectionRestored|Heartbeat|Typing|UserPromptAccepted|PersistentAdapterSmoke|OnboardAck|WorkerInboxReceived|EditMessage|MainChanged)"'` → (3) `grep -cE '"name":"(Delegate|UserPrompt|WorkerReport|WorkerAck|AgentHello|Handoff|Report|Command)"'`. Surfacing incident: watcher `bbae2ph8r` 2026-05-31 wake caught a server `History` replay payload as a `WAKE (meaningful)` event — historical inbound from prior sessions matched by name-only grep, not a new external delivery. Constellation.md §13.16.6 Filter-discipline paragraph rewritten end-to-end; no seed body change required (seeds reference §13.16.6 by anchor, not by body). Cadence: per `feedback_release_versioning_cadence.md`, cut a Constellation-only patch tag immediately rather than rolling into the next seed-lib release.
- **v2.4.1** (2026-05-31) — **§13.16.6 element-ordering reinforcement: cursor-tail probe MUST execute BEFORE unconditional rearm.** Discipline-tightening patch. The 6-element turn-end ritual composition is unchanged; what was missing is the mandatory ordering between element 1 (inbox cursor-tail probe) and element 2 (unconditional rearm). Probe is reframed from "telemetry only, never a gate" (v2.4.0 wording) to **PRIMARY catch** for the armed-window blind spot — rearm alone does NOT recover inbound below its own base. Explicit 4-step ordered sequence added: (a) tail inbox.log from last-surfaced cursor → (b) surface any meaningful inbound → (c) advance cursor → (d) THEN unconditional rearm. Anti-pattern paragraph added with 2026-05-31 incident anchor: agent ran rearm first, missed seq 123 (Status) + seq 127 (Report: main DB P1 ship `5cd7fca` + §13.19 P2 spec-delivery query) from the prior watcher's SIGSTOP-suspend window; user surfaced "메인이 회신 대기중이라는데" to force the cursor-tail probe that should have run at turn-end automatically. Composition paragraph updated to call out element 1 as PRIMARY (armed-window blind-spot catch) + element 2 as forward-looking (next-idle-window catch); BOARD_STALE gate downgraded from "safety net" to "fail-safe / backstop when agent-side discipline fails". Cascade across `Constellation.md §13.16.6` + seed × 6 (Master / Lite / Compact × EN/KO) with the established verbosity gradient. First test-drive of the `feedback_release_versioning_cadence.md` policy (cut versions in flight, not batch catch-ups) — v2.4.0 was the long-overdue catch-up; v2.4.1 lands the same day for a single discipline-tightening change.
- **v2.4.0** (2026-05-31) — **Constellation coordination-layer protocol expansion: §13.16.6 absolute turn-end ritual (6 elements) + §13.16.7 BOARD_STALE + §13.17 main-chat ban + §13.18 recommend+proceed + §13.19 deadlock formal spec + §13.20 blocker tracking + §13.21 fresh-context defer anti-pattern; `.eux` v1 spec publish + UI 6 / KEY-MGMT WS protocol drafts; EstreUX seed × 6 cascade (option C closed); reference sync to main UI 3·4·5·6 ship.** Seven new/revised `§13.x` Constellation sub-sections form a continuous autonomous-execution discipline chain. (1) **§13.16.6 v2** — turn-end mandatory ritual escalated to absolute-tier 6-element checklist: inbox.log cursor-tail probe · UNCONDITIONAL watcher rearm (probe is telemetry, never a gate; cost-asymmetry argument FP-spawn-harmless < FN-miss-silent-stall; conditional rearm from earlier draft RETIRED) · board state real-time update · BOARD_STALE gate · **planned-queue scan (#442)** · **pre-large-work context materialization** (durable artifact emit so post-compact resume is possible). Watcher liveness probe + cursor-tail fix + 4-evidence-confirmed armed-window-blind-spot diagnosis + bg-task SIGSTOP wall-clock-vs-task-clock observation + filter-discipline self-emission exclusion. (2) **§13.16.7 BOARD_STALE gate** — pre-rearm board freshness check via `git log -1 HEAD --format=%ct > git log -1 -- state.json --format=%ct` (identity-based; an earlier 120s time-buffer version self-trapped and is retired). (3) **§13.17 main-chat structured-choice prompts FORBIDDEN** — `AskUserQuestion`-style option UI bans in agent main-chat (no board projection); route via `defer-OK` review-items tab or `need-now` UI6; review-items tab badge "0 → hidden, tab stays" UX discipline. (4) **§13.18 non-branching choices recommend+proceed** — when a recommendation exists for a non-fork-of-architecture choice, 1-line announce + proceed; do NOT escalate via `A/B/C/D?` list; parallel is default-presumption when feasible. (5) **§13.19 deadlock formal spec** — wait-pattern classification (strict / quasi / healthy + livelock=quasi variant) · detection (wait-edge DFS 2-agent + wait-class timeouts + §13.16.6 agent-active correction) · prevention (priority ordering §13.9 tree + `ReviewSLAAck{eta,SLA-OR-WORK}` + termination signals `DONE / BLOCKED{reason,waitingOn} / NEEDS_HUMAN`) · auto-resolution (preemption `PreemptRequest/Force` + deadline + starvation guard) · escalation 4-tier (ordering → preemption → mediation → human §13.17) · 3-tier ack layering above §13.13 (transport / commitment / application). Q2-Q3-Q4-Q6 decisions: CNP rejected-as-wire + preserved-as-discipline; mediator hybrid (board adapter default + 3rd-party agent opt-in); human-fallback bounded-probe-count × wait-class severity, cap ~3-5/24h; renegotiate-SLA fixed cap N=2 then human-escalate. (6) **§13.20 blocker tracking + periodic nudge discipline** — `BlockerManifest` emit BEFORE idle wait (subject / reason / EG-side action / request-history + count); periodic `BlockerNudge` (env `CONSTELLATION_BLOCKER_NUDGE_CYCLES`, default 1 per ~45min rearm); 4-tier escalation polite → explicit + ETA → hard deadline → human §13.17; distinct from §13.19 (linear-stalled blocker vs cyclic-deadlock). Live-board panel ask to main upstream (external-blocker-manifest visualization). (7) **§13.21 fresh-context defer = anti-pattern** — compact is harness-fired on threshold (no agent `/compact` discretion); fresh-context expectation is wishful + unrealistic; planned MUST NOT be deferred under context-length excuses (#442 reinforcement: time + context excuses both forbidden). The §13.16.6 element 6 is the *capability* (materialize artifacts), §13.21 is the *discipline* (defer forbidden). Three-section policy chain ships: §13.18 (choice-surface) → §13.16.6 #442 + element 6 (work-progress-surface) → §13.21 (time-surface). **`.eux` format v1**: `constellation/reference/docs/eux-format-v1.md` (26.9KB · 9 directives total: 5 formal/tier-gated + 4 structural; v0's 8 + new `@behavior`; `@deps` absorbed under `@ports.deps` per RRP A4 amendment per 12-file EG empirical evidence); `EUX-V1-CATALOG.md` (291 lines — profile distribution, per-file matrix, Verify "runtime 6-check matrix" with C1-C6 prefix to disambiguate from spec §4 G1-G6 structural gates); `WS-PROTOCOL-KEY-MGMT.md` (22KB · 5 message types `KeyIssue/KeyList/KeyRevoke[immediate|sessionEnd]/KeyLabel/AgentNameChanged` + 5-state machine + role-naming align `main|local|collab|upstream`); `WS-PROTOCOL-UI6-SELECTION.md` (25.8KB · 3 message types `SelectionPrompt/Answer/Cancel` + 3-state machine; cohesion-separated from KEY-MGMT). **EstreUX seed × 6 cascade** closes long-deferred option C: `§5 #12 EstreUX adoption + citation` core rule + `§ Constellation → ### EstreUX (Estre Universal eXpression)` sub-section across Master EN/KO + Lite EN/KO + Compact EN/KO with tier verbosity gradient. **Runtime updates**: `self-wake-watcher` poll interval 15s → 5s (`MAX 196 → 588`, ~49min rearm preserved); `ws-core.eux` v0.2 fragmented frame assembly (FIN-bit + `_frag` accumulator + `_deliver` single entry, `conn.remoteAddr`/`conn.ua` ports); anonymous-HELLO reject (`conn.meta.anonymous` flag + origin logging + relay-skip); pre-commit `check-reference-syntax.mjs` syntax gate + `check-reference-regressions.mjs` proposal-002/004 functional guard; `local-ide-agent` legacy-identifier sweep → generic `main-agent` per §13.14 redaction. **Reference sync** to main UI 3·4·5·6 ship (521+ insertions): KEY-MGMT legacy `RegisterUpstreamKey` reconciliation (supersede + transitional alias, deprecated v0.2, retire on zero-traffic gate); UI6 impl alignment (`SelectionPrompt` sender `role === main` tightening, board permission `meta.role !== agent`, 3 wire-path declarations). **Honest history**: rolled-out-then-corrected commits kept as follow-up + retired-version notes (NOT reverted) — `a7911fd` armed-window blind spot replacing GC-kill misdiagnosis; `9be6bf0` head-vs-state-commit-time replacing 120s self-trap buffer; `0185b8d` conditional-rearm retired by `852a113` unconditional escalation. **Deadlock joint formalization** with main upstream RRP `reports/2026-05-31-deadlock-resolution-rrp.md` + EG draft v0.1 (commit `82ab55e`) → §13.19 (commit `32a2c75`). Drafted by the EstreGenesis agent with upstream live-board main co-review across Delegate seq 70 / 73 / 77 / 79 / 83 / 85 / 88 / 92 / 94 / 98 / 103 / 105 / 109 / 111 / 113 / 120 over the 2026-05-31 cycle.

---

<a id="한국어"></a>

## 변경 이력 (한국어)

각 tier 는 자체 버전 보유. 마스터가 권위 있는 진화 트랙; Lite·Compact 는 정기적으로 마스터에서 파생되나 한 릴리스 지연될 수 있음.

본 파일이 **changelog SSoT** — 각 시드 파일은 tier, 언어, 현재 버전, counterpart, 그리고 시드를 복사한 대상 프로젝트의 README가 아닌 본 `CHANGELOG.md` 포인터만 담은 짧은 헤더 메타데이터를 유지. 마이그레이션 B 로 프로젝트 업그레이드 시 에이전트는 시드 헤더의 버전 마커와 본 changelog 로 delta 를 계산.

- **v1.0** (2026-04-18) — 초판 (마스터). Author의 두 번째 AI Native 프로젝트 기반.
- **v1.1** (2026-04-19) — Research → Report → Plan 루프 섹션 추가. 핵심 원칙 #7.
- **v1.2** (2026-04-20) — 마이그레이션 A/B/C 가이드 추가. 에이전트 지침 상단 Bootstrap/Migration 모드 분기. Lite·Compact tier 초판 릴리스.
- **v1.3** (2026-05-02) — 핵심 원칙 2개 추가: #8 (인덱스 ↔ 본문 동기화) · #9 (외부 인터페이스 N-way sync). 신설 섹션 6개: § 인덱스 동기화 정책 · § 외부 인터페이스 N-way sync · § 마크다운 `~` escape 정책 · § RAG 인덱스 최적화 · § 외부 전달용 빌드 파이프라인 · § 문서 인플레이션 방지 (부록 `<NN>b_*.md` 패턴). 3 tier 모두 업데이트; Lite·Compact 는 동일 패턴의 축약본 추가. 모든 추가는 Author의 두 번째 AI Native 프로젝트의 2026-04-21 → 2026-05-02 운영 실측 사례에서 추출: 외부 표면이 일주일 stale 상태로 누락됨 (N-way sync 정책 동기), 외부 송부용 PDF 의 마지막-순간 마크다운 취소선 오작동 (tilde escape 정책 동기), 다른 에이전트가 옛 인덱스로 작업 (인덱스 동기화 정책 동기), Claude 프로젝트 워크스페이스의 파일 용량 100% 도달 (RAG 친화 인덱스 규칙 동기).
- **v1.3.1** (2026-05-02) — 자기 완결 툴링: 마스터 시드 § 파일 템플릿이 외부 송부용 툴체인 전체 inline 스크립트를 내장 — 시드만 가지고 전체 부트스트랩 가능. 신설 템플릿: `scripts/escape-md-tildes.mjs` (의존 없는 Node ≥18, idempotent, placeholder 보호 알고리즘), `scripts/build-md-to-html.mjs` (`marked` 기반 MD → A4 HTML + h2 page-break + 프로젝트 언어 폰트 스택), 편의 wrapper `scripts/build-pdf.ps1` (Windows) / `scripts/build-pdf.sh` (macOS/Linux) — 3 단계 `escape → HTML → Chrome --print-to-pdf` 파이프라인 chain. § 마크다운 `~` escape 정책에 "알고리즘" 서브섹션 추가 (placeholder 보호가 *왜* 필수인지 — 순진한 regex 의 함정). § 외부 전달용 빌드 파이프라인에 "왜 이 도구들인가" 서브섹션 (`marked` vs Puppeteer trade-off, A4 page rules) + macOS/Linux Chrome 등가물. Lite·Compact tier 의 § 마크다운 `~` escape (축약) 에 placeholder 알고리즘 요약과 크로스플랫폼 PDF 파이프라인 보강; 마스터 시드가 inline 스크립트의 단일 진실 원천 유지.
- **v1.3.2** (2026-05-02) — `.gitignore` 템플릿 + cross-tier 정합성 감사. **`.gitignore`**: 마스터 시드 § 파일 템플릿이 inline 템플릿 내장 — Common (OS, IDE, env, 로그) + Node / Python / Go / Rust / JVM / C-C++ / Mobile / DB 스택별 행 + 시드 산출물 블록 (`.generated.` infix 컨벤션 사용 — 빌드 산출물만 정확히 휩쓸고 수공 첨부는 보호). **Cross-tier 정합성 감사**: README 의 stated 원칙 ("각 tier 는 self-contained — 다른 tier 로의 forward/backward 참조 없음") 이 v1.0 부터 위반돼 왔고, v1.3.1 릴리스가 cross-tier 참조를 더 추가해 악화. v1.3.2 가 **Lite** 와 **Compact** tier 를 원칙 준수로 정상화 — 모든 "→ 마스터 시드 § ..." cross-reference 제거. Lite 는 909 / 912 줄 (한국어 / 영문) 에서 신설 § 파일 템플릿이 inline AGENTS.md, `.agent/rules.md`, `.gitignore` (Common + Phase 2 스택 + 시드 산출물), `scripts/escape-md-tildes.mjs` (압축 Node ≥18), `scripts/build-md-to-html.mjs` (압축 `marked` + A4 CSS), 11 종 지원 AI 서비스 브릿지 stub 모두 내장. Compact 는 119 줄/언어 에서 신설 § 스캐폴드 spec 가 각 스캐폴드 대상에 알고리즘-spec 묘사 — 에이전트가 spec 읽고 실제 파일 생성. 3 tier 모두 README 가 처음부터 약속해 온 *프로젝트 repo 에 독립 ship* 이제 준수.
- **v1.3.4** (2026-05-03) — 강제 훅 아키텍처 추가. Master 는 Layer 1 Claude Code `PreToolUse` + Layer 2 `git pre-commit` 2중 방어, 단일 regex SSoT `scripts/hooks/_patterns.mjs`, `.git/hooks/pre-commit` shim 및 `.claude/settings.local.json` 병합 installer, 경로 매칭·Bash 따옴표/HEREDOC false-positive 회피·테스트·cross-AI 적용 범위를 전체 섹션으로 설명. Lite 는 축약 섹션, Compact 는 원칙 #10 과 동기화 규칙 bullet 추가.
- **v1.3.5** (2026-05-03) — 작업 분해 전략 추가. 분해 경로가 여러 개인 복잡 작업에서 에이전트가 선택한 진행 형태를 announce 하고, 판단/관성에 따라 확인 대기 없이 진행하며, 사용자 피벗을 즉시 반영하는 cross-AI 행동 패턴을 추가. Master 는 임계값·관성 우선순위·AI 별 도구 매핑·안티패턴을 포함하고, Lite·Compact 는 축약 형태로 반영.
- **v1.3.6** (2026-05-03) — 인덱스 ↔ 본문 동기화에 외부 지식 인덱스 자동 동기화 추가. 프로젝트가 Claude 메모리·Notion·wiki·RAG 메타데이터 같은 외부 KB 에 구조화 폴더를 미러링하면, pre-commit 이 전용 sync 스크립트로 메커니컬 구조(카운트·stub·번호·orphan 경고)를 자동 갱신하고 의미 콘텐츠는 사람 큐레이션으로 유지. Master 는 전체 subsection, Lite·Compact 는 축약 절 반영.
- **v1.3.7** (2026-05-03) — Phase 0 이 작업 언어 다음에 에이전트 응답 말투를 선택하도록 확장. 한국어 프롬프트는 `~니다`, `~에요/예요/어요`, `~음/슴/임`, 친구/동료 말투, 직설 도전 말투, 직접 방향성 프롬프트 6가지를 제시하고, 영문 프롬프트는 이에 부합하는 tone 옵션을 제공. 기본값은 사용자가 대화를 건 톤과 동일하거나 한 단계 공손하게. 생성되는 `AGENTS.md`, `.agent/rules.md`, 브릿지 stub 이 언어와 말투를 분리 기록.
- **v1.4.0** (2026-05-03) — 문서 제목 전반에 EstreGenesis 정식 네이밍 적용, 소스 프로젝트 참조 공개용 익명화, public repo 공개. 콘텐츠 베이스: legacy private archive, commit `5944a50` (v1.3.6, 2026-05-03).
- **v1.4.1** (2026-05-06) — 메타데이터 정합화. v1.3.6 → v1.4.0 흡수 과정에서 Master tier (KO/EN) 의 마이그레이션 B delta 표에 v1.3.4 (강제 훅 아키텍처) · v1.3.5 (작업 분해 전략) · v1.3.6 (외부 지식 인덱스 자동 동기화) 행이 누락 — 다른 4 tier 는 정상 보유. v1.4.0 콘텐츠는 그대로 유지하고 누락된 3 행만 보강해 6-tier sync 복원. Master 의 Phase 7 § 파일 스캐폴딩 체크리스트에 옵션 `scripts/hooks/` 블록 명시 (강제 훅 아키텍처는 절대 규칙 강제가 필요한 프로젝트의 opt-in). README v1.4.0 항목에 legacy archive SHA 박제 추가.
- **v1.5.0** (2026-05-07) — 6개 시드 파일 전체에 Bootstrap Residency 추가. 새 Phase 2.5 가 `<scope-root>` 선택 전 작업공간이 소스 repo, 개인 agent-docs sidecar, 다중 프로젝트 오케스트레이션 repo, upstream/local split 중 무엇인지 결정. Minimal, Full manual, repo-provider assisted bootstrap 지원 (GitHub, GitLab, Bitbucket, Azure DevOps, Gitea, Forgejo, self-hosted/local remotes, 사용자 제공 repo 목록), workspace-limited agent 안내, upstream 폴더 기본값 `upstream/` 및 사용자 지정명 허용, sidecar 소스 폴더 `.gitignore` 등록은 사용자 식별 + 에이전트 확인 후로 제한. Master 는 트리거 조건이 있는 Adoption Catalog 를 추가하고 Lite·Compact 는 축약 반영.
- **v1.5.1** (2026-05-08) — 적용 프로젝트 피드백 기반 패치 릴리스. 6개 시드 헤더 모두에서 changelog 포인터가 시드를 복사한 대상 프로젝트의 README.md가 아니라 **upstream EstreGenesis repository README.md** 를 뜻한다고 명시. KO Master 는 v1.5.0 흡수 잔여 결함도 보정: Phase 0·Phase 4 마무리 줄을 Phase 7로 정정, 신규 Phase 2.5 sub-heading 한국어화, Adoption Catalog 제목/표 헤더 한국어화.
- **v1.6.0** (2026-05-09) — Agent-time vs human-time 추정. AI 에이전트가 인간 dev 팀 baseline 으로 추정하면 plan 이 5\~10× 부풀어 calendar mismatch 발생하고 PM 템플릿이 현실을 잘못 묘사. Phase 0 가 언어·말투 다음에 한 가지 더 묻도록 확장 — **실행 페이스 모드**: Cautious 2\~4× (무료 티어 또는 Continue.dev·로컬 모델 사용 Aider 같은 로컬 LLM, 출력 토큰/초 기준 추가 보정), Proactive 5\~6× (유료 플랜 기본값), Burst 6\~8× (고처리량 플랜), Sprint 9\~10× (사실상 무제한 토큰, 최대 병렬화). 각 모드 안에서 작업 유형이 위치 조정 — 실행 중심은 상단, 디버깅은 중간, 연구·전략은 인간 검토가 율속이라 모드 무관 ~1×. 모든 duration 추정은 **agent active time** 과 **human review / approval time** 분리 + handoff 갭 흡수하는 **calendar window**. 모드는 프로젝트 단위지만 진행 중 전환 가능 ("switch to sprint" / "drop to cautious") — 활성 PM 추정치 재산정 + CHANGELOG 기록 자동. `.agent/_lessons/` 자가 보정: ±30% 초과 delta 만 `estimation` 태그로 기록, 같은 모드에서 3개+ 누적 시 보정 multiplier 제안. Master 는 핵심 원칙 #11 + § Agent-Time 추정 정책 섹션 풀버전 (2축 프레임워크, 모드 전환 프로토콜, 자가 보정 루프, 안티패턴, cross-AI 적용성). Lite 는 원칙 #8 + 축약 정책 섹션. Compact 는 원칙 #13 인라인. PM 템플릿은 모든 tier 에서 split-time 형식으로 갱신. AGENTS.md 핵심 규칙 라인 1 이 언어 + 말투 + 페이스 모드 함께 기록.
- **v2.0.0** (2026-05-27) — **Constellation** 추가 (핵심원칙 #12 / Lite #9 / Compact #14), 선택적 모듈 하나. 멀티에이전트 코디네이션을 파일 기반 `.agent/_coordination/` 에서 실시간 라이브보드(WebSocket + A2A 메시징 + 대시보드)로 격상. **A2A 브릿지 인터페이스** (role board/main/local/upstream/collab · 핸드셰이크 · 메시징 · turn 기반 브릿지 데몬 + self-wake watcher 런타임 패턴) 가 불변 계약; 도입 깊이는 시드 티어 따라감. 별도 `Constellation.md` 가이드 + `constellation/*.eux` 컴포넌트 spec (`gateway-client.eux` = 디테일 티어 A2A 어댑터 포함) 으로 ship, tier 시드는 raw GitHub URL 로 참조해 lean 유지. 라이브보드 UI 컴포넌트는 `.eux` 로 작성해 **EstreUX** 로 brew — EstreUX 는 EstreGenesis 가 *참조*하는 별도 런타임(번들·소유 X); EstreGenesis 는 EUX 포맷을 소유·교육하지 않음 — EUX 는 Constellation 의 빌드타임 의존이지 시드의 레이어가 아님. 파일 기반 코디네이션(Phase 5)이 기본. 이전 v1.6.0 상태는 `1.x` 브랜치 보존; 2.0 은 `main`. Constellation 은 공개 EstreGenesis Claude 플러그인으로 성숙해 감.
- **v2.1.0** (2026-05-28) — **Constellation 런타임 증류 + 자족화.** 라이브보드 런타임을 공개 `.eux` 로 증류해 비공개 레퍼런스 런타임 접근 없이도 호환 보드를 구현 가능: `constellation/server.eux` (deps-0 HTTP + WS 라우터 — 에이전트 레지스트리, role 분류, A2A relay + reply-window 페어링, telemetry 제외, graceful main 핸드오프, 채널별 history), `local-bridge.eux` (turn 기반 에이전트용 파일-IO control/A2A 브릿지), `self-wake-watcher.eux` (무한대기 self-wake), `watchdog.eux` (server/bridge liveness 가드). `Constellation.md` 를 **자족화** — 오케스트레이션 프로토콜 + 무한대기 방법론을 본문 증류(A2A reply-envelope, telemetry 제외, 핸드셰이크 순서), 비공개 파일 인용 제거, **watch-state 불변식**(보드 합류 중엔 대화를 끝내지 않음 — 항상 watcher 재무장 후 watch 복귀) + re-arm duty + watch 규율 강화. **EstreUX brew 런타임 참조** 를 끝까지 명확화: repo URL + clone-and-run brew CLI(`node bin/estreux.mjs brew`) 를 `Constellation.md §6` 와 `constellation/README.md` 에 추가(비공개 레퍼런스 클라 인용 대체), EstreUX 한 줄 포인터를 6개 시드 티어 전체에 propagate 해 마이그레이션 에이전트가 런타임을 찾을 수 있게. EstreUX 는 참조 Apache-2.0 빌드 의존으로 유지(번들·소유 X). draft 는 EstreGenesis 에이전트가 디스크 런타임 소스에서 증류하고 upstream 라이브보드 메인이 정밀 review.
- **v2.1.1** (2026-05-28) — EstreUX brew 런타임 참조 동기화. EstreUX 가 `v0.1.0` 발행(deps-0 ~21 KB 엔진, files 화이트리스트)되어, v2.1.0 의 "clone-and-run, npm 아님" 문구를 권장 경량 **giget** fetch(`npx giget gh:SoliEstre/EstreUX/spike#v0.1.0 ./estreux-engine` → `node ./estreux-engine/expand.mjs brew <comp>.eux`)로 갱신하고 full-clone(`bin/estreux.mjs`)·npm 발행 준비본을 대안으로 명기. `Constellation.md §6`·`constellation/README.md`·시드 6종 EstreUX 포인터에 반영, Compact 티어 EstreUX 중복 표현 정리.
- **v2.2.0** (2026-05-28) — **Constellation reference 마스터 copy.** rough/detail `.eux` spec 옆에 풀세트 기준이 필요한 다운스트림용으로 `constellation/reference/` 신설: `state-schema.md` (보드 SSoT 데이터 모델 — `state.json` top-level + 작업 트랙 `current/done/planned` + 검토·결정 패널 + 자유 추가 요청 + 채널별 history + 키 + 피드백, generic-PM vs 도메인-특수 경계 명시)과 `dashboard/` (`index.html`·`style.css`·`app.js` 비공개 구현에서 일반화한 vanilla DOM 마스터 copy, schema 기준 `state.json` 렌더; copy해서 stack에 맞게 re-skin). §2 A2A 브릿지 인터페이스만 byte-identical 불변; reference copy는 구체 출발점이지 계약 아님. `gateway/`·`runtime/` reference 마스터 copy는 post-v2.2.0 patch 예정(주기적 재증류로 drift 관리). rough tier brew 결과가 ping/pong 수준 보드만 만든 다운스트림 마이그레이션 사례로부터 진단 — 갭은 "무엇을 렌더할까?"(데이터 모델)지 "어떻게 렌더할까?"(UI 디자인) 아니었음. EstreGenesis 에이전트 draft, upstream 라이브보드 메인 정밀 review.
- **v2.3.0** (2026-05-29) — **Superscalar 모듈 + 자율 실행 절대 원칙.** 핵심 원칙 2개 신규 추가 (Master #13/#14, Lite #10/#11, Compact #15/#16): (1) **실행 스케줄링 (Superscalar, 선택 참조 모듈)** — `issue_width`를 5개 차원(Anthropic effort band · pace_mode 상한 · Little's Law throughput · Kanban WIP · `autonomy_available_workers`)으로 제한, cost-benefit 게이트(~30-60k 토큰 지평 crossover) 통과 시 dispatch; (2) **자율 실행 (절대)** — 정해진-다음-단계(Phase 순서 · `planned` 트랙 · `blocked` 해제분 · in-order retire 큐)는 묻지 말고 진행, 게이트는 오직 (a) 손실 / 외부 발행, (b) 새 중대 분기 결정 시점 (RRP / 설계 — *결정 시점* 만; 그 결과로 잡힌 Phase A/B/C 계획은 *결정된 실행*), (c) 재기동 필요 deploy (적용은 자율, *재기동 타이밍* 만 조율), (d) 명시적 사용자 steering. Phase 0에 네 번째 질문 추가 (`실행 스케줄링: serial/parallel` + `speculation: off/on`); AGENTS.md template § 5 핵심 규칙 라인 1이 자율 실행 규칙으로 정합. Master는 § 실행 스케줄링 신규 섹션 추가하여 `Superscalar.md` 참조 (§ Constellation과 동일 모듈-참조 패턴); Lite/Compact는 원칙 안 압축 참조. v2.2.x patch 시리즈를 한 anchor로 통합: **v2.2.1** (Hermes gateway reference 마스터 copy), **v2.2.2** (`constellation/reference/gateway/main` + `runtime/` references — Node deps-0 generic adapter), **v2.2.3** (upstream 라이브보드 Report 2 fixes — A2A classifier · `AgentHello` shape literal · cursor unit drift), **v2.2.4** (Report 2 follow-up — server-stamped truth · leniency-WARN · role hint vs truth · runtime liveness). Superscalar Stage 1 dogfood baseline (`Superscalar.md §11` Entry 01-03, Phase C reference 작업) 안정 ship; `autonomy_available_workers` 차원과 Andon 워커-오토모드 precheck는 dogfood telemetry(Haiku-tier 워커가 자율 lane에 부적합 — 매 dispatch마다 권한 창이 throughput 무너뜨림)로부터 추가. EstreGenesis 에이전트가 draft하고 upstream 라이브보드 메인 협업 review (Delegate seq 119 dogfood telemetry + seq 123 절대 원칙 박기).
- **v2.5.1** (2026-06-01) — **Constellation 트랙-2 reference 정합 (11개 패턴): `Constellation.md` §13.11 envelope + §13.16.9 라우팅 결정 트리 + §13.16.11 KEY-MGMT 보강 + 신규 `constellation/reference/docs/dashboard-render-patterns.md` reference doc.** Constellation patch — 2026-05~06 dogfood 기간 upstream main 커밋들에서 surfaced된 11개 dashboard-render 인접 패턴 트랙-2 정합 (5개 기존 surfaced 패턴 + 단일 multi-fix 커밋의 6개 신규 패턴). Frontmatter `version: v2.3.10 → v2.3.11`. **Tier-1 protocol body 보강** (`Constellation.md`): (a) **§13.11.3 신규 sub-section — envelope normalization and client-local render**: envelope 레이어의 server-/receiver-side 정규화 규칙 3개 (독립 failure mode, 같은 envelope 레이어 composition) — rule 1 **채널 `type` 정규화** (sender가 `type` 생략 시 server가 outbound에 `type:"CUSTOM"` 스탬프 MUST; agent는 minimal `{ name, value, targetAgentId? }` carrier shape로 emit 가능 + server 정규화에 의존; 이 정규화 없으면 dashboard render가 TEXT_MESSAGE로 fall-back하여 JSON이 chat panel에 raw text로 누출); rule 2 **A2A-intent 판정 규칙** (source-set-agnostic — `source !== "user" && source !== "board" && targetAgentId && targetAgentId !== self`; CRITICALLY `source`는 `"agent"`로 fixed되지 않음 — upstream agent들은 `source = <their-agentId>` (nonstandard but conformant) emit 가능; positive-set 가정 `source === "agent"`은 upstream A2A 트래픽 전체를 silent false-negative; negation pattern은 emitter variant 전반에 portable); rule 3 **timestamp 의무 + client-local 렌더** (sender가 생략 시 server가 `msg.timestamp = Date.now()` epoch ms 스탬프 MUST; dashboard 렌더는 `new Date(epoch).toTimeString()`로 client-local tz 표시 MUST — wire-format ISO 문자열을 그대로 표시하면 sender의 tz가 노출되어 multi-tz 팀에서 혼선). Composition: rule 1+3 server-side; rule 2 receiver-side; 3개 모두 독립 failure mode. (b) **§13.16.9 라우팅 결정 트리** 추가: canonical form `(targetAgentId 보유?) → 해당 agent로 라우팅; (targetAgentId 부재 AND name ∈ board-directed) → wsToBoards 만 (wsPrimaryAgent fallback 없음); (targetAgentId 부재 AND name ∈ A2A-intent) → wsPrimaryAgent + optional board mirror; (targetAgentId 부재 AND 미등재) → fail-safe default A2A-intent`. post-v2.4.17 L867 라우팅 분기를 canonical 구현으로 reference. 트리는 §13.11.3 rule 1 (`type` 정규화 선행)과 compose; 두 단계 모두 동일한 `wsNormalize` + `wsRoute` outbound 경로의 part. (c) **§13.16.11 KEY-MGMT 신규 sub-section — kind-enum SSoT**: 모든 key-bearing envelope에 동반되는 `kind` enum (`collab` | `upstream` | `local-key`)을 codify하는 통합 key-management 계약. Closed set + fail-safe reject on unknown (§6.5 `sourceContentRef.kind` 규율과 동형 — silent misclassification 방지, 예를 들어 unknown kind를 `collab`로 coerce하면 비collab agent에 `group:collab` 누출). 4개 SSoT surface: server `keyStore`가 모든 entry에 `kind` 스탬프; `KeyList` broadcast가 entry별 `kind` 동반; `HELLO.meta`가 resolved `kind` echo로 self-classification 지원; dashboard key-management modal이 entry별 `kind`-badge 렌더. Composition: §13.16.9 routing은 key-management name을 board-directed allowlist에 포함해야; §13.14 redaction discipline — `kind` enum은 vocabulary (비민감), key 값 (`ck-…` / `uk-…` suffix)만이 환경-특수 credential. Wire shape 보존 — `kind`는 추가 top-level field로 issuance 시 server-stamp, `KeyList` entry에 mandatory, `HELLO.meta`에 optional. **Tier-2 dashboard render policy** (신규 파일 `constellation/reference/docs/dashboard-render-patterns.md`, ~3902 단어 / ~26.8 KB, 8개 section): wire 위 render-side 패턴을 다루는 companion doc — §1 introduction (scope: 2026-05~06 dogfood surfaced render-tier 패턴; `Constellation.md`의 companion); §2 A2A row interaction (3단계 discovery: glanceable summary → hover preview 4-quadrant peek panel → click pin floating panel with pin/✕/header-drag → drag relative-cursor offset; 레퍼런스 impl `wsRowHover` / `wsA2aCardEl`); §3 collapse `[hidden]` CSS 함정 + triangle indicator direction (`display: flex`가 UA 기본 `[hidden] { display: none }` 오버라이드; 명시 `.ws-a2a-body[hidden] { display: none }` fix; 삼각형 convention ▾ expanded / ◀ collapsed — collapsed indicator는 좌향, 의미는 "content is on the left of this button"이지 tree-view의 ▸가 아님; toggle button 배치는 row 오른쪽 끝 + row click handler의 descendant가 아닌 sibling 관계로 propagation 충돌 회피); §4 board render card form (A2A-intent는 아이콘 + envelope summary + collapsible JSON body; board-directed는 per-name UX surface — Status는 system-notice strip, Typing은 input-area chip, ConnectionRestored는 banner, Heartbeat는 liveness indicator 전용; raw TEXT fallback은 type 누락 시 = §13.11.3 rule 1 server-side 결함의 symptom); §5 date-line divider (자정 경계에서 chronological stream에 divider 삽입; `position: sticky; top: <header-height>`로 최근 날짜 라벨이 스크롤 중 유지; `오늘 / 어제 / YYYY-MM-DD` 텍스트 per locale + `~ ~` decorator; sticky offset은 page header 만이 아니라 chat-panel header 높이 반영 필요); §6 A2A attachment chip (mime-icon emoji glyph + filename + size + preview/download 트리거, card body 안에 칩으로 렌더 — 별도 row가 아니라 attachment-message 연관성 시각화; 칩 패턴은 `attachments[]` / `files[]` / `media[]` field-name 선택과 무관하게 portable); §7 closed-session connection indicator (녹색 점 `●` connected vs 회색 점 `●` history-only — 끊긴 agent row는 roster에 유지, 점만 update; tooltip은 정확한 연결 상태 + local-tz timestamp 표시; §13.13 `delivered` ack escalation path와 pair); §8 upstream A2A general-tab 판정 fix (§13.11.3 rule 2 정합 source-set-agnostic 판정 규칙 — 기존 `source === "agent"` positive-set check가 upstream A2A 트래픽 전체를 silent false-negative하여 operator가 "upstream A2A invisible on dashboard"로 보고하던 결함 해소; §13.11.3 rule 2의 receiver-side 절반). Implementation references appendix가 패턴별 upstream commit SHA를 매핑 (코드 인용 없이 SHA + filename + function name) — adopter 추적성. Cross-reference: `Constellation.md` §13.11 + §13.16.9 + §13.16.11 + `constellation/reference/state-schema.md`. **Scope 규율**: Constellation.md 본문 + 신규 dashboard-render-patterns reference doc. 시드 lib 무변경 (시드는 §13.x를 본문이 아닌 anchor reference — cascade 없음). 프로토콜 wire 무변경 — §13.11 envelope shape 보존, §13.11.3은 server-side 스탬프 규율 + receiver-side 판정 규칙을 codify하나 field semantics 변경 없음; 신규 `kind` enum field는 additive + back-compatible. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.5.0** (2026-06-01) — **Superscalar.md v0.3 — §2 lane-class-aware `issue_width` cap 분할 (read vs write) + §2 policy cap vs runtime concurrency ceiling sub-section + §5 lane-class-차등 adoption threshold + §11 Entry 05 (downstream Stage 1 dogfooding n=7).** Minor 릴리스 — Superscalar.md 정책-doc body 변경 + Compact + Master 시드 cascade. 다운스트림 적용 프로젝트의 Stage 1 dogfooding bundle 반영 (실시간 채팅 서비스 마이크로서비스, Superscalar + Constellation 포함 시드 전면 채택, Burst pace mode `issue_width` cap 6, ~3일간 n=7 retired lane, case-based ledger) — 제안서는 `_proposals/005_2026-06-01_superscalar-dogfooding-stage1/`. **§2 cap 분할**: 단일 `issue_width` 공식이 종전에 모든 lane class를 균일하게 커버 → v0.3 에서 `issue_width_write = min(effort_band, pace_mode_cap, Little's-Law, Kanban-WIP, autonomy_available_workers)`와 `issue_width_read = min(effort_band, runtime_concurrency_ceiling, autonomy_available_workers)`로 분할. read class에서 제외한 두 항(Little's Law, Kanban WIP)은 retire-merge 경합만 모델링 — read-only lane은 structurally 그 경합 surface가 없음 (store buffer 없음, retire-merge ordering 없음, WAW hazard 없음, synthesis에서 disposable). Read-only / analysis lane (subagent context sweep, workflow read fan-out, telemetry read)은 비가역 부수효과 없고 §3 irreversibility barrier의 read/write 경계 (read/analyze = default-allowed; write/deploy/send = retire-gated)를 상속. **§2 신규 sub-section — policy cap vs runtime concurrency ceiling**: `issue_width_*` cap은 정책 bound; runtime concurrency ceiling (예: workflow의 `min(16, cores − 2)` per workflow; agent-tool fan-out 의 harness 최대 parallel tool calls)은 별개의 물리 bound. 관계: `effective_concurrent_lanes = min(policy_cap, runtime_ceiling)`. workflow 기반 read fan-out 에선 runtime ceiling 이 보통 dominate; agent-tool 기반 fan-out 에선 policy cap 이 보통 dominate. `issue_width_write`는 hard policy bound (runtime headroom 무관하게 초과 = 정책 위반 — retire-merge 경합이 동시 write 수에 비례); `issue_width_read`는 위의 runtime ceiling 에 종속되는 soft preference (`issue_width_read` 초과해도 `runtime_concurrency_ceiling` 이하이면 정책 위반 아님, 최대한 calibration signal). **§5 adoption-threshold lane-class 차등**: threshold 값 자체는 종전과 동일; 새로운 점은 각 threshold가 어느 lane class에 binding하는지. `merge-conflict rate > 15% → issue_width_write -= 2`는 write lane 만 (read-only lane은 merge surface 없음, conflict = NA). `speculative accuracy < 60% → spec off` · `concurrent token cost vs in-order > 3× → cap 재조정` · `FM-1.3 detection ≥ 1/session → 가드 강화`는 대칭 (cross-cutting concern, retire-merge 경합 아님). **§11 Entry 05 추가**: 다운스트림 Stage 1 dogfooding n=7, ~3일. Case mix = 1 board-worker A2A · 5 read-only research subagent · 1 workflow fan-out (case 7 = 7 reader lane, 정책 cap 6 을 downside 0 으로 초과 — §2 cap 분할의 실증 앵커). 확인: latency-hiding 명제 · speculation off-by-default 0/7 · MAST FM-1.3 = 0 · in-order retire 유지. §5 readout: n=7 에서 전부 green (case 7 의 cap 초과는 read-only lane, conflict = NA, merge-conflict signal 에 안 잡힘 — §5 lane-class 차등의 실증 확인). 미exercise 표면: worktree-isolation 0/7 · speculation 0/7 · wide write fan-out 0. Entry 04 와의 관계: 보완적, 중복 아님 (Entry 04 = cap 미만 고정 width 4 의 in-lane 깊이 변화; Entry 05 = read class 의 cap 초과 cross-lane fan-out). Outcome: P1 (lane-class cap 비대칭, actionable) + R1 (cap 분할 §2/§5 채택) + R2 (policy/runtime 관계 §2 명료화) 모두 upstream 반영. **Seed cascade**: Master EN/KO § 실행 스케줄링 — `### 불변 — issue_width 공식` body가 lane-class-aware 분할 + policy/runtime ceiling note 로 갱신 (Master는 anchor가 아닌 공식 body 자체를 embed); Compact EN/KO Principle #15 inline 공식이 lane-class form + §5 binding note 로 확장; Lite EN/KO 무변경 (Lite tier 는 `Superscalar.md` 를 anchor reference, 공식 body 미embed). Bootstrap Phase 0 boilerplate 무변경 (lane class 는 내부 메커닉; 사용자 표면의 `serial` vs `parallel` 선택은 영향 없음). **Frontmatter**: Superscalar.md frontmatter `version: v0.3.0` (신규 추가), 제목 "design draft v0.2" → "design draft v0.3", status 라인에 "lane-class-aware (v0.3 read/write cap split)" + `§11 Entry 01-05` (01-03 에서) 추가. **Scope**: minor — Superscalar.md body 변경 + Master/Compact 시드 cascade. Constellation.md 무변경. Wire 무변경. `feedback_release_versioning_cadence.md` 정합 — 의미있는 정책-doc body 변경 + 시드 cascade는 minor cut, patch 아님.
- **v2.4.20** (2026-06-01) — **Hermes Dev Agent 핸드오프 번들 통합 (10 files, 239,654 bytes).** Handoff-archive-only patch. Constellation.md 본문 unchanged; seed lib unchanged; 프로토콜 wire unchanged; canonical `constellation/gateway-client.eux` + `constellation/reference/gateway/hermes/{README.md,ws_agent_client.py}` unchanged. 핸드오프 번들은 EG 커밋 `51a93826f8d3` (v2.4.14 태그) 기준으로 작성되어 프로젝트 소유자 지시에 따라 EG 트리에 통합. **통합 결과**: (a) 신규 디렉터리 `constellation/reference/docs/2026-06-01_hermes/` 에 `GEMINI-OUTPUTS-REVIEW.md` (12,794 bytes; 이전 Gemini 작성 초안에 대한 3축 리뷰 — 7개 critical findings C1-C7 + 7개 high findings H1-H7 — `HELLO_ACK` SSoT 부재, 인증 파라미터 혼동, 기본 role `worker` 비스펙, AG-UI phase event 불일치, envelope 필드 누락, delivery semantics 충돌, redaction 요구 오독), `UPSTREAM-AGENT-REFLECTION.md` (5,231 bytes; upstream-peer 10개 불변식), `AUTONOMOUS-AGENT-ONBOARDING-GUIDE.md` (5,497 bytes; API/CLI/upstream agent 온보딩 가이드), `HANDOFF-MANIFEST.json` (각 핸드오프 파일 sha256 provenance; §13.14 정합 `root` 필드를 `<hermes-workspace>` placeholder로 redact), `README.md` (핸드오프 context + 파일 인덱스 + 통합 결과 테이블); (b) `constellation/reference/gateway/hermes/_hermes-handoff-20260601/` 서브폴더에 side-by-side 스냅샷 — Hermes-side `gateway-client.eux` (7,516 bytes; `2.3.7-upstream-ref` 증류본, `SERVER_HELLO` 기반 핸드셰이크 + `at-most-once` delivery + ack-layer skeleton, EG 기존 5,740 bytes canonical과 구조적 차이 — Hermes 리뷰가 `HELLO_ACK`/`worker`-role/single-event 결함을 지적; promotion 결정은 별도 follow-up), Hermes-side `README.md` (5,927 bytes) + `ws_agent_client.py` (18,544 bytes; 정정 프로토콜 stance에 따른 Python 어댑터 재작성), 신규 `constellation-hermes-ref-summary.md` (2,780 bytes), 신규 `constellation-hermes-gateway-ref.zip` (8,700 bytes; 바이너리 번들 보존). (c) Hermes-side `reference/estregenesis/Constellation.md` (170,957 bytes / 900 lines / v2.3.7)는 직접 통합에서 **유보** — gitignored `assets/handoffs/2026-06-01_hermes/` archive 내 review-only artifact 로 보관; EG-current Constellation.md (188,287 bytes / 981 lines / v2.3.10) authoritative. 섹션 헤더 구조는 정합 (동일 §1-§7, 동일 §13.9-§13.21); EG-current 가 §13.16 에 v2.4.15-v2.4.19 추가 codification 보유 (element 2 turn-end mandatory rearm + §13.16.8 A2A communication priority + §13.16.9 board-directed vs A2A-intent 분류 + §13.16.10 pre-send inbound check) — Hermes-side 스냅샷은 이를 predates. Diff 요약 outbox Report 에. **Redaction grep**: 1 replacement (Hermes local-workspace path 토큰 `/run/media/esterisk/222CD85A2CD82B15/HermesAgentWorkspace/Project/constellation` 가 `GEMINI-OUTPUTS-REVIEW.md` line 11 + `HANDOFF-MANIFEST.json` `root` 필드에 출현, 둘 다 §13.14 정합 `<hermes-workspace>` placeholder 로 redact); 추가 신규 추가 트리 grep clean 확인. **Scope discipline**: 핸드오프-archive 통합 한정 — Constellation.md 본문 unchanged, seed lib unchanged, 프로토콜 wire unchanged, canonical gateway-client + hermes reference 파일 unchanged. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.19** (2026-06-01) — **A2A PR System RRP v0.2 → v0.3 DRAFT: dogfood PR #1 e2e 완주에서 도출된 7개 학습 + §10A dogfood-1 case study + AC-dogfood-6 달성 (integration-branch commit `140fd64`).** RRP-only patch — `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` 본문 보강. Constellation.md 본문 unchanged; seed lib unchanged; 프로토콜 wire unchanged. v0.3 패치는 첫 Level 1 protocol-mediated PR e2e 완주 과정에서 surfaced된 7개 RRP-body 학습을 incorporate: **(1) `sourceContentRef.kind` enum 정식화 (§4.1.1)** — 세 closed value (`branch-ref` / `inline-files-diff` / `inline-section-mirror`) + unknown kind에 대한 fail-safe reject (receiving bridge가 fall-through 대신 reject — "fallthrough이 branch-ref handler를 거쳐 wholesale repo overwrite를 유발"하던 near-disaster 방지). **(2) `inline-section-mirror` semantics (§6.5)** — `body` + `targetPath` + `mergeStrategy` (현재 `append-section`만 정의; 향후 enum: `prepend-section` / `replace-section` / `insert-after-anchor`); bridge invariant은 `git add <targetPath>` 격리 (절대 `-A` 아님), no branch-fetch, PR_LIVE 단계 existence check, dry-run `abs+exists` log를 강제. **(3) `targetPath` base 규약 (§6.6)** — receiving bridge의 `HUB_REPO_DIR` 기준 repo-relative (절대경로 아님 / bridge-cwd 기준 아님 / sub-tree 기준 아님); trusted-mirror PR의 경우 sender가 receiver의 file layout 지식을 보유. **(4) `already-in-target` cherry-pick skip 규율 (§9.3)** — Level 1 merge handler는 cherry-pick 직전 `git merge-base --is-ancestor <draftRef> <targetBranch>`를 check; true이면 cherry-pick 건너뛰고 바로 `git push`로 진행 (`applyDiffAndCommit`이 이미 local target branch에 `draftRef`를 commit한 케이스에서 duplicate-commit error 방지). **(5) Dry-run `abs+exists` 검증 log (§10.6)** — dry-run 모드는 `abs=<resolved-absolute-path> exists=<bool>`를 log해야 함 — path-resolution 결함이 PR_LIVE 단계 이전에 surface; "dry-run은 no-op stub이라 경로 결함을 catch할 수 없다"는 gap을 closes. **(6) `trusted_mirrors.targetRepo` alias resolve order (§7.ε)** — receiving bridge는 기본 `targetRepo` alias로 `"hub"`를 등록; resolve order는 `state.json.pr_system.target_aliases` primary → `git remote get-url origin` fallback → reject; sender는 canonical convention으로 `targetRepo: "hub"` emit (§13.14에 따라 redaction-safe by default — wire envelope에 실 production repo path 절대 미탑재). **(7) `PR_LIVE` env semantics + `dryRun: false` marker + user merge delegation 패턴 (§10.7)** — receiving bridge에서 `CONSTELLATION_PR_LIVE=1`이 real git 연산을 enable; env는 operator-managed (A2A로 toggle 불가) — autonomous-execution external-publish 게이트 보존; PR_LIVE 활성 시에만 `PRDraftReady` / `PRMergeAck`가 `dryRun: false` marker 동반; operator는 class별로 merge authority 사전 위임 가능 (예: "trusted-mirror PR은 검토 후 문제 없으면 머지+푸시") — per-PR 확인 부담 감소. **신규 §10A dogfood-1 case study sub-section**: e2e 프로토콜 경로 (`PRRequest → capability check → applyDiffAndCommit PR_LIVE 실 append + git add 격리 + commit → PRDraftReady{dryRun:false} → main 검토 (redaction grep 0건 + 경로 정합 + 중복 check) → PRReviewAck approve → PRMergeRequest (role==main self-target relay) → cherry-pick skip (draftRef 이미 main) + push origin main → PRMergeAck{pushSuccess:true, commitSha:140fd64} → PRStatusUpdate merged`); 외부 publish 이전에 gate stack이 catch한 3개 구조적 결함 (placeholder lookup 실패 / inline-section-mirror handler 누락 / targetPath base 불일치 — 각 결함의 root cause + 해결 + codification anchor 한 줄씩); AC-dogfood-6 달성 (§12.3 — 첫 Level 1 mirror PR e2e via A2A envelope only, no `gh pr create`, integration-branch commit SHA `140fd64`가 `PRMergeAck`에 보고됨); downstream Constellation adopter용 학습 (dry-run 게이트 우선 / receiving bridge의 kind enum coverage 검증 / `target_aliases` 명시 설정 / redaction grep + 경로 정합 검토는 non-optional / user merge delegation은 class별 opt-in). **Status banner 갱신**: v0.2 DRAFT (Phase 1 Level 2 shipped, Level 1 path pending) → v0.3 DRAFT (Phase 1 + Phase 1b shipped, AC-dogfood-6 achieved via PR #1). §13 phase ladder에서 Phase 1b가 DONE으로 승격. **Memory feedback 보강** (커밋 대상 아님): `feedback_pre_send_inbound_check.md`에 probe-then-inspect-or-abort 패턴 추가 — probe가 count > 0를 return하면 agent는 emit 이전에 각 meaningful inbound의 body를 inspect해야 함 + superseding vs non-superseding 분류 + incorporate-disclosure-and-proceed 또는 abort-and-revise 선택; node-script 구현 패턴 (`process.exit(2)`로 count > 0 abort) + 2026-06-01 dogfood cycle self-violation incident worked example (probe가 1개 meaningful inbound를 surface했음에도 body inspect 없이 emit; non-superseding이라 damage=0이었으나 process violation은 실재 — v2.4.19는 probe는 돌렸으나 count를 informational로 다룬 residual loophole을 closes) codify. **Scope 규율**: RRP-only patch. Constellation.md 본문 unchanged. Seed lib unchanged. Wire unchanged. Memory 미커밋. `feedback_release_versioning_cadence.md` 따라 in-flight patch 태그 cut, 누적 catch-up 아님.
- **v2.4.18** (2026-06-01) — **`Constellation.md §13.16.10` 신규 sub-section: pre-send inbound check 규율 (read-before-write, git push-before-pull 유비).** Constellation-only patch (frontmatter version v2.3.9 → v2.3.10). §13.16.9 (board-directed vs A2A-intent 분류)와 §13.17 (메인-챗 금지) 사이에 신규 §13.16.10 (~55줄) 삽입. **규칙**: 모든 outbound A2A 메시지는 agent의 last-surfaced inbox cursor에 대한 cursor-tail probe를 선행해야 함. Cursor 이후 새로운 meaningful inbound가 존재하면 agent는 그 inbound를 먼저 surface하고 (a) 새 정보를 outbound payload에 incorporate하거나 (b) 새 inbound가 planned send를 supersede한다면 outbound를 defer. Probe가 clean한 경우에만 (cursor up-to-date, 새 meaningful inbound 없음) outbound 진행. Probe는 turn당 1회가 아닌 **outbound emission 직전마다** 발화. **근거 — git push-before-pull, 그러나 더 엄격**: 분산 VCS에서 `git push` 전 `git pull`이 codify하는 read-before-write 규칙과 같은 형태. Constellation A2A는 git보다 **더 엄격**해야 함 — git은 non-fast-forward push를 transport layer에서 reject하고 operator에게 pull을 강제하나, Constellation A2A는 **in-flight rejection이 없음**: stale-context send는 그냥 server에 도착, counterpart에 relay되어 sender가 본 적 없는 fresh inbound를 overwrite하거나 contradict할 수 있음. Drift는 silent. Pre-send probe는 transport 자체가 강제하지 않는 read-before-write 규칙을 A2A 프로토콜에 명시적으로 도입. **구체적 failure mode**: counterpart가 `t=0`에 새 context를 담은 `Report X` 발신. Agent는 `t=1`에 pre-`t=0` context로 framing된 `Report Y` 큐잉. Agent가 `X`를 먼저 surface하지 않고 `Y`를 발신하면 `Y`는 stale framing으로 counterpart에 도착 — counterpart는 두 diverging thread를 reconcile해야 하고 일반적으로 follow-up correction outbound가 추가로 필요. Fix는 일방향: agent는 항상 쓰기 전에 읽음. **§13.16.10 내용** (~55줄): 규칙 statement; git push-before-pull 유비 (in-flight-rejection 비대칭 note 포함); 구체적 failure mode; §13.16.6 element 1 (turn-end cursor-tail probe) 단독으로 불충분한 이유 (turn-boundary 레이어 vs mid-turn 레이어 — disjoint coverage window); 4-step 운영 형태 (probe → surface-if-found → emit → advance-cursor); §13.16.6 / §13.16.8 / §13.16.9 / §13.21와의 composition; 금지 anti-pattern (turn-start에 "방금 확인했다" 이유로 skip, "small" outbound라서 skip, probe하고도 결과 무시). **Composition**: **§13.16.6 element 1** (turn-end cursor-tail probe — turn-boundary 레이어)을 보완 — outbound emission 직전마다 발화하는 mid-turn 레이어 추가; 두 layer는 disjoint window 커버 (turn-end는 inter-turn-gap inbound catch; pre-send는 intra-turn inbound, 어떤 두 emit 사이든, catch). **§13.16.8 A2A 우선순위**는 pre-send probe를 prerequisite work와 message emission 사이에 배치: `prereq → probe → emit`; probe는 §13.16.8이 ship하는 drift-containment 규칙 자체에 대한 guard. **§13.16.9 channel allowlist**는 probe의 meaningful-inbound 필터를 공급 — §13.16.6에서 watcher가 사용하는 A2A-intent allowlist와 동일, intent class와 무관하게 uniform 적용 (probe 비용은 negligible — `wc -l` 1회 + `grep` 1회). **§13.21 fresh-context defer**는 불변 — probe-driven 수정 또는 drop은 *current-cycle* read-before-write이지 phantom future self로의 deferral 아님; 두 규칙은 깔끔하게 compose (§13.16.10으로 now revise-or-drop; §13.21으로 never defer to fresh context). **금지 anti-pattern**: "turn-start에 inbox 확인했다"는 이유로 skip — turn당 multiple outbound 각각 fresh probe 필요, agent가 1회 probe를 multiple emission에 amortize 불가; "small" 또는 "low-priority" outbound라서 skip — skip-pattern이 규율을 erode, probe는 unconditional; probe했으나 결과 무시 — 규칙 defeat, probe 비용은 지불했으나 안전성은 폐기. **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16.x를 본문이 아닌 anchor reference). 프로토콜 wire 무변경 — §13.11 wire envelope shape 무변경; agent-side outbound 규율만 codify. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.17** (2026-06-01) — **`Constellation.md §13.16.9` 신규 sub-section: board-directed vs A2A-intent `CUSTOM` 메시지 분류 SSoT + bridge `Status` auto-send 비활성 + watcher meaningful allowlist 확장.** Constellation-only patch (frontmatter version v2.3.8 → v2.3.9) + reference runtime + reference bridge. v2.4.17은 모든 `CUSTOM` 메시지 `name`을 intent별로 분류하는 SSoT를 codify: **board-directed** (보드 대시보드 UX surface에 렌더, agent inbox로 가는 A2A wire 메시지 아님) vs **A2A-intent** (§13.x A2A relay discipline에 따라 target agent inbox로 라우팅). 분류는 두 downstream 선택을 governance: target-unspecified `CUSTOM`의 server-side relay 라우팅 (reference server의 `wsPrimaryAgent` relay 분기와 `wsToBoards` mirror policy) + §13.16.6의 watcher meaningful-filter membership. 두 surface가 종전엔 name으로부터 ad-hoc하게 intent를 추론 → §13.16.9가 allowlist를 한 번 명명하여 두 surface를 동기화. **기술 context — channel inversion**: reference server의 `target-unspecified CUSTOM → wsPrimaryAgent (main A2A inbox)` 라우팅 정책이 name별 board-directed vs A2A-intent를 구별하지 않아 board-directed 메시지 (`Status`/`Typing`/`UserPromptAccepted` 등)가 main agent A2A inbox 채널로 누출. 같은 라우팅 레이어의 `wsToBoards` always-on mirror 정책과 결합되어 A2A-intent 메시지 (`Report`/`Delegate`/`BlockerManifest`/`PR*` 등)도 board 대시보드 chat panel로 mirror되며 panel의 display layer가 board-directed text content를 기대하기에 raw JSON으로 렌더됨. 순효과: 채널 inversion — board-directed 메시지는 A2A로, A2A 메시지는 board chat에 raw JSON으로. §13.16.9가 name allowlist를 SSoT로 제공하여 server-side 라우팅 분기 + 대시보드 render-side patch를 unblock; 라우팅 fix와 render fix는 본 patch 범위 밖, upstream-main 후속. **§13.16.9 내용** (~60줄, §13.16.8과 §13.17 사이 삽입): 목적 (relay 라우팅 + watcher filter를 governance하는 intent 분류 SSoT); **board-directed allowlist (12 names)** — `Status`, `Typing`, `UserPromptAccepted`, `PersistentAdapterSmoke`, `ConnectionRestored`, `Heartbeat`, `AgentList`, `History`, `OnboardAck`, `MainChanged`, `EditMessage`, `WorkerInboxReceived`; **A2A-intent allowlist (25+ names)** — `Report`, `Delegate`, `WorkerReport`, `WorkerAck`, `AgentHello`, `Handoff`/`HandoffRequested`/`HandoffReady`, `Command`/`Priority`/`Cancel`, `UserPrompt`, `BlockerManifest`/`BlockerNudge` (§13.20 family), `PRRequest`/`PRDraftReady`/`PRReviewAck`/`PRMergeRequest`/`PRMergeAck`/`PRStatusUpdate` (§13.22 family per A2A PR RRP v0.2), `DeadlockProbe`/`ReviewSLAAck`/`PreemptRequest`/`PreemptForce`/`MediationProposal`/`MediationAck`/`EscalationRequest` (§13.19 family); **transport-tier (별도 class)** — `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate`/`Pong`/`Ping` (§13.13 ack discipline 관할) + `ServerNotice` (broadcast-class, reference server의 `wsToAll` 경로); **라우팅 implications** — `targetAgentId` 부재 `CUSTOM`은 allowlist 참조 (board-directed → `wsToBoards`만; A2A-intent → `wsPrimaryAgent` + optional board mirror; 미등재 → fail-safe default A2A-intent, silent-drop-of-A2A가 redundant board noise보다 worse failure mode); `targetAgentId` 보유 `CUSTOM`은 기존 A2A relay 경로 유지 (intent class와 무관); **watcher filter cross-link** — §13.16.6의 meaningful allowlist === §13.16.9의 A2A-intent allowlist, noise blocklist === board-directed allowlist + transport-tier; §13.16.9가 canonical SSoT; **bridge auto-send discipline** — client-side bridge는 `targetAgentId` 없이 board-directed name을 auto-emit MUST NOT (board-only display엔 silent drop으로 충분; auto-emit이 channel-inversion 결함 생성); **dashboard render policy** (informational) — always-on board mirror가 A2A-intent `CUSTOM`을 deliver할 때 대시보드는 envelope summary (`name` + `targetAgentId` + brief `re`/`subject`) + collapsible details로 렌더 SHOULD, raw JSON 아님; **scope note** — §13.11의 wire envelope shape 무변경; target-unspecified 라우팅 + watcher-filter membership 위한 `name` 필드의 해석만 codify. **§13.16.6 cross-reference 갱신**: v2.4.16 single-stage 필터 패턴의 meaningful allowlist + noise blocklist가 anchor로 §13.16.9에서 name을 read; §13.16.6의 canonical bash 필터 패턴이 full §13.16.9 A2A-intent allowlist (§13.20 + §13.22 + §13.19 family fold-in)로 regenerate. **`constellation/reference/runtime/local-bridge.cjs`** — line 173 auto-`Status` emission 비활성 (comment out), §13.16.9 bridge auto-send discipline 정합. 동일 `connect` handler의 `ServerNotice` broadcast는 유지 — §13.13 transport-tier `ServerNotice`는 broadcast-class이지 target-unspecified A2A 아님. 인라인 comment에 §13.16.9 discipline + channel-inversion 근거 명명. **`constellation/self-wake-watcher.eux` `@behavior meaningful(from)`** — 절 본문 확장: noise blocklist 열거가 §13.16.9 board-directed + transport-tier에 정합 (`MainChanged`, `History`, `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate` 추가); meaningful allowlist 열거 명시 추가 (종전엔 "blocklist에 없는 모든 것"으로 implicit); 미등재 name fail-safe default-wake 명시; §13.16.9 cross-reference로 두 enumeration의 SSoT 선언. `@intent` 라인도 §13.16.9 cross-reference 획득. **`constellation/reference/runtime/self-wake-watcher.sh` `meaningful()`** — Node 기반 필터의 `NOISE` array가 §13.16.9 board-directed + transport-tier list에 정합 (`MainChanged`, `History`, `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate` 추가); 인라인 헤더 comment에 §13.16.9 SSoT + v2.4.16 ev-agnostic 규칙 + 진단 anchor 명명; 파일 상단의 한국어 comment block도 §13.16.9 reference + operator 가시성을 위한 full A2A-intent allowlist 열거로 갱신. 스크립트는 ev-agnostic name-only single-stage 유지 (v2.4.16 stable). **Scope discipline**: Constellation-only patch + reference runtime + bridge. 시드 lib 무변경 (시드는 §13.16.x를 본문이 아닌 anchor reference). 프로토콜 wire 무변경 (target-unspecified 라우팅 + watcher-filter membership 위한 `name` 필드 해석만). Server-side 라우팅 patch (§13.16.9 allowlist 기반 target-unspecified `CUSTOM` 분기)와 대시보드 render patch (A2A-intent board mirror의 envelope-summary 렌더링)는 upstream-main 후속, 본 patch 범위 밖. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.16** (2026-06-01) — **`Constellation.md §13.16.6` watcher 필터 형태: ev-agnostic name-only single-stage (v2.4.2의 1st-stage `ev:"inbound"` gate 제거 — bridge-alive direct inbound 포맷을 놓치던 결함).** Constellation-only patch (frontmatter version v2.3.7 → v2.3.8) + reference runtime + `.eux` spec. v2.4.2가 도입한 two-stage 필터 (`grep '"ev":"inbound"'` 1st stage → name blocklist 2nd stage → meaningful name allowlist 3rd stage)의 1st stage가 **bridge-alive A2A 트래픽 전체를 silent 하게 drop** — `inbox.log`가 두 가지 envelope 포맷을 담는데 bridge-alive direct inbound 포맷에는 `ev` 필드가 없기 때문. **두 포맷**: (a) **legacy event-relay** `{"t":..., "ev":"inbound", "msg":{"name":...}}` — bridge 프로세스가 자기 라이프사이클 이벤트 (`closed` / `ws-error` / `inbound` 로그) 기록 시 쓰는 형태; (b) **direct inbound** `{"at":..., "name":..., "value":..., "source":"agent"}` — bridge가 server 로부터 실제 A2A envelope 를 deliver 할 때 쓰는 형태. v2.4.2 1st-stage `grep '"ev":"inbound"'`은 (a)에 대해 정확했으나 (b)에 대해 hard fail — 포맷 (b)의 모든 line이 name stages 도달 전에 걸러짐. **진단 evidence**: watcher `bv0u5h95p` (armed 02:19:31Z, base=518) + `bd7k7xoy3` (armed 02:32:49Z, base=518) 모두 REARM ~45min / "no meaningful inbound" 종료 — inbox lines 519~522 (3 main `Report` envelope + 1 server `Ack`)가 armed window 내부에 direct 포맷으로 도착했음에도 불구하고. 두 watcher 모두 cadence 대로 정상 polling 중이었으나 필터가 actual meaningful 트래픽에 구조적으로 deaf; ev-agnostic 형태로 spawn된 fresh watcher (`bql0322pj`)는 두 포맷 모두 catch. **v2.4.16은 single-stage name-only로 collapse**: `tail … assets/collab/inbox.log | grep -vE '"name":"(History|Ack|AckProcessed|AckCumulative|AckPolicyUpdate|ServerNotice|AgentList|Status|ConnectionRestored|Heartbeat|Typing|UserPromptAccepted|PersistentAdapterSmoke|OnboardAck|WorkerInboxReceived|EditMessage|MainChanged)"' | grep -cE '"name":"(Delegate|UserPrompt|WorkerReport|WorkerAck|AgentHello|Handoff|Report|Command)"'`. v2.4.2의 의도 (self-emission echo + transport noise 차단)는 name blocklist 만으로 **완전히** 보존됨 — blocked name 들은 legacy event-relay이든 direct envelope이든 동일하므로 blocklist는 format-agnostic, ev-stage는 legacy 포맷에서 redundant + direct 포맷에서 hard fail. v2.4.2의 두 false-positive class 분석 (self-emission #1, server-replay/History #2)은 옳고 여전히 유효; ev-stage gate 만 retire. **변경 파일**: `Constellation.md §13.16.6` Filter-discipline 단락 재작성 (ev-stage 제거; "Why ev was dropped" 신규 단락 — 두 포맷 + 진단 anchor 문서화) + frontmatter v2.3.7 → v2.3.8; `constellation/self-wake-watcher.eux` `@behavior meaningful(from)` 절이 ev-stage를 명시적으로 disclaim (재도입 방지); `constellation/reference/runtime/self-wake-watcher.sh` `meaningful()` Node 기반 blocklist 상단에 v2.4.16 ev-agnostic 규칙 + 두 포맷 + 진단 anchor 를 명명하는 인라인 헤더 코멘트 추가. **Scope discipline**: Constellation-only patch + reference runtime + `.eux` spec. 시드 lib 무변경 (시드는 §13.16.6을 본문이 아닌 anchor reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.15** (2026-06-01) — **A2A PR System RRP v0.1 → v0.2 DRAFT: Level 1 + Level 2 추상화 + β′ topology + Q1/Q2/Q3 메인 upstream Phase 1 ship `2213a14` 기준 해소.** RRP-only patch — `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` 전반 개정; RRP 파일 외 변경은 본 CHANGELOG 항목뿐. **2-level 추상화 (신규 core concept, §2.4)**: 단일 wire 어휘 (§4) + state machine (§9)가 두 가지 물리 PR 실현을 커버. **Level 1 — virtual PR (A2A-envelope-only)**: source 와 target 이 같은 operator를 공유 AND 양측 모두 repo-level 파일시스템 직접 접근 가능 시 적용; 수신 측이 `sourceContentRef` 읽어 target repo의 로컬 checkout 에 변경 직접 적용 → commit → push; `gh pr create` 완전 skip; 리뷰는 A2A wire 상 in-band; MERGED ≡ integration branch에 commit push 완료. **Level 2 — Github PR (provider-mediated)**: 기존 Phase 1 ship `2213a14` 형태; `handlePrRequest`가 target repo 대상 `gh pr create` 호출; PR이 실제 provider object 로 존재; 리뷰/머지는 provider 경로, A2A 봉투는 상태 mirror. **Selection rule**: `category == "trusted_mirror"` AND `operator-shares-target` AND `target-repo-direct-access` → Level 1 기본; 그 외 Level 2. `PRRequest.level` payload field 로 전달 (Phase 1 ship 호환 위해 default `2`). **타입별 의미 분기**: `PRDraftReady.draftRef` = commit-sha (Level 1) vs pr-number (Level 2); `PRMergeRequest` = integration branch에 commit (Level 1) vs `gh pr merge` (Level 2); `PRMergeAck` = commit SHA + push 성공 (Level 1) vs provider 머지 상태 (Level 2); `PRStatusUpdate.state` 는 level-discriminated. **β′ content sourcing topology 채택 (§7.β′)**: v0.1의 "β + α fallback" 권고를 대체. β′ = local-bridge가 source content를 source repo 가 아닌 **target** repo의 `mirror/...` branch 로 직접 push 후 intra-target PR open — β 고유의 cross-repo provider auth 복잡성 회피. v0.2 권고: Level 2 β′ primary; α 는 small-diff (~50KB) fallback; β, γ 는 Phase 1 / Phase 1b 범위 미사용; Level 1 은 content sourcing 이 implicit (mirror branch 불요). **우선 open question Q1/Q2/Q3 메인 Phase 1 ship `2213a14` 기준 해소** (§11): **Q1 (topology)** — local-bridge `cmd:createPR(spec)` 채택 (topology b); local-bridge가 operator IDE 프로세스에서 동작하며 operator `gh` CLI auth 재사용, Phase 1 / Phase 1b 범위에서 별도 bot identity / credential store 불요. **Q2 (content-sourcing)** — β′ 채택; 근거: cross-repo provider auth 단순화 (provider tier에서 PR이 intra-target). **Q3 (authority model)** — strict `role==main` gate 채택; server의 `wsPrMergeReject` 경로가 non-`main` 발신을 거부하고 `decisions` 패널 항목 기록; 프로토콜 authority chain 이 deterministic 하며 provider ACL drift 와 무관. **Phase 1 ship 반영**: `server.cjs` `_PR_CUSTOM` 5-type observer set + `wsPrMergeReject` + `PRStatusUpdate`; `local-bridge` `handlePrRequest` (trusted-mirror validate → target repo β′ mirror branch → `gh pr create` → `PRDraftReady`) + `handlePrMergeRequest` (`gh pr merge` → `PRMergeAck`); `state.json.pr_system.trusted_mirrors` 레지스트리 (§13.14/§13.15 pair). Dry-run default: `CONSTELLATION_PR_LIVE` env knob OFF (live PR creation opt-in — autonomous-execution external-publish gate 의 env-tier 인스턴스화). **Phase 1b 신규 sub-phase (§13)**: `handlePrRequest` 에 Level 1 분기 추가 — `PRRequest.level == 1` 일 때 `gh pr create` skip, source diff 를 target repo 로컬 checkout 에 적용, commit 스테이지, `PRDraftReady` 에 `draftRef: <commit-sha>` 담아 발신; `handlePrMergeRequest` Level 1 분기는 staged commit을 integration branch 로 push 하고 `PRMergeAck` 에 integration-branch commit SHA + push 성공 보고; server 는 `PRRequest.level` 필드 + `level == 1` 시 수신 측 capability matrix 검증. **Migration path 정정**: phase 0 RRP review → phase 1 Level 2 prototype (DONE — `2213a14`) → phase 1b Level 1 path (PENDING; §10 §13.14/§13.15 mirror PR dogfood 를 Level 1 PR 로 재실행; AC-dogfood-6 pass 가 exit) → phase 2 전용 PR-bot 에이전트 (옵션, topology a) → phase 3 full §13.20 통합. **승인 기준 (§12)**: AC-dogfood-6 신규 — 첫 Level 1 mirror PR (EG → hub `WS-PROTOCOL.md` §13.14/§13.15 mirror)이 A2A 봉투만으로 end-to-end 성공; 수신 측이 변경을 직접 적용 → commit → integration branch 로 push → `PRMergeAck` 에 integration-branch commit SHA 보고; `gh pr create` 미관여. **Status banner**: `v0.1 DRAFT` → `v0.2 DRAFT (Phase 1 Level 2 shipped, Level 1 path pending)`. 비우선 Q4-Q8 open 유지. **Scope discipline**: RRP-only patch — Constellation.md body 무변경 (§13.22 번호 부여 + 본문 통합은 joint-formalization 이후); 시드 lib 무변경; 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.14** (2026-06-01) — **`Constellation.md §13.16.6` element 2 timing을 turn-end mandatory rearm으로 복원 (v2.4.9 + v2.4.12 next-cycle-start 배치 롤백).** Constellation-only patch (frontmatter version v2.3.6 → v2.3.7). v2.4.9가 처음 element 2 (unconditional watcher rearm)를 current turn-end에서 next-cycle start로 이동시켰고, v2.4.11이 turn-end로 되돌렸으며, v2.4.12가 다시 next-cycle-start 형태를 재적용. **v2.4.14는 규율을 turn-end mandatory rearm으로 확정.** **기술 근거**: next-cycle-start 배치는 inter-cycle gap (current turn-end → next user-input 도착)을 watcher 없는 상태로 둠 — next-cycle start에 spawn하도록 예약된 watcher는 그 gap 동안 아직 살아있지 않으므로, gap 중에 도착하는 inbound는 wake할 watcher 없이 그대로 떨어짐. Watcher는 current turn-end에 무장되어 inter-cycle gap 내내 살아있으면서 next user-input 이전에 도착하는 inbound를 catch해야 함. gap 중 도착하는 main 응답이 바로 본 section이 방지하고자 하는 silent-stall failure mode. **§13.21 composition 명료화**: rearm step을 next-cycle start로 deferring하는 것은 현재 cycle이 진 빚을 cross-cycle 미루기 — §13.21이 금지하는 바로 그 pattern. v2.4.12의 "structural correct location" 해석은 structural location이 실제로 어디인지를 놓침: inter-cycle inbound를 catch하는 watcher의 structural location은 inter-cycle gap이 시작되기 BEFORE, 즉 current turn-end이지 gap 이후가 아님. **Cost-asymmetry 논거 복원**: v2.4.9 / v2.4.12가 회피하려 했던 concurrent-watcher-overlap은 cost-asymmetry 하에 무해 — FP cost (one task slot + log line 약간) trivial; FN cost (watcher 없는 inter-cycle gap + 외부에서 surface될 때까지 수시간 missed inbound) 치명적. Turn-end rearm은 매 턴 trivial FP cost를 지불하여 치명적 FN cost를 회피. **`Constellation.md §13.16.6` edits**: heading을 "Turn-end mandatory rearm — the discipline that prevents the multi-cycle failure mode"로 복원; element 2 본문을 "At the close of EVERY user-facing response (just before emitting the final text)..."로 복원; "Element 2 timing — relocated from turn-end to next-cycle start" 단락 제거; 6-element checklist의 cadence-interaction annotation `[executed at the start of the next cycle, not at turn-end]` 제거; 4-step list (a) tail → (b) surface → (c) advance → (d) THEN unconditional rearm를 모두-turn-end 형태로 복원 (cycle annotation 없음); "Wrong order — anti-pattern" + "Composition with the other four elements" 단락을 turn-end 형태로 복원; v2.4.12 말미 단락 (next-cycle-start-는-deferral-아님 clarifier) 제거. **§13.16.6 말미 신규 단락** — 추가 flip-flop 방지 lock-in: timing이 turn-end mandatory rearm으로 확정됐음을 명시, inter-cycle gap을 기술 근거로 명명, §13.21 composition 정정 명시 (next-cycle-start는 cross-cycle deferral), cost-asymmetry 논거 복원 (concurrent-watcher-overlap 무해; deferred rearm + missed inbound 치명적), 향후 rearm을 turn-end에서 다른 곳으로 옮기려는 제안은 inter-cycle gap이 어떻게 다르게 커버되는지 먼저 입증할 것을 요구. §13.16.8 cross-reference도 청소 (v2.4.9 next-cycle-start 괄호 제거). Frontmatter version v2.3.6 → v2.3.7. **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16.6을 본문이 아닌 anchor reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.12** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering이 next-cycle-start rearm 형태로 복원 (v2.4.9 재적용; v2.4.11 revert 정정).** Constellation-only patch (frontmatter version v2.3.5 → v2.3.6). v2.4.11은 v2.4.9의 element-ordering 정정 (element 2 rearm: next-cycle start → current turn-end)을 §13.21 (defer-to-next-cycle = anti-pattern) 및 #442 (planned-queue scan / no-idle-deferral) 위반 사유로 되돌렸음. **그 revert 사유는 §13.21에 대한 오독**: §13.21은 commit된 planned work를 가상의 fresh-context cycle로 defer하는 것을 금지 — 현재 cycle이 자기 작업을 "next fresh context가 처리할 것"으로 말없이 re-route하는 excuse pattern. Rearm은 그 pattern이 아님: rearm은 다음 user-facing cycle의 첫 action으로, cycle 시작 즉시 동일한 연속 agent process가 실행. 한 cycle boundary를 가로질러 step을 그 structural correct location에 배치하는 것은 structural placement이지 deferral-as-excuse가 아님. **v2.4.12는 v2.4.9 형태 복원**: element 2 (unconditional watcher rearm)는 cycle N의 turn-end 종료가 아닌 다음 user-facing cycle (cycle N+1)의 첫 action으로 실행. Elements 1 (cursor-tail probe)과 3-6 (board-state 갱신, BOARD_STALE handoff, planned-queue scan, pre-large-work materialization)은 current turn-end 실행 유지. Mandatory-order 제약 (element 1 strictly before element 2)은 그대로 유지되며 cycle-N → cycle-N+1 turn boundary를 가로지름; §13.21이 금지하는 것 — 다음 cycle 시작에서 element 2를 빠뜨리는 것 — 은 본 section이 계속 차단하는 별도의 failure mode. **두 가지 기술 근거 재확인**: (a) **concurrent-watcher-overlap 회피** — cycle N turn-end에서의 same-turn rearm은 거의 동일한 `base` cursor에 무장된 두 watcher가 같은 forward window를 커버하는 상태를 생성, 공유 base에서 추가 coverage 없음 (둘 다 base 이하 line의 armed-window blind spot 공유); next-cycle-start 배치는 boundary 전반에 걸쳐 정확히 하나의 watcher만 armed (직전 watcher는 cycle N+1 start 시점에 이미 WAKE했거나 REARM 시간 초과로 종료, 따라서 next-cycle-start spawn은 overlap이 아닌 replacement). (b) **깔끔한 base-cursor 의미론** — cycle-N+1-start rearm의 `base = cursor-after-latest-processed-inbound`, cycle-N+1-start re-probe가 surface한 inbound를 by construction 포함; cycle N의 same-turn rearm은 probe-time과 final-text-emission 사이에 도착하는 inbound를 여전히 놓침. **`Constellation.md §13.16.6` edits**: heading을 "Unconditional rearm — the discipline that prevents the multi-cycle failure mode"로 복원; element 2 본문을 next-cycle-start 실행 시점 설명으로 복원; 6-element checklist의 cadence-interaction `[executed at the start of the next cycle, not at turn-end]` annotation 복원; "Element 2 timing — relocated from turn-end to next-cycle start" 단락 복원 (두 가지 근거 포함); "Mandatory ordering" 단락의 4-step list에 `[at turn-end of cycle N]` / `[at next-cycle start of cycle N+1, as the first action]` annotation 복원; "Wrong order — anti-pattern" 단락이 cross-boundary ordering으로 갱신 (chat quotation 직전 sanitization 유지 — verbatim quote 재도입 없음); "Composition with the other four elements" 단락이 cross-boundary ordering을 반영. **말미 단락 교체 — §13.21 composition 명료화**: §13.21 + #442 cross-link, "rearm은 current-cycle work, next-cycle 아님" 주장을 담은 v2.4.11의 말미 단락이, "step을 structural correct location인 next-cycle start로 schedule하는 것은 deferral이 아님; §13.21은 deferral-as-excuse (가상의 fresh-context cycle로의 silent re-route)를 금지, structurally correct cycle boundary에서의 step placement는 아님"을 명시하는 신규 말미 단락으로 교체. 두 composition은 함께 성립: §13.16.6은 element 2를 next-cycle start에 배치; §13.21은 cycle이 도래한 후 agent가 그것을 default하는 것을 금지. Frontmatter version v2.3.5 → v2.3.6. **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16.6을 본문이 아닌 anchor reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.11** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering 되돌림: watcher rearm (element 2)이 current turn-end로 복귀 (v2.4.9 timing 변경 롤백).** Constellation-only patch (frontmatter version v2.3.4 → v2.3.5). v2.4.9가 element 2 (unconditional watcher rearm)를 current turn-end에서 next-cycle start로 이동시킨 timing 변경을 concurrent-watcher-overlap 회피 + 깔끔한 base-cursor 논거로 ship했음. **v2.4.11에서 그 timing 변경을 되돌림**: rearm은 현재 cycle의 turn-end 의무 — final response text 발신 전에 elements 1, 3-6과 함께 inline 실행. **기술 근거**: next-cycle-start timing이 §13.21 "defer-to-next-cycle = anti-pattern" 규칙을 위반 (현재 cycle이 진 빚은 inline으로 완료해야 함; context-window-pressure로 인한 compact가 next cycle을 영영 도달 못하게 만들 수 있는 상황에서 next cycle로 defer는 §13.21이 codify하는 failure mode) + #442 planned-queue-scan / no-idle-deferral 규율 위반 (rearm은 agent가 inbound-side liveness loop에 진 current-cycle 작업, future cycle용 queueable 항목 아님). Unconditional spawn discipline (commits `a7911fd`, `852a113`)을 정당화한 cost-asymmetry 논거가 여전히 유효하고 이 결정을 묶음: FP cost = one task slot + log line 약간 (harmless concurrent watcher); FN cost = deferred rearm을 next cycle이 영영 도달 못할 경우의 hours of lost inbound. Concurrent-watcher-overlap은 이 asymmetry 하에서 무해 — overlap된 base cursor의 watcher가 여럿 살아있어도 task slot과 log line 약간 낭비할 뿐이고, deferred rearm의 silent-stall 비용은 hours of lost inbound. **`Constellation.md §13.16.6` edits**: heading을 "Turn-end mandatory rearm — the discipline that prevents the multi-cycle failure mode"로 복원; element 2 본문에서 next-cycle annotation 제거하여 unconditional rearm at turn-end 복원; v2.4.9가 추가한 "Element 2 timing — relocated from turn-end to next-cycle start" 단락 완전 제거; 4-step 구체 sequence를 v2.4.9 이전 형태로 복원 ((a) inbox.log tail → (b) meaningful inbound surface → (c) cursor advance → (d) THEN unconditional rearm, 모두 turn-end, cycle annotation 없음); Mandatory ordering + Wrong-order anti-pattern 단락을 v2.4.9 이전 형태로 복원 (cross-cycle ordering remark 없음); Composition-with-the-other-four-elements 단락을 v2.4.9 이전 형태로 복원. **§13.16.6 말미 신규 단락**: §13.21 + #442로 명시적 cross-link, "rearm은 current-cycle work, next-cycle 아님" 명시 — timing 변경이 재도입되지 않도록 no-defer composition을 lock-in. 신규 단락은 prior refinement와 revert 사유 명명 (FP cost ≪ FN cost asymmetry; concurrent-watcher-overlap은 무해). **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16.6을 본문이 아닌 anchor reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.10** (2026-06-01) — **`Constellation.md §13.16.8` 신규 sub-section: A2A communication 우선순위 규율.** Constellation-only patch (frontmatter version v2.3.3 → v2.3.4). §13.16.7 (BOARD_STALE 게이트)와 §13.17 (메인-챗 금지) 사이에 신규 §13.16.8 (~50줄) 삽입 — 상태 변화 (사용자 steering / 신규 태스크 / 외부 이벤트, 예: counterpart `Delegate`/`Report`/`Status`/`BlockerNudge` 인바운드) 발생 시 작업 계획에 하나 이상의 outbound A2A 메시지가 포함되는 경우에 적용되는 규율 codify. **규칙**: 에이전트는 (1) 신규 계획의 action list를 스캔하여 outbound-A2A 항목 식별, (2) counterpart가 의존하는 prerequisite work도 함께 식별 — 참조 문서 commit-push (URL을 가리키는 `Delegate` payload는 그 URL에서 문서를 fetch할 수 있을 때까지 unusable), payload assembly, counterpart의 의사결정이 의존하는 `state.json`/라이브보드 업데이트 — 후 (3) 메시지와 prerequisite 둘 다 큐 head로 이동, 다른 planned action보다 앞; (4) 메시지가 emit된 후에만 나머지 action 진행. **두 가지 기술 근거**: (a) **drift-containment** — EG가 spec/state/doc을 counterpart에게 알리기 전에 갱신하면 counterpart는 unnotified-gap 기간 내내 stale baseline에서 동작, superseded-baseline 작업 + 후속 reconciliation cost 위험; 메시지를 먼저 보내면 drift window를 round-trip latency (transport-bounded, 일반적으로 EG 큐 depth보다 orders of magnitude 작음)로 제한, EG local-work duration이 아님. (b) **counterpart-start-time minimization** — 신규 계획이 counterpart에게 작업 시작을 의무화하는 경우 (DB 벤치마킹, reference-runtime cp, RRP review 등) cycle 완료까지의 총 wall-clock은 `message-emit-time + counterpart-work-duration`; emit-time을 앞당기면 cycle이 직접 단축되고, 메시지를 gate하지 않는 local EG action은 메시지 발신 후 counterpart 작업과 병렬 가능하지만 늦은 emit으로 잃은 head-start는 회복 불가. **대칭성**: §13.16에서 분류한 모든 역할 (`main` / `local` / `collab` / `upstream`)이 cross-agent 변화를 initiate할 때 적용 — worker→main report, main→worker delegate, EG→upstream report, upstream→downstream steering 모두 동일 규칙. **기존 §13.x와의 composition — orthogonal gate를 override하지 않음**: §13.14 redaction은 여전히 메시지 *content* gate (placeholder 치환, technical-only 표현); §13.17 메인-챗 금지는 여전히 메시지 *destination* gate (구조화 Q/A의 board-mediated 라우팅); §13.20 `BlockerManifest` emission도 자체가 A2A communication으로 동일 우선순위; prerequisite work가 자체로 destructive operation일 경우 destructive-operation 승인 게이트는 우회되지 않음. **§13.16.6 turn-end / next-cycle ritual과의 composition**: cursor-tail probe (element 1)와 next-cycle rearm (element 2, v2.4.9에서 next-cycle start로 relocate)은 자체 스케줄로 동작하며 본 규율과 독립 — 본 규율은 현재 cycle 작업 계획이 trigger한 신규 outbound 메시지를 관할, liveness-maintenance loop가 아님. Element 5 (planned-queue scan)는 자연스럽게 상호작용: scan이 다음 progressable 항목을 outbound A2A 메시지 (또는 그것의 prerequisite work)로 분류하면 본 우선순위 규칙이 head-of-queue로 confirm. **§13.18 recommend+proceed와의 composition**: 본 우선순위 규칙은 §13.18 위에 위치 — 1줄짜리 "recommend+proceed" announcement도 메인-챗 surface의 outbound 메시지이며, 그 emission이 counterpart를 unblock하는 경우 (counterpart의 `ReviewSLAAck`이 기다리던 `Status` 업데이트, downstream 에이전트가 읽을 board-side `Decision`) 동일한 drift-containment + counterpart-start-time 근거로 다른 planned action보다 우선. **본 규칙이 방지하는 failure mode**: 5-action 계획에서 action #3 = worker로 `Delegate`인데 plan-order로 실행하면 worker 시작 시간이 action 1-2의 wall-clock 비용만큼 지연되고 action 2가 worker가 읽는 spec을 건드렸다면 stale-state 작업 위험; 본 우선순위 규칙은 prerequisite-of-3 → 3 → (1, 2, 4, 5 natural order)로 재정렬, 동일한 final state에 counterpart가 가능한 가장 이른 시점에 시작하고 drift는 round-trip latency로 제한. **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16을 본문이 아닌 anchor reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.9** (2026-06-01) — **`Constellation.md §13.16.6` element-ordering 정정: watcher rearm (element 2)이 turn-end에서 next-cycle start로 이동.** Constellation-only patch (frontmatter version v2.3.2 → v2.3.3). 6-element turn-end ritual 구성은 그대로; element 2 (unconditional watcher rearm)의 실행 시점만 변경. Element 2는 이제 현재 응답의 turn-end (cycle N의 종료)가 아닌 **다음 user-facing 응답의 시작 시점 (cycle N+1의 start)에서 첫 action으로** 실행. Elements 1 (cursor-tail probe), 3 (board-state 실시간 갱신), 4 (BOARD_STALE 게이트 handoff), 5 (planned-queue scan), 6 (pre-large-work context 구체화)는 turn-end 실행 유지. Mandatory-order 제약 (element 1이 element 2보다 strictly 앞)은 그대로 유지되며 이제 cycle-N→cycle-N+1 turn boundary를 가로지름. **기술 근거**: (a) **concurrent-watcher-overlap 회피** — cycle N의 turn-end에 element 2 실행 시 직전 cycle의 watcher가 여전히 polling 중일 수 있어, 거의 동일한 `base` cursor에 무장된 두 watcher가 같은 forward window를 커버하는 상태가 생김; 동일-turn rearm은 coverage를 더하지 않으면서 (둘 다 armed-window blind spot 공유) redundant concurrent watcher만 추가. Next-cycle start로 이동하면 turn boundary 전반에 걸쳐 정확히 하나의 watcher만 armed 상태 — 직전 watcher는 cycle N+1 start 시점에 이미 WAKE했거나 REARM 시간 초과로 종료, 따라서 next-cycle-start spawn은 overlap 아닌 replacement. (b) **깔끔한 base-cursor 의미론** — cycle N의 turn-end에서 element 1이 직전 watcher `base` 이하 inbound을 포함하도록 agent cursor 전진; element 2가 같은 turn-end에 실행되면 fresh watcher의 `base`는 probe-time과 final-text-emission 사이에 도착하는 inbound를 여전히 놓침. Element 2를 next-cycle start로 이동하면 rearm `base`는 cycle-N+1-start 시점 line count — cycle-N+1-start re-probe가 surface한 inbound를 by construction 이미 포함; rearm-base 불변량이 "임의 mid-turn 시점 cursor"가 아닌 "최신 처리 inbound 이후 cursor"가 됨. **순효과**: turn boundary 전반에 걸쳐 한 번에 하나의 watcher armed 유지하면서 같은 coverage 확보, base-cursor 불변량 명료화. **변경 없음**: 나머지 turn-end ritual 요소 모두 turn-end 실행 유지; unconditional spawn discipline의 cost-asymmetry 논거 그대로; element 1이 element 2보다 strictly 앞 제약 그대로. `Constellation.md §13.16.6` edits: frontmatter version bump, "Element 2 timing — relocated from turn-end to next-cycle start" 단락 신규 추가, "Mandatory ordering" 단락의 4-step list에 `[at turn-end of cycle N]` / `[at next-cycle start of cycle N+1, as the first action]` 주석 추가, "Wrong order — anti-pattern" 단락이 relocation cross-reference, "Composition with the other four elements" 단락 정정. **Scope discipline**: Constellation-only patch. 시드 lib 무변경 (시드는 §13.16.6을 본문이 아닌 anchor로 reference). 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 — in-flight patch tag cut, catch-up 누적 아님.
- **v2.4.6** (2026-06-01) — **DB P2 Mode B 진입 docs publish.** `constellation/reference/docs/` 신규 doc 2종: (1) **`DB-P2-Benchmark-Framework.md`** (~36.6KB · 8 sections) — purpose + 메트릭 카탈로그 (correctness/performance/operational/robustness 버킷) + sample sizing (최소: 10k events + 7일 · 권장: 100k events + 30일 + stability gate) + 비교 방법론 (sequential-not-parallel cost · disk totals · same-crash recovery · production read mix · hrtime clock) + 데이터 수집 메커니즘 (server-side telemetry hook + `benchmarkActive` 플래그 + scheduled `exportJsonl()` diff + dashboard tab spec) + 승격 결정 프레임워크 (quantitative + qualitative gate + `promotion-decisions/` 아티팩트) + 롤백 리허설 프로토콜 + open questions × 5. (2) **`DB-P2-Mode-B-Entry-Runbook.md`** (~26.4KB · 8 sections) — pre-flight 6단계 mechanization (DB-P2-Guide §6.2 정합: backend probe verify · 디스크 공간 verify · JSONL 백업 verify · zero-traffic window · state.json edit+restart · post-check reconcile) + backfill expectations + Mode B 일상 모니터링 + 벤치마킹 활성화 포인터 + 롤백 절차 (Mode B → Mode A, 6단계) + 실패 시나리오 quick-ref 표 + Mode C 승격 preview + 커뮤니케이션 discipline (ServerNotice + informational BlockerManifest). 두 doc 모두 DB-P2-Guide.md (§3/§4/§5/§6/§7/§9) + `constellation/history-store.eux` (L39-215 / L286-469 / L475-567) + Constellation.md §13.20 cross-link. **효과**: Mode B 진입 step-by-step 가이드 + 벤치마킹 자료 수집 framework 동시 제공. Mode C 전환은 충분한 자료 확보 시점까지 보류 (최소: 100k events / 30일 / 8+ quantitative gate pass). Scope: docs-only patch — Constellation.md body 무변경, 시드 lib 무변경, 프로토콜 wire 무변경. `feedback_release_versioning_cadence.md` 정합 자체 태그 cut.
- **v2.4.7** (2026-06-01) — **A2A PR System RRP v0.1 draft publish.** 신규 RRP 문서 `constellation/reference/docs/2026-06-01-a2a-pr-system-rrp.md` (~55KB · 15 sections + headmatter)가 신규 Constellation 프로토콜 기능 §13.22를 draft — A2A 메시지 레이어를 통한 cross-repo Pull Request 오케스트레이션. 현재의 수동 hub-repo PR 작성 코디네이션 부담을 해소 (cross-repo spec parity 요구가 늘수록 scale 안 됨). **Wire 어휘**: 5개 신규 `CUSTOM` 타입 — `PRRequest` / `PRDraftReady` / `PRReviewAck` / `PRMergeRequest`+`PRMergeAck` / `PRStatusUpdate`. **에이전트 topology**: 3 option 검토 (전용 PR-bot 에이전트 / local-bridge `cmd:createPR(spec)` / 하이브리드) — 권장: topology (b) local-bridge v0.1 (로컬 git/gh CLI auth 재사용, 별도 credential store 불요). **승인 규율**: 3 카테고리 — `trusted_mirror` (state.json opt-in 목록) / `standard` (per-PR opt-in 기본) / `emergency` (block, 자동 절대 금지). **컨텐츠 소싱**: α (payload에 diff) / β (브랜치 ref + git API 읽기) / γ (fork 기반) — 권장: β + small diff α fallback. **권한**: §13.20 permission table 확장 — target repo `role==main`만 `PRMergeRequest`; `PRReviewAck`는 모든 에이전트; `PRRequest` 개방. **State machine**: 5-state PR 라이프사이클 (DRAFT → OPEN → IN_REVIEW → APPROVED → MERGED + side paths CLOSED / CHANGES_REQUESTED) + §13.19 데드락 통합 (리뷰 >SLA 대기 = quasi-deadlock 후보) + 라이브보드 panel ask (PR 큐 시각화). **Dogfood plan**: canonical 첫 use case = EG → `<hub-repo>` WS-PROTOCOL.md §13.14/§13.15 mirror PR — §13.14/§13.15 mirror PR 자체를 신규 시스템으로 라우팅하는 first proof-of-protocol 제안. **Open questions × 8** (3개 우선순위 flag): topology — `<hub-repo>`가 로컬 `gh` CLI identity로의 PR 수락? 컨텐츠 소싱 — direct `mirror/...` 브랜치 push 또는 fork 기반? 권한 모델 — strict `role==main` gate 또는 provider ACL 위임? **승인 기준**: spec (§13.22 본문) · impl (local-bridge `createPR` cmd) · dogfood (§13.14/§13.15 mirror PR을 프로토콜로 생성, 수동 X) · composition (§13.19 + §13.20 통합). **마이그레이션 path**: phase 0 RRP review → phase 1 prototype → phase 2 전용 PR-bot 에이전트 → phase 3 full §13.20 통합. **Redaction**: §13.14 정합 — `<hub-repo>` / `<main-prod>` placeholder 전반 사용; `SoliEstre/EstreGenesis` (공개 repo)만 직접 명명. **Scope**: RRP-only draft — review-only 아티팩트, 메인 검토 대기. v0.1 DRAFT status banner + 모든 구체적 디자인 결정에 "(open for main review)" 플래그. Constellation.md §13.22 본문 통합은 post-review patch. 시드 lib 무변경. `feedback_release_versioning_cadence.md` 정합 자체 태그 cut.
- **v2.4.5** (2026-06-01) — **§13.16.4 HELLO churn dampening SSoT 정정 + bridge HELLO{pid:process.pid} 배선.** 메인 upstream Delegate seq 165 (msgId `m-mpua03au-164`, Fix 2 ship commit `d385561`, 2026-06-01) 후속 — 메인 `server.cjs`에 §13.16.4가 +51줄 순수-additive `wsHelloFlapCheck` + `_flapHello` 링 버퍼(N=5/K=3/span=10s/cooldown=30s)로 ship되어 HELLO churn 감지 시 `ServerNotice{kind:"flap-rejected",incumbentPid:<prev>}` emit. EG 측은 메인이 요청한 두 후속을 처리: (1) **`Constellation.md §13.16.4` SSoT 정정** — heading 승격 `(adopted at upstream production; reference impl pending)` → `(adopted at upstream production · reference impl shipped d385561)`; adopted-behavior 단락을 "queued for a follow-up cp"에서 "shipped in commit d385561, 2026-06-01; constellation/reference/runtime/로의 server.cjs 참조 cp는 별도 follow-up cycle"로 재작성; cooldown bullet 명문화 (기본 30000ms = 30s — flap burst 흡수에 충분, 정상 재기동이 HELLO 인정까지 ~30s 대기); env-knobs bullet 명문화, 메인 d385561 정합 4개 이름 verbatim (`CONSTELLATION_FLAP_RING_N` 기본 5 · `CONSTELLATION_FLAP_RING_K` 기본 3 · `CONSTELLATION_FLAP_SPAN_MS` 기본 10000 · `CONSTELLATION_FLAP_COOLDOWN_MS` 기본 30000; deployment별 operator-tunable, 예: slow-restart bridge가 N=10 / span=20000 필요할 때); 이전엔 null이던 `incumbentPid` 필드를 채울 bridge HELLO{pid:process.pid} 워크스트림에 대한 1줄 postscript 추가. (2) **bridge HELLO{pid:process.pid}** — `constellation/local-bridge.eux` 3 edit: @behavior `connect` HELLO 봉투가 `{pid: process.pid, capabilities.inbound: ...}` 담도록 정합 + §13.16.4 incumbentPid 배선 inline note; @machine OPEN entry 괄호 확장하여 `HELLO carries {agentId, pid: process.pid}` 명시; 파일 말미에 `@incident bridge_pid` 신규 라인 추가, seq 165 / msgId `m-mpua03au-164` / ship 트리거 `d385561` 앵커링. `constellation/reference/runtime/local-bridge.cjs` 1 edit: `ws.onopen` 콜백 HELLO payload 객체에 `pid: process.pid` 단일 키 추가. 효과: 다음 bridge connection부터 server §13.16.4 `wsHelloFlapCheck`이 emit하는 `ServerNotice{kind:"flap-rejected"}`의 `incumbentPid`가 null 대신 실제 PID 담음 → churn event 중에 operator가 어느 bridge instance가 incumbent인지 식별 가능. **Scope discipline**: 시드 lib 무변경 (Constellation.md §13.16.4 anchor가 시드에서 본문 아닌 anchor reference이므로 cascade 없음); `feedback_release_versioning_cadence.md` 정합 Constellation-only patch tag 즉시 cut (in-flight cut, catch-up 누적 아님). **§13.20 BlockerManifest 정정**: Fix 2 후속 close → 1 active jet (review_4점 후속) + upstream-steering 4 items 잔존.
- **v2.4.4** (2026-06-01) — **`.eux` `@source` provenance 승격 + `history-store.eux` 신규 distillation (DB P2 백엔드).** 메인 upstream Delegate seq 159 (msgId `m-mpu9febn-158`, a5bc §F 매핑, 2026-06-01) 정합. 매핑표는 함수→라인 범위 — 메인 `server.cjs` (실제 799줄, 스펙 794 정정) + `history-store.cjs` (~580줄) + `examples/ws-wait.sh` (BOARD_STALE 게이트) 커버. EG 측 3가지 작업: (1) **`constellation/history-store.eux` (신규, ~19.9KB)** — DB P2 HistoryStore 백엔드 v1 distillation, 메인 upstream `a28150e` ship 정합. v1 디렉티브 11종 모두 사용 (`@component` / `@intent` / `@expansion` / `@targets` / `@state` / `@ports` / `@machine` / `@source` / `@behavior` / `@rationale` / `@incident`). 7개 명시 `@source` 라인: HistoryStore 추상 계약 (L26-34) · JsonlStore 클래스 (L39-215) · SQLITE_SCHEMA DDL (L225-238) · selectSqliteBackend probe (L246-279) · SqliteStore 클래스 (L286-469) · HistoryStoreMux 클래스 (L475-567) · createHistoryStore 진입 팩토리 (L572-577). 모드 A/B/C state-machine + 자동복귀 (drift ≥2/30분 + partial-fail ≥3/10분) + N=10 reconcile 게이트 — DB-P2-Guide.md 정합. (2) **`constellation/server.eux`** — 명시 `@source` 디렉티브 추가 (이전엔 inline `(Distilled from ...)` 만). v2.3.x+DB-P2 ship 상태 반영, seq 159 §F 라인 범위: L188-360 (KEY-MGMT) · L414-625 + L670/L674-678/L795-796 (§13.19 데드락 관찰 계층) · L159-160/L382-385/L681-690/L733-734/L736-758/L778-787 (UI6 SelectionPrompt + hard-TTL sweep) · L368-381 + L386 (DB P2 thin shim — SqliteStore/HistoryStoreMux dormant by default, 모드 A 바닥 무변경) · L247-265/L267-277/L279-300 (`wsCollabOnboardMd` / `wsCommit` / `wsSetMain` / `wsHandoffReady` / `wsHandleOrch` dispatch). 기존 inline provenance 보존 (v2.3 이전 handshake/relay 경로의 still-valid 계보 per `.eux` v1 §3.8). (3) **`constellation/self-wake-watcher.eux`** — 명시 `@source` 디렉티브 추가, seq 159 §F 정합. `examples/ws-wait.sh` 라인 범위: L31-38 (env 기본값) · L46-55 (`meaningful()` 필터) · L57-71 (BOARD_STALE 게이트, §13.16.7 / #410 / #434 amendment — probe L60-66, 비교 L64-65, WAKE L67-70) · L73-92 (poll 루프) · L93-94 (re-arm timeout). **이유**: `.eux` v1 §3.8가 inline `(Distilled from ...)` 형식에서 명시 `@source` 디렉티브 승격을 권장; 디렉티브는 라인-정밀 WARN-tier. seq 159 §F 매핑이 v2.3.x+DB-P2 ship 상태의 메인 검증 라인 범위 제공 → EG `drift-check --contract`의 spec ↔ runtime ↔ `.eux` distillation 3-way 정합 (G6 `@source` WARN 게이트) — backend/protocol 프로파일 파일 대상. **Scope discipline**: Constellation.md 무변경; 시드 lib 무변경; 프로토콜 본문 변경 없음. `feedback_release_versioning_cadence.md` 정합 — .eux-only patch가 자체 태그 cut (다음 시드-lib 릴리스에 묶지 않음). **다음 cycle (별도)**: `server.cjs` (799줄) + `history-store.cjs` (~580줄) raw `cp`를 `constellation/reference/runtime/`로 — outbox split-delivery (W20/W22 패턴, ~80KB 20+ frame) OR cp scope에 대한 사용자 steering 대기.
- **v2.4.3** (2026-05-31) — **DB P2 EG-side guide publish — `constellation/reference/docs/DB-P2-Guide.md` (33KB · 11 sections + 7 open questions).** 메인 upstream Delegate seq 144 (msgId m-mptlrmm3-143) — 메인 DB P1 ship (5cd7fca: HistoryStore + JsonlStore wrap)에서 P2 (SqliteStore via node:sqlite primary + better-sqlite3 fallback + dual-write + count 대조 + bootstrap opt-in) 진입. EG 책임 (seq 79 DB RRP 정합): discipline + gate + migration + rollback 가이드; 메인 server.cjs 구현. 가이드 구조: migration mode chain (A jsonl-only → B dual-write transitional → C db-primary terminal) · node:sqlite probe + better-sqlite3 fallback (Node 22.5+ + `--experimental-sqlite` 플래그 검증) · dual-write backfill (mode-B entry one-shot, idempotent `INSERT OR IGNORE`, batched transaction) · count 대조 gate (mismatch = mode-B→C promotion abort + diff report, auto-resolve X) · bootstrap opt-in (fresh install default mode A, operator-flipped via `state.json.modes.historyBackend`) · 실패 모드 + recovery (WAL corruption, partial dual-write, count drift, §13.20 BlockerManifest emission) · schema sketch (JSONL field shape 1:1 mirror) · `exportJsonl()` round-trip safety net · cross-links (§13.20, §13.19.4, P1 5cd7fca). 7 open questions: WAL checkpoint 주기 · old JSONL retention 정책 · 미래 schema migration 규율 · multi-server/replica story (out of scope) · `exportJsonl()` 10^7+ 성능 · `source` column enum 확장성 · 혼합 Node 버전 deployment에서 backend probe cross-compat. Constellation.md 무변경; seed lib 무변경. `feedback_release_versioning_cadence.md` 정합 독립 track — Constellation-reference-doc-only patch가 자체 tag cut. §13.20 BlockerManifest 6 → 4 (이 commit으로 DB-P2-guide blocker close; 잔존: a5bc §F · Fix 2 PR · WS-PROTOCOL §13.14/§13.15 mirror PR · upstream/EG steering).
- **v2.4.2** (2026-05-31) — **§13.16.6 Filter discipline 확장: server-replay noise (`History`) + transport-layer ack를 blocklist 추가.** Constellation-only patch (시드 lib version은 v2.4.1 그대로). v2.4.0 filter discipline은 `ev:"inbound"` scope로 self-emission echo (false-positive #1) 차단; v2.4.2는 두 번째 false-positive class 추가 — `History` payload (서버측 재연결 시 세션 replay; line-level 큼, prior session의 `Delegate`/`WorkerReport` 다수 포함 → naive name-grep이 fresh inbound로 오인) + transport-layer `Ack`/`AckProcessed`/`AckCumulative`/`AckPolicyUpdate` frame. Canonical bash filter 두 단계: (1) `grep '"ev":"inbound"'` → (2) `grep -vE '"name":"(History|Ack|AckProcessed|AckCumulative|AckPolicyUpdate|ServerNotice|AgentList|Status|ConnectionRestored|Heartbeat|Typing|UserPromptAccepted|PersistentAdapterSmoke|OnboardAck|WorkerInboxReceived|EditMessage|MainChanged)"'` → (3) `grep -cE '"name":"(Delegate|UserPrompt|WorkerReport|WorkerAck|AgentHello|Handoff|Report|Command)"'`. Surfacing incident: watcher `bbae2ph8r` 2026-05-31 wake가 server `History` replay payload를 meaningful 로 catch — prior session inbound name-only 매치, fresh external delivery 아님. Constellation.md §13.16.6 Filter-discipline paragraph 재작성; 시드 본문 변경 X (시드는 §13.16.6 anchor reference). Cadence: `feedback_release_versioning_cadence.md` 정합 — Constellation-only patch tag 즉시 cut (다음 seed-lib release 묶지 않음).
- **v2.4.1** (2026-05-31) — **§13.16.6 element-ordering 강화: cursor-tail probe는 unconditional rearm BEFORE 의무.** Discipline-tightening 패치. 6-element turn-end ritual 구성은 그대로; element 1 (inbox cursor-tail probe)와 element 2 (unconditional rearm) 간 mandatory ordering 누락분 보완. Probe 역할을 "telemetry only, never a gate" (v2.4.0 표현)에서 **PRIMARY catch** (armed-window blind spot 1차 수단)로 재정의 — rearm 단독은 자기 base 이하 inbound 회복 못함. 명시 4-step 순서: (a) last-surfaced cursor부터 inbox.log tail → (b) meaningful inbound surface → (c) cursor advance → (d) THEN unconditional rearm. 안티-패턴 paragraph 추가 (2026-05-31 incident anchor): 에이전트가 rearm 먼저 실행 → seq 123 (Status) + seq 127 (Report: 메인 DB P1 ship `5cd7fca` + §13.19 P2 spec 입수 경로 질의) — 직전 watcher SIGSTOP-suspend window에 들어온 inbound 누락; 사용자가 "메인이 회신 대기중이라는데" surface로 강제하지 않으면 turn-end에 자동 probe 했어야 함. Composition paragraph 업데이트하여 element 1 = PRIMARY (armed-window blind-spot catch), element 2 = forward-looking (next-idle-window catch) 명시; BOARD_STALE 게이트는 "safety net"에서 "fail-safe / agent-side discipline 실패 시 backstop"으로 격하. `Constellation.md §13.16.6` + 시드 6종 (Master / Lite / Compact × EN/KO) cascade, verbosity gradient 정합. `feedback_release_versioning_cadence.md` 정책 첫 적용 (catch-up 누적 회피, in-flight cut) — v2.4.0이 장기 catch-up이었고 v2.4.1이 같은 날 단일 discipline-tightening 패치로 ship.
- **v2.4.0** (2026-05-31) — **Constellation 코디네이션 계층 프로토콜 확장: §13.16.6 absolute turn-end ritual (6 elements) + §13.16.7 BOARD_STALE + §13.17 메인-챗 금지 + §13.18 recommend+proceed + §13.19 deadlock 정식 명세 + §13.20 blocker tracking + §13.21 fresh-context defer 안티패턴; `.eux` v1 spec publish + UI 6 / KEY-MGMT WS 프로토콜 draft; EstreUX 시드 6종 cascade (옵션 C 종료); 메인 UI 3·4·5·6 ship에 reference 동기.** 7개 신규/개정 `§13.x` Constellation sub-section이 자율-실행 discipline chain을 형성. (1) **§13.16.6 v2** — turn-end mandatory ritual을 절대-tier 6-element 체크리스트로 격상: inbox.log cursor-tail probe · UNCONDITIONAL watcher rearm (probe는 telemetry, 게이트 아님; cost-asymmetry FP-spawn-harmless < FN-miss-silent-stall; 이전 draft의 conditional rearm RETIRED) · 보드 state 실시간 갱신 · BOARD_STALE 게이트 · **planned 큐 점검 (#442)** · **큰 작업 진입 전 context 구체화** (post-compact resume 보장 위한 durable artifact emit). watcher liveness probe + cursor-tail fix + 4-evidence 확정 armed-window-blind-spot 진단 + bg-task SIGSTOP wall-clock-vs-task-clock 관찰 + filter-discipline self-emission 제외. (2) **§13.16.7 BOARD_STALE 게이트** — rearm 직전 보드 신선도 검사 `git log -1 HEAD --format=%ct > git log -1 -- state.json --format=%ct` (identity-based; 이전 120s 시각-버퍼 버전은 self-trap이라 retire). (3) **§13.17 메인-챗 구조화 선택 프롬프트 금지** — `AskUserQuestion` 류 옵션 UI를 에이전트 메인-챗에서 금지 (보드 projection 없음); `defer-OK` 검토사안 탭 또는 `need-now` UI6로 라우팅; 검토사안 탭 뱃지 "0 → 숨김, 탭 유지" UX discipline. (4) **§13.18 비-분기 선택 recommend+proceed** — fork-of-architecture 아닌 선택에 recommendation 있으면 1줄 announce + 진행; `A/B/C/D?` 목록 escalate 금지; 병렬 가능 시 기본 추정. (5) **§13.19 deadlock 정식 명세** — wait-pattern 분류 (strict / quasi / healthy + livelock=quasi 변종) · 감지 (wait-edge DFS 2-에이전트 + wait-class timeout + §13.16.6 agent-active 보정) · 예방 (우선순위 §13.9 tree + `ReviewSLAAck{eta,SLA-OR-WORK}` + 종료신호 `DONE / BLOCKED{reason,waitingOn} / NEEDS_HUMAN`) · 자동해소 (preemption `PreemptRequest/Force` + deadline + starvation 가드) · escalation 4-tier (ordering → preemption → mediation → human §13.17) · §13.13 위 3-tier ack 계층 (transport / commitment / application). Q2-Q3-Q4-Q6 결정: CNP는 wire vocab 거부 + discipline 차용 유지; mediator hybrid (board adapter 기본 + 3rd-party 에이전트 opt-in); human-fallback bounded-probe × wait-class severity, cap ~3-5/24h; renegotiate-SLA fixed cap N=2 후 human-escalate. (6) **§13.20 blocker tracking + periodic nudge discipline** — idle 진입 전 `BlockerManifest` emit (subject / reason / EG-side action / request-history + count); periodic `BlockerNudge` (env `CONSTELLATION_BLOCKER_NUDGE_CYCLES`, default 1 per ~45min rearm); 4-tier escalation polite → explicit + ETA → hard deadline → human §13.17; §13.19과 구별 (linear-stalled vs cyclic-deadlock). 메인 upstream에 라이브보드 panel 신설 ask (external-blocker-manifest 시각화). (7) **§13.21 fresh-context defer = 안티패턴** — compact는 임계 시점 자동 발화 (에이전트 `/compact` 임의 불가); fresh-context 기대는 wishful·비현실적; planned는 컨텍스트 길이 핑계로 미루지 말 것 (#442 강화: 시간 + 컨텍스트 핑계 모두 금지). §13.16.6 element 6이 *capability* (artifact 구체화), §13.21은 *discipline* (defer 금지). Three-section policy chain: §13.18 (choice-surface) → §13.16.6 #442 + element 6 (work-progress-surface) → §13.21 (time-surface). **`.eux` format v1**: `constellation/reference/docs/eux-format-v1.md` (26.9KB · 9 디렉티브 합계: 5 formal/tier-gated + 4 structural; v0의 8 + `@behavior` 신규; `@deps` → `@ports.deps` 흡수 per RRP A4 amendment, 12-file EG 경험 근거); `EUX-V1-CATALOG.md` (291줄 — profile distribution, per-file matrix, Verify "runtime 6-check matrix" with C1-C6 prefix to disambiguate from spec §4 G1-G6); `WS-PROTOCOL-KEY-MGMT.md` (22KB · 5 message types + 5-state machine + role naming `main|local|collab|upstream` 정합); `WS-PROTOCOL-UI6-SELECTION.md` (25.8KB · 3 message types + 3-state machine; KEY-MGMT과 cohesion 분리). **EstreUX 시드 6종 cascade** — 오래 보류된 옵션 C 종료: `§5 #12 EstreUX 도입 + 인용` 핵심 규칙 + `§ Constellation → ### EstreUX (Estre Universal eXpression)` sub-section을 Master EN/KO + Lite EN/KO + Compact EN/KO에 tier verbosity gradient 정합 cascade. **런타임 업데이트**: `self-wake-watcher` poll interval 15s → 5s (`MAX 196 → 588`, ~49분 rearm 유지); `ws-core.eux` v0.2 fragmented frame 어셈블리 (FIN-비트 + `_frag` 누적 + `_deliver` 단일 진입, `conn.remoteAddr`/`conn.ua` ports); anonymous-HELLO 거부 (`conn.meta.anonymous` 플래그 + origin 로깅 + relay-skip); pre-commit `check-reference-syntax.mjs` syntax gate + `check-reference-regressions.mjs` proposal-002/004 functional guard; `local-ide-agent` 레거시 식별자 sweep → generic `main-agent` per §13.14 redaction. **Reference 동기** 메인 UI 3·4·5·6 ship (521+ insertions): KEY-MGMT 레거시 `RegisterUpstreamKey` 정합 (supersede + transitional alias, v0.2 deprecated, zero-traffic gate로 retire); UI6 impl 정합 (`SelectionPrompt` sender `role === main` tightening, board permission `meta.role !== agent`, 3 wire-path 선언). **Honest history**: rolled-out-then-corrected commit을 follow-up + retired-version note로 유지 (revert 안 함) — `a7911fd` armed-window blind spot이 GC-kill 오진단 대체; `9be6bf0` head-vs-state-commit-time이 120s self-trap buffer 대체; `0185b8d` conditional-rearm은 `852a113` unconditional 격상으로 retire. **Deadlock joint formalization** 메인 upstream RRP `reports/2026-05-31-deadlock-resolution-rrp.md` + EG draft v0.1 (commit `82ab55e`) → §13.19 (commit `32a2c75`). EstreGenesis 에이전트 draft, upstream 라이브보드 메인 협업 review (Delegate seq 70 / 73 / 77 / 79 / 83 / 85 / 88 / 92 / 94 / 98 / 103 / 105 / 109 / 111 / 113 / 120 — 2026-05-31 cycle).
