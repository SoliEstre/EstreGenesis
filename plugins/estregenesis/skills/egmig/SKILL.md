---
name: egmig
description: "Short alias for eg-migration — migrate an EXISTING project (one that already has CLAUDE.md / .cursor/rules / copilot-instructions) onto the EstreGenesis standard via the seed's Migration A or C. Same behavior as /eg-migration; use whichever name you remember."
---

# /egmig → `/eg-migration`

Alias. The procedure lives in one place and this is not a copy of it.

**Read the sibling skill `../eg-migration/SKILL.md` (relative to this skill's base directory) and execute it as written.** The rule-file audit, the M1/M3 classification, the seed URLs and the verification pass are all there.
