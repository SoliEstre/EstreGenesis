---
name: egup
description: "Short alias for eg-upgrade — bring a seeded project up to the current EstreGenesis release (seed delta via Migration B, additive only) and refresh its installed EG plugins. Same behavior as /eg-upgrade; use whichever name you remember."
---

# /egup → `/eg-upgrade`

Alias. The procedure lives in one place and this is not a copy of it.

**Read the sibling skill `../eg-upgrade/SKILL.md` (relative to this skill's base directory) and execute it as written.** The version-marker read, the changelog delta, the numbered additive menu and the plugin refresh are all there.
