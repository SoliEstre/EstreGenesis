---
name: egboot
description: "Short alias for eg-bootstrap — bootstrap a NEW project onto the EstreGenesis standard (fetch the seed tier, install it as .agent/seed_prompt.md, run the seed's Bootstrap mode). Same behavior as /eg-bootstrap; use whichever name you remember."
---

# /egboot → `/eg-bootstrap`

Alias. The procedure lives in one place and this is not a copy of it.

**Read the sibling skill `../eg-bootstrap/SKILL.md` (relative to this skill's base directory) and execute it as written.** Everything — the new-vs-existing guard, the tier table, the seed URLs, the module hand-off — is there.
