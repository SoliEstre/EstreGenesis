---
name: corperate
version: 0.1.0
description: "Pointer alias for the corporate skill — the module's name is spelled Corporate. Use when corperate is typed; it resolves to the same procedure with no duplicated content."
---

# `/corperate` → `/corporate`

Alias. The module is spelled **Corporate**; this pointer exists so the common misspelling resolves instead of failing.

**Read the sibling skill `../corporate/SKILL.md` (relative to this skill's base directory) and execute it as written.** The adoption threshold, the four form axes, the host inventory, the practice-derived role proposal, the desk scaffolding, and the roster/declaration steps all live there.

This file is a pointer, not a copy — the same discipline the vocabulary module applies to definitions. A duplicated procedure is a procedure that drifts.
